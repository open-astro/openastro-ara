<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- Opt OUT of Flutter's experimental "merged UI + platform thread" mode. It
	     changes how the macOS main run loop services timers, which stops the
	     webview_cef (CEF) message pump — an NSTimer on the main run loop driving
	     CefDoMessageLoopWork (CefWrapper.mm) — from firing. Without that pump CEF
	     stalls and the Sky Atlas webview renders white/blank. This regression is
	     Flutter-version-driven, not CEF-version-driven (it broke the embed on both
	     CEF 130 and 149), so the opt-out is the real fix. Remove only once the fork
	     drives the pump via the run-loop-merge-safe path. -->
	<key>FLTEnableMergedPlatformUIThread</key>
	<false/>
	<key>NSBonjourServices</key>
	<array>
		<string>_openastroara._tcp</string>
	</array>
	<key>NSLocalNetworkUsageDescription</key>
	<string>WILMA scans your local network to find OpenAstro Ara servers running on a Raspberry Pi or similar.</string>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
</dict>
</plist>
