# Changelog

All notable changes to OpenAstro Ara are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versioning follows [Semantic Versioning](https://semver.org/spec/v2.0.0.html) —
pre-1.0, breaking changes can happen in any release per the playbook §0.6.

For Ara-specific release tagging convention (`v0.0.1-ara.N`), see
[design/PORT_PLAYBOOK.md §34.5](design/PORT_PLAYBOOK.md). Phase boundary tags
(`phase-N-complete`) are NOT release tags — they're internal milestone markers
used by the port's `port/ara → master` promotion cadence (playbook §22.0).

## How to update this file

Every PR that lands a user-visible change adds a bullet to the **[Unreleased]**
section as part of its diff (convention enforced by the port-driver skill, see
`.claude/skills/port-driver/SKILL.md` step 4). Pick the matching subsection:

- **Added** for new features, endpoints, settings, or capabilities.
- **Changed** for changes in existing behavior.
- **Deprecated** for soon-to-be removed features.
- **Removed** for now-removed features or files.
- **Fixed** for bug fixes.
- **Security** for vulnerability fixes.

Mechanical PRs (pure renames, pure deletes during demolition, doc-only edits to
internal design files) don't need a CHANGELOG entry. When in doubt, add one.

At release time (e.g. `v0.0.1-ara.1`), the **[Unreleased]** section is renamed
to `[0.0.1-ara.1] - YYYY-MM-DD` and a fresh `[Unreleased]` placeholder is added
at the top. This happens in the same commit that pushes the release tag.

---

## [Unreleased]

### Added
- **§38 — the daemon refuses to edit or delete a sequence with an active run.** `PATCH`/`DELETE /api/v1/sequences/{id}` now return 409 Conflict while that sequence's run is Starting/Running/Paused/Aborting, instead of rewriting or deleting the file out from under the live executor. Any client (or curl) is covered — the invariant lives in the daemon now, not just in WILMA's pre-flight probes.
- **§27 — single-client policy in WILMA (client half).** WILMA now claims the control slot on connect (and silently re-claims its own session after a Wi-Fi blip), answers the daemon's takeover requests with an "X wants to connect — Allow / Keep me connected" modal, and treats WebSocket close 4004 as "Session transferred": a modal explains another client took control (the running sequence is unaffected) and reconnecting is an explicit button, never automatic. Denied or unavailable claims degrade gracefully — the live event stream keeps working against older daemons and while another client holds control.
- **§27 — single-client policy: connect handshake + 4004 takeover (server).** The daemon now serves one controlling client at a time: `POST /api/v1/server/connect` claims the slot (a live holder gets a `connection.request` modal over its WebSocket and can allow or reject the takeover; a holder silent for 60 s is swept automatically), `POST /api/v1/server/disconnect` releases it, and `GET /api/v1/server/session` shows who is connected. A displaced client's WebSocket is closed with code 4004. Pre-§27 clients that never call `connect` are unaffected — the event stream and REST keep working unchanged.

### Fixed
- **§28 — unknown sensor temperature is recorded honestly.** A camera that reports no CCD temperature now records "unknown" instead of a fabricated 0.0°C (existing databases upgrade in place; dark-matching for uncooled cameras keeps working across old and new recordings), and exports/views show a blank instead of a fake reading.

### Fixed
- **§28 — sub-second calibration frames are catalogued honestly.** A 0.5 s bias used to be recorded as 1 s (or 0 s when rescanned from FITS — the decimal `EXPTIME` header failed the integer parse); the frames catalog now stores real seconds end-to-end, existing databases are upgraded in place on the daemon's next start, and a camera that doesn't report gain records "unknown" instead of a fake −1.

### Added
- **§54 — overnight problems can now reach your phone.** Pushover and Telegram push channels are live: fill in both of a channel's fields (Pushover app token + user key; Telegram bot token + chat ID) under Settings → Session → Notifications and warning-or-worse events forward automatically — critical ones ride Pushover's high priority, bypassing your phone's quiet hours. The in-app feed stays the source of truth; a provider outage never delays anything.
- **§29 — captures stop before the disk fills, not after.** With the "abort on critical disk space" policy set, each exposure now checks the save volume's free space before the shutter opens and refuses to start when it's critically low — an exposure that can't be saved is wasted sky time. The disk monitor's 60-second poll used to leave a window a burst of large frames could slip through; the default "warn" policy is unchanged (never blocks).
- **§58.10 — nighttime problems ring louder.** While your site sits in astronomical darkness, equipment-impacting warnings are automatically raised one severity level (warning→error, error→critical) so the bedside alarm behaviour engages earlier for a sleeping user — each raised message says it was escalated and why. Daytime severities are untouched; a profile toggle turns it off.
- **§58.8 — the first flip on a profile announces itself.** The one failure mode that actually breaks gear is a new profile with a wrong pause value flipping the OTA into the tripod — so the very first meridian flip on a profile now posts a critical alert and waits 60 seconds before proceeding (stop the sequence if you want to check your settings first). Later flips run silently, and changing the optics train re-arms the one-time announce.
- **§58.7 — flip failures reach you in every mode.** A failed meridian flip now posts a Critical notification even without the §58.9 safety layers enabled (it used to halt the sequence silently), and says whether the guider could be restored. Two quieter degraded paths also speak up: a failed best-effort re-center ("imaging continues on an unverified pointing") and a failed post-flip autofocus ("focus may have drifted").
- **Settings → System → About is real.** The app's identity, version (read from the build), license posture (AGPL-3.0 client / MPL-2.0 daemon, forked from N.I.N.A.), a link to the source repo — and the legally required **open-source licenses page** listing every bundled Dart/Flutter package with its full license text, collected automatically at compile time.
- **§10.6 — sequences can drive a specific switch.** The Set Switch Value instruction is now in the sequence editor's palette (new Switch category) and gained a device selector for multi-hub rigs — target port 3 of *that* power box, not whichever hub happens to be first. Existing and NINA-imported sequences keep their behaviour (no device named = the first hub, exactly as before).
- **NEXTGEN §3.1 — "enough stars to stack?" advice.** Supplying a target position to the Optimal Sub calculator now also predicts how many stars each sub can register (from your aperture, seeing, sky and field of view, via the HYG-validated star-count model) and reports the star-detectability floor: when a sub long enough for read noise would still be too star-poor to align, the window says stars — not read noise — set the floor. Tonight's Sky flags starved fields the same way ("~N stars/sub — thin for registration"). Advisory only; nothing gates on it.
- **§60.9 — the library and calibration screens follow a running capture live.** Every catalogued frame now emits the frame.complete event, and the Image Library, calibration coverage badges, and dark-library build progress refresh automatically as frames land — no more pull-to-refresh while a run fills the catalog.
- **§39.10 — export frames for post-processing.** Select frames in the Image Library and Export downloads a tar of their FITS files via the OS save dialog — ready for PixInsight, Siril, or anything else. That completes the bulk bar: rate, tag, move, export, delete are all live.
- **§65.9 — manual stretch sliders in the frame viewer.** Pick the "manual" palette and shape the stretch yourself: black point, midtone, and white point sliders re-render the full-resolution preview as you drag (debounced to keep the daemon happy on LAN).
- **§40.8 — move frames between sessions.** Select frames in the Image Library and file them under a different session (fixing recovered orphans or snapshots that belong with a real run) via a session picker in the bulk bar.
- **§40 — big catalogs page cleanly.** The Image Library and Calibration session lists load beyond the first 200 sessions with a Load-more button instead of silently truncating.
- **§40.5 — tags and full capture settings in the viewer.** The frame viewer now shows the frame's recorded gain, offset, sensor temperature, focus position, and dimensions, plus editable tag chips — add or remove tags without leaving the image.
- **§40.5 — rate frames from the viewer.** A star row under the preview rates the open frame (tap the current rating to clear); the library strips update immediately.
- **§65 — stretched previews in the frame viewer.** Opening a frame now renders a full-resolution server-side stretch (auto STF by default) with a palette picker — linear, log, asinh, sqrt, equalized — while the thumbnail paints instantly as a placeholder.
- **§50 — Stats CSV exports come from your real catalog.** The Stats dashboard's Sessions/Frames CSV buttons now stream the daemon's catalog (sessions as a per-session rollup with light counts and integration), and the last of the built-in demo data is gone.
- **§40 — find frames faster.** The Image Library's header pills are live: narrow every session's frame strip by filter or minimum rating, and search sessions by target name — active filters highlight, and one tap clears them.
- **§40.6 — Resume Target is real.** The Image Library's Resume Target button now builds a ready-to-run sequence for continuing a multi-night project: the exact sequence the session originally ran when it was recorded, otherwise a per-filter capture plan rebuilt from the lights you took (same filters, exposures, gain, focus, and frame counts) — review, add your slew, and run.
- **§40.8 — bulk frame operations.** Select frames in the Image Library and rate them (1-5 stars or clear), add/remove tags, or delete them — with a confirmation that separates "remove from catalog" from "also delete the FITS files from disk".
- **§40 — the Image Library shows your real catalog.** Session cards, frame strips, and capture-time thumbnails now come from the daemon instead of demo data; tapping a frame opens a pinch-zoomable viewer with its recorded metadata, and every session card can generate matching flats directly.
- **§39.10 — a Calibration screen ties it together.** New Calibration entry in the status bar: browse every imaging session with per-filter summaries and flats/darks coverage badges, generate matching flats from a dialog and land directly on the ready-to-run sequence in the Run tab, and manage the dark library — catalogued dark groups plus the matrix build form.
- **§39.8 — the dark library is real.** The calibration dark library now reflects the frames you've actually captured (grouped by exposure, gain, and sensor temperature) instead of fixture data, and "build dark library" generates a ready-to-run sequence — cooling the camera to each set-point and looping the requested dark matrix — that you review and start like any other sequence. Already-covered combinations can be skipped, and build progress tracks real catalog coverage.
- **§39.5 — "Capture Matching Flats" now produces a runnable sequence.** Generating matching flats for a past session saves a ready-to-run flat sequence: for every filter the session used, it switches to that filter, returns the focuser to the focus position those lights were captured at, and shoots the flats with the same gain and offset — so dust-mote shadows and vignetting calibrate out exactly. Preview-only mode still returns just the plan.
- **§25.5.1 — pick your filter for manual captures.** The Imaging tab's exposure controls gain a Filter dropdown (between Bin and Frame type), sourced from your profile's filter-wheel slot labels — every Take One now records the filter you actually used instead of a hardcoded 'L'. (Live View frames are ephemeral and never catalogued, so no filter is recorded there.)

### Changed
- **§60.9 — BREAKING (wire): sequence run counters renamed `frames_*` → `instructions_*`.** They always counted sequence instructions, not camera exposures; renamed on both the REST run-state and the WebSocket events while the wire has no external consumers (daemon + WILMA ship together). The stream's ordering contract is now documented in `openapi.yaml`: a terminal event may arrive without a preceding `started`, and `sequence.progress` never arrives after the run's terminal event.
- **§60.9 — sequence progress events no longer pile up under fast capture.** A sequence reporting progress at camera-capture rates used to queue one WebSocket publish per tick with nothing bounding the backlog; progress events now coalesce — at most one publish in flight, bursts collapse into a single trailing update carrying the freshest state. Start/pause/complete events are never throttled, and low-rate progress is still delivered instantly.

### Added
- **§37.4/§38 — your filter names now live in the profile and follow you into the sequence editor.** Filter-wheel slot labels persist to the daemon (`/api/v1/profile/filter-wheel/labels`) and load automatically whenever the app connects — so the sequence editor's filter picker offers your filters (not the built-in defaults) on every device, edits in Settings → Filter Wheel save as you commit each row, and everything still works offline with the reference defaults.
- **NEXTGEN §5 — your camera's quantum efficiency fills itself in.** On connect, ARA now recognises the sensor behind most modern astro cameras (IMX571/455/533/294/183/585 and friends, plus classic CCDs) and fills the profile's peak-QE from a small published-data library — one less spec-sheet number to hunt down before the Optimal Sub advisor works. Only ever fills a blank: anything you've entered yourself is never touched, and an unrecognised sensor simply keeps the calculator's documented default.
- **§36.8 — Tonight's Sky shows which targets are in their window right now.** Each row's timing line turns green with a leading "now ·" while the target's dark window is open, and dims once the window has fully passed for the night — so "what should I be shooting at this instant" is answerable at a glance. Purely visual: nothing is hidden or re-ranked.
- **§68.4/§69 — the command palette now searches help, not just settings.** Every in-app help article is searchable by intent — type "equipment hub down" or "alpaca bridge missing" and the palette surfaces the new **AlpacaBridge not detected?** troubleshooting article (install command, service check, subnet notes) right alongside settings hits; activating a help result opens the help sheet. Long help titles now ellipsize instead of overflowing the sheet header.
- **§68.2 — the profile wizard now catches a missing AlpacaBridge up front.** Screen 2 probes the daemon's equipment-discovery path automatically when you arrive; if the bridge isn't reachable you get a clear "AlpacaBridge not detected" panel with the exact install command (`sudo apt install alpaca-bridge`) and a Retry button, and **Next stays disabled** until detection succeeds — so a missing equipment hub surfaces on the spot instead of as mysterious empty device lists on the next screen. Pointing at a non-standard bridge? Fill in the address override and skip detection explicitly.

### Fixed
- **§25.3 — the Switch chip now turns amber while a port is actuating.** Flipping a switch port (or connecting/disconnecting a switch device) used to leave the top-bar Switch chip green as if nothing was happening; it now shows the same busy amber the other equipment chips do for the duration of the action.
- **§36 — a blocked sky-data delete no longer looks like it worked.** Removing a downloaded catalog whose files were locked or permission-denied used to answer exactly like "already removed" (404), so the UI treated still-present files as freed. The daemon now answers 409 with the reason, and the Data Manager shows "the files are in use or protected — close anything using them and try again" so you know to retry.

### Added
- **§25.5.5 — cameras with a simple on/off cooler get a cooler switch.** A camera whose cooler has no temperature regulation (ASCOM `CanSetCCDTemperature=false` but `CoolerOn` implemented) used to show no cooler controls at all; the daemon now probes for the cooler itself at connect (`has_cooler`) and the camera panel shows the on/off switch for it — the set-point field still appears only with real TEC regulation. Older daemons keep the previous behaviour.
- **§64 — Live View renders colour for OSC (colour) cameras.** A one-shot-colour camera's live view used to show the raw sensor mosaic as a grey cross-hatch; it now debayers each live frame into a real colour image (at bin 1×1 — binned readouts aren't a colour mosaic and stay greyscale, as does mono). Uses the same super-pixel debayer + per-channel auto-stretch recipe as capture previews, so live view and previews can't disagree about colour; the live frame's reported dimensions halve accordingly.
- **§36.8 — Tonight's Sky now tells you what the moon is doing to each target.** Every ranked object reports the moon's distance from it (at its dark window's midpoint), tonight's disc illumination, and how much of the object's window has the moon up at all. The panel shows it at a glance: a green **Moonless** chip when the moon stays down for the whole window, otherwise "☾ 42° · 78%" — amber only when the moon is both close and bright. Advisory only, in the spirit of advise-don't-dictate: a bright moon never hides or down-ranks a target (it also appears as a zero-point line in the "Why?" score breakdown), and making it a score input stays a future, user-tunable step.
- **§36 — catalog overlay markers now show their names.** Toggled catalog overlays (Messier, NGC/IC, the type filters…) label their circle markers with the object's designation once you zoom in past a 45° field of view — the brightest ~60 objects in view get names (in the catalog's colour, floating just above the marker), updating as you pan and zoom, deduplicated when an object appears in two toggled catalogs. Wide-field views stay clean: zoom out and the names get out of the way. Second slice of the Catalogs epic (after the drawer + persistence); Sharpless/Herschel lists and AL programs still follow.
- **§59 — autofocus shows live probe progress.** The Focuser panel's Run-autofocus button (and anything polling the job) now sees real per-probe progress — "Autofocusing… (3/9)" — instead of an opaque spinner for the whole multi-minute sweep: the sweep reports each probe as structured progress, and the background job's done/total reflect it.
- **§37/NEXTGEN — the wizard's filters now feed the planner.** The filter names you enter on the wizard's filter-wheel step become your profile's planning filter set on Save — each filter's type (Hα, OIII, LRGB, dual-band…) auto-detected from its name with the same inference as the Settings "seed from wheel labels" button, refinable later in Settings → Imaging → Filter set. Until now the wizard's filter entries were saved nowhere; with this, a fresh setup gets emission-aware Tonight's Sky advice and per-filter Optimal Sub numbers immediately.
- **§37/NEXTGEN — the wizard's camera step now captures read noise and peak QE.** The two sensor values a camera never reports over ASCOM (they come from the spec sheet) can be entered right in the profile wizard — optional fields with plain-language help — so a fresh setup ends fully exposure-planning-ready and the Optimal Sub advisor works without a later Settings detour. Values you already auto-captured from the connected camera (sensor name, full well, e⁻/ADU) are preserved untouched, and an implausible QE entry (a fraction typo like "0.8" meaning 80, or >100) keeps the stored value rather than corrupting it.
- **§59 — a "Run autofocus" button on the Focuser panel.** Settings → Equipment → Focuser gains a one-tap autofocus trigger: it starts the daemon's V-curve sweep with your profile's autofocus settings, shows a live "Autofocusing…" state while the sweep runs, and reports the outcome inline — including the daemon's actual failure reason (curve fit unusable, too few stars, focuser fault) instead of a generic error. Disabled with a hint until a focuser is connected; a double-tap can't start a second sweep.
- **§59 — run autofocus on demand.** `POST /api/v1/equipment/focuser/autofocus` kicks off one V-curve sweep with your profile's autofocus settings as a background job (202 + a job you can poll at `/api/v1/jobs/{id}`; a failed sweep reports why in the job's error). A second request while a sweep is running joins the running job instead of fighting the focuser. This is the server half of a WILMA "Run autofocus" button.
- **§59 — autofocus actually focuses now.** The live V-curve sweep is built: the daemon steps your focuser through the configured sweep (all in one direction, so mechanical backlash biases every sample identically), measures star HFR at each position through the same camera path real captures use (probe frames are never saved to your catalog), fits the focus curve, and moves to the fitted best — restoring your starting position if anything fails, per your autofocus settings. This makes three things real at once: the **Run Autofocus** step in imported NINA plans, autofocus-after-N-exposures triggers, and the meridian flip's **re-focus after flip** (a failed re-focus logs and continues — it never aborts your night). A sweep that can't trust its data — too few stars, a flat curve, a best-focus estimate outside the sweep — fails loudly instead of guessing.
- **§38 — imported "Center and Rotate" steps now actually centre the target.** A NINA plan's `CenterAndRotate` executes for real: the daemon runs its plate-solve centering loop (capture → solve → sync → re-slew until within your profile's threshold) on the step's own coordinates, and a centre that doesn't converge fails the step loudly instead of quietly ruining every following frame. With a rotator connected the step still fails loudly (rotation isn't wired yet — better than silently mis-framing a rotator rig); without one, the position angle is preserved in the plan but skipped, exactly as NINA behaves without a rotator.
- **§36 — Catalog overlays survive relaunch.** The Catalogs panel's toggles now persist the same way the Display panel's layers do: turn on Messier and the galaxies filter tonight, and they're re-fetched and redrawn automatically the next time you open Ara (namespaced `cat:` keys riding the existing planetarium prefs round-trip — no new storage).
- **§36 — a Catalogs panel on the planetarium: overlay Messier, Caldwell, NGC/IC, and object-type filters.** A new **Catalogs** button beside Display opens a drawer listing the daemon's derived catalogs ("Catalogs": Messier, Caldwell, NGC, IC) and OpenNGC type filters ("Types": galaxies, open/globular clusters, planetary/emission/reflection nebulae, supernova remnants). Toggling one draws its objects as colour-coded circle markers on the sky (each catalog keeps a stable colour; the daemon serves the brightest objects first). Requires the OpenNGC deep-sky package — the panel says so and points at the Data Manager when it isn't installed. First slice of the Catalogs epic: names/labels, Sharpless/Herschel cross-reference lists, and Astronomical League programs with progress tracking come in following slices.
- **§38 — Pause and Resume actually work now (server).** Pausing a running sequence suspends it at the next instruction boundary — the in-flight instruction (an exposure, a slew) always finishes first, then the run holds between instructions, reports `Paused`, and emits `sequence.paused`; Resume picks up exactly where it left off (`sequence.resumed`). The pause only reports once the engine has genuinely suspended, never on the mere request, and Abort/Stop always win over an active pause. Works at every nesting depth of the sequence tree; after a long pause, triggers (like a meridian flip) are re-evaluated fresh on resume. One scope note: a **parallel** block pauses at its entry only — bare instructions already running concurrently inside it finish out (nested sequential containers inside a branch still pause at their own boundaries).
- **§38 — Pause and Resume are back on the sequencer toolbar (client).** With the daemon's pause now real, the toolbar re-offers **Pause** while a run is executing, and the Run button relabels to **Resume** while a run is paused — the same wholesale re-surface the removal in #551 planned for. The run-state chip reports `Paused` the moment the engine actually suspends (the daemon pauses at the next instruction boundary, so the in-flight instruction finishes first), driven by the re-enabled `sequence.paused`/`sequence.resumed` events the client's WS fold already understood.
- **§36.8 — Tonight's Sky can now be planned for a different rig or a mosaic without editing your profile.** `GET /api/v1/planning/tonight` accepts optional optical-train overrides (`focalLengthMm`, `reducer`, `sensorW`, `sensorH`, `pixelUm` — each replaces just that one field of your active profile's optics for the request) and a mosaic tile count (`mosaicX`/`mosaicY`, default 1, up to 20 per axis) that enlarges the framing field of view. Absent overrides behave exactly as before (your profile's optics, single frame), so "what's worth shooting if I add a reducer / shoot a 2×2?" no longer means changing your gear settings.
- **§36.8 — Tonight's Sky now docks beside the planetarium, with a working recentre.** The Planning tab's **"Tonight's Sky"** button opens the ranked panel side-by-side with the planetarium (the sky view shrinks to make room, rather than the panel covering it); tap it again to go back to the full-screen sky. Each target now has a **recentre** button (the location pin) that flies the planetarium straight to that object. (FOV/mosaic controls still to come.)
- **§36.8 — The Tonight's Sky panel now shows the equipment-aware plan, not just a flat list.** Each target carries a colour-graded **0–100 score badge**, a **framing-fit chip** for your active optical train ("Fills frame" / "Small" / "Too big"), and a compact **timing line** — tonight's dark window (in your local time), dark hours available, hours still remaining, and transit. Tap **"Why?"** to expand the score's reason tags, and add a target to a new sequence per row. Low-scoring/low-altitude targets stay visible (ranked lower, never hidden — advise, don't dictate). (FOV/mosaic controls land in a following slice.)
- **§36.8 — Tonight's Sky is now equipment-aware: it ranks targets by a transparent 0–100 "worth shooting tonight" score for *your rig*.** Each target gets a **framing fit** for your active optical train (too small / good / too big — a wide nebula that frames beautifully at 448 mm overflows the sensor at 3000 mm, and the list reorders accordingly), a **0–100 score** blending framing (dominant, 35 pts), dark hours tonight (25), peak altitude/airmass (20), surface brightness vs your Bortle sky (12), and magnitude (8), plus the short **reason tags** that explain *why* it scored what it did. The planner now **advises rather than dictates**: instead of only listing what's already up at the query instant, it surfaces every target with a real dark window tonight — including **not-yet-risen** ones and low-but-spectacular targets — ranked, never silently dropped. Each also reports the **dark hours still remaining** from now. (Mosaic and per-request optics overrides land in a following slice; this uses your active profile's optics.)
- **§36.8 — Tonight's Sky now ranks the real OpenNGC catalog, with a visibility window for each target.** The planner replaces the 20 hardcoded objects with the full installed OpenNGC deep-sky catalog (culled to realistically-shootable magnitudes), and reports per object: its **visibility window tonight** (above your site horizon *and* the sky dark enough for your twilight setting — civil/nautical/astronomical), its **transit time**, and the **max integration hours** that window allows. Twilight gating uses a low-precision Meeus solar-position model. With no sky-data installed it gracefully falls back to the built-in starter list. (Equipment-aware framing/scoring lands in the next slice.)
- **§36 — the Planning sky view is becoming a real planetarium (Stellarium Web).** Foundation slice: the Planning tab now embeds the **Stellarium Web Engine** (a WebGL planetarium) in place of the Aladin atlas, so you get a true alt/az sky — real horizon, atmosphere, planets, satellites, constellation art — pointed at your profile's location with a live clock. The engine and its ~4.2 MB of sky data are bundled and served locally, so the planetarium works **fully offline**. The engine is AGPL (built unmodified from source; see `assets/stellarium/README.md` + `LICENSE-AGPL-3.0.txt`) and driven across the CEF process boundary via a small JS bridge. GoTo on object selection, camera-FOV framing + mosaic, Tonight's Best discovery, and the sequencer hand-off build on this in following slices.
- **§36 — click any star (or DSO) to identify it, SkySafari-style.** Tapping an object in the planetarium opens a panel with its name, magnitude, RA/Dec, Az/Alt and rise/set. The bundled Stellarium engine only made *labelled* objects (DSOs, the brightest stars) selectable, so this needed an **engine rebuild**: every rendered star now registers a clickable hit-area (capped at the brightest ~4000 per frame so dense Milky-Way fields stay fast), and bright stars show their names on the sky. A clicked star resolves to its friendliest designation — a proper name when it has one, else a catalogue id (HD/HIP/TYC/Gaia) — and never renders blank. (Engine source change lives in the `stellarium-web-engine-build` repo; the rebuilt WASM is vendored in `assets/stellarium/`.)
- **§36 — mount controls + a horizon safety gate on the planetarium object panel.** A selected object now offers **Center / GoTo / Stop / Home**: GoTo slews the connected mount, Stop aborts the slew, Home sends it to its home position. GoTo refuses a target that's **below the horizon now** (or that never rises from your site) with a clear reason — including the rise time when known — instead of silently doing nothing, so the scope never swings at the ground.
- **First-run server discovery now rescans on its own.** The connect screen re-runs mDNS discovery every few seconds (so a daemon started *after* the app appears without any action), adds a **⟳ Rescan** button for an immediate pass, and keeps manual host/port entry as the always-works fallback. The old screen did a single one-shot scan and then sat on "Scanning…" forever even as it claimed to be rescanning.
- **§36 — the local horizon now draws on the Planning sky atlas.** The Aladin atlas overlays your site's horizon — the curve (in RA/Dec) the horizon traces for your profile's latitude/longitude at the current sidereal time, plus a **Zenith** marker and **N/E/S/W** cardinal labels — so you can see at a glance what's up and which way you're looking. It's fetched from the `/api/v1/planning/horizon` endpoint and refreshes each time you open Planning. (Aladin is an equatorial atlas, not an alt/az planetarium, so the horizon is a curve that moves with your location and time.) Below-horizon shading, a planning-time picker, and a custom terrain horizon follow in later slices.
- **§36 — Planning horizon endpoint (server).** New `GET /api/v1/planning/horizon` projects the active profile's local horizon onto the equatorial (RA/Dec, J2000) sky for a given time (`?at=<ISO-8601>`, default now), so the Aladin atlas can overlay it. Returns the horizon as a closed RA/Dec curve (the profile's `DefaultHorizonAltitudeDeg` swept through azimuth 0–360°), the zenith point, and the N/E/S/W cardinals — all computed from the site latitude/longitude and the local sidereal time. (Aladin is an equatorial atlas, not an alt/az planetarium, so the horizon is a curve that moves with location and time.) The client horizon overlay + time control build on this in follow-up slices.
- **§37.5 — manual mount control: GoTo, a direction pad, and a speed picker.** The Mount panel now has a **Manual control** section with a **GoTo** (enter RA hours / Dec degrees → slew there), an 8-way **press-and-hold direction pad** (N/S/E/W + corners; hold to move, release to stop) driven by ASCOM `MoveAxis`, and a **Speed** picker populated from the mount's reported axis rates. A centre **Stop** (and the existing Stop) halts all motion as the deadman backstop. New `POST /api/v1/equipment/mount/moveaxis` endpoint; the GoTo reuses the existing slew endpoint. (This also unblocks parking on mounts like iOptron that won't park from the Home position — slew somewhere first, then Park.)
- **§37.5 — a Home button on the Mount panel.** Sends the mount to its homing switch (`POST /api/v1/equipment/mount/home` → ASCOM `FindHome`), shown next to Park/Unpark/Stop only when the mount reports `CanFindHome`, and disabled while parked (unpark first) or busy. The daemon already used `FindHome` from the sequencer; this exposes it to the panel.
- **§52.1 — one-tap Reconnect for every equipment device.** Each device's settings panel (and the Switch panel) now has a **Reconnect** action that re-establishes the last-connected device without re-running discovery — handy after a gear power-cycle or a dropped connection. It reuses the daemon's remembered-device store via new `POST /api/v1/equipment/{type}/reconnect` endpoints (Switch reconnects every remembered switch); a 404 means nothing's been connected for that type yet, so the UI points you at Connect…/Choose… instead.
- **§38 — skip the current target mid-run.** A new `POST /api/v1/sequences/{id}/skip-current` cancels whatever the running sequence is currently executing (e.g. a target that's drifted out of position) and advances to the next item — handy alongside the existing `AboveHorizonCondition`/`AltitudeCondition` sky-position gating. (`SequenceRootContainer.SkipCurrentRunningItems()` was already in the domain; this exposes it through the run engine + REST.)
- **§38 — a Skip button on the sequencer toolbar (client).** The run controls now offer **Skip** (between Run and Abort), enabled only while a run is actively executing an item and not already aborting. It calls the `skip-current` endpoint so the user can drop a target that's no longer well-positioned and let the run move on — without aborting the whole sequence.

### Changed
- **Licensing — the WILMA client is now AGPL-3.0-or-later; the daemon stays MPL-2.0.** The repo carries a per-directory license split: `client/openastroara_client/` gains its own `LICENSE` (GNU AGPL v3 or later), while the server/daemon and everything else remain under the Mozilla Public License 2.0 (required by the NINA-derived code's lineage, which is unchanged). The client is wholly original work, and AGPL's network clause keeps derived clients open even when they're served from a device rather than shipped. The two halves communicate over REST/WebSocket as separate programs, so the licenses apply independently — see `NOTICE.md` and `design/PORT_DECISIONS.md` (2026-07-01). Existing MPL-2.0 revisions remain MPL at those revisions; this applies going forward.
- **§36.8 — Tonight's Sky inclusion changed from "up right now" to "has a dark window tonight".** Targets are now listed when they have any real dark window in the night (so not-yet-risen ones appear), and ranked by the new equipment-aware score instead of current altitude. A target with no dark window tonight is the only thing dropped.
- **§36 — the planetarium now renders in the platform's *native* webview; CEF was removed entirely.** The Stellarium planetarium renders through the OS web engine via `webview_all` — **WKWebView** (macOS/iOS), **WebView2** (Windows), **WebKitGTK** (Linux), **System WebView** (Android) — replacing the bundled CEF/Chromium (`webview_cef`). CEF was compiled in and registered on every platform regardless of build flags; it froze under sustained offscreen-render load on macOS, and on Linux its `libGL`/CEF init broke Flutter's GTK OpenGL context outright (the whole app failed to render — a stock Flutter app on the same machine renders fine). Dropping it collapses the renderer to one code path, removes the multi-process CEF helper scaffolding + the hundreds-of-MB Chromium download from the build, and lets the macOS Hardened-Runtime entitlements shed the CEF-only JIT/executable-memory grants. It also unblocks Linux and makes Android/iOS feasible (CEF is desktop-only). The dead Aladin atlas widget (long superseded by the Stellarium engine) was removed in the same pass.
- **§36 — planetarium pan/zoom is now native; on-screen pads removed.** With the webview giving proper trackpad/mouse input (scroll to zoom, drag to pan), the corner direction pad and the +/- zoom cluster were redundant and were removed for a cleaner full-bleed sky. The bottom "GoTo here" reticle was also removed (clicking objects directly replaces it). The Flutter DEBUG ribbon is also hidden (it overlapped the connect screen's Rescan action).
- **Equipment panels: clearer copy + a tidier Filter Wheel.** Removed the internal spec-section labels that leaked into the UI (the "§52.1 connection lifecycle" hint under Auto-connect on the Camera/Mount panels, and the "§37.11"/"§29.3.2" references). The Focuser panel's dead autofocus placeholder rows are gone — it now links to the real, editable settings under **Imaging → Autofocus**. On the **Filter Wheel**, each live slot now shows its **driver slot number** (0-indexed, matching "Current filter (slot N)"); the focus-offset readout is relabelled "focus offset" and hidden entirely when every slot reports 0 (so wheels like the ZWO EFW don't show a column of zeros); and the local **"Slot labels (for sequences)"** section is hidden while a wheel is connected (its driver names are authoritative) and shown only for offline sequence authoring.
- **§36 — the macOS minimum is now 12.0 (Monterey); Catalina (10.15) and Big Sur (11.x) are no longer supported.** The §36 Sky Atlas moved to CEF 149, whose framework requires a macOS 12.0 deployment target — so the app's `MACOSX_DEPLOYMENT_TARGET` was raised from 10.15 to 12.0. Users on 10.15/11.x can no longer run the desktop client.

### Fixed
- **Equipment status no longer shows stale "connected" green after the server drops.** The top-bar device chips (CAM/FW/FOC/MOUNT/ROT/GUIDE/FLAT/SW/WX/SAFE/DOME) and the device panels in Options mapped each device's *last-known* status straight to the dot/layout, so when the server was killed or became unreachable they kept rendering a false green long after the app had lost contact. A new `serverLinkUpProvider` (true only while the live server link is fully connected) now gates them: link down → chips go grey (disconnected) and each device panel shows a clear "Server disconnected — device status is unavailable" state instead of stale connect/disconnect controls. They repopulate with real status the moment the link is back.
- **§60.9 — the live server WebSocket no longer drops every fresh connection (status was stuck "Reconnecting").** Two compounding bugs: (1) the daemon's resume phase enforced its 5-second "send a resume request" window by cancelling its `ReceiveAsync`, but a cancelled `WebSocket.ReceiveAsync` *aborts* the socket (close 1006) — so any client that stayed silent past 5s (i.e. every fresh subscriber) was dropped, then reconnected and dropped again, forever. The resume phase now races the receive against a delay *without* cancelling it, handing the still-pending receive to the main loop on timeout, so a silent client stays connected as a fresh subscription. (2) The client only sent a resume frame on a *re*connect; a fresh connect sent nothing, so it tripped (1) every time. The client now always sends a resume frame first (token `"0"` when fresh), which also draws a resume-response back that flips the link to connected. The status chip is also relabelled to the explicit client↔server link — **Server connecting… / connected / reconnecting… / disconnected** — so it can't be mistaken for a device/camera state.
- **Mount manual-control: a held direction button now stops if the mount goes busy.** On the §37.5 direction pad, if the mount became busy from another source (a sequence step, a GoTo) while a direction was physically held, the button greyed out but the axis kept moving until the user released. The press-and-hold button now dispatches the stop on the disable transition (not only on release/teardown), so a held nudge halts as soon as the control disables. (Stop / AbortSlew remained the deadman regardless.)
- **Mount Park now works (it was silently failing).** Two issues: (1) the Park button POSTs `/equipment/mount/park` with no body, but the endpoint required a `[FromBody] ParkRequestDto`, so model binding rejected the bodyless request (400/415) and the mount never parked — the body is now optional (its only field is an optional reason, defaults to null). (2) Some mounts (e.g. iOptron) won't execute a park from a stationary state, so the daemon now enables tracking before issuing Park — exactly as ASCOM ConformU/NINA do. (Unpark/Stop were unaffected; Slew always sends RA/Dec.) Note: a mount that parks to a position *other* than its home/zero may still no-op a park issued from the home position — slew it to a normal pointing position first (see the new Manual control GoTo).
- **Every device panel now offers Reconnect again after you disconnect it.** The device services keep the last device after a disconnect and report it with `state == disconnected` (a non-null status GET, not a 404), but the shared `EquipmentConnectionCard` only showed its disconnected layout — the one carrying the **Reconnect** + **Connect…** buttons — when the status was null. So a device disconnected during the session rendered the *connected* layout (just a Disconnect button), with no way to reconnect it short of re-discovering via Connect…. The card now treats a `disconnected` status the same as a missing one, so Reconnect reappears the moment you disconnect — across all nine equipment panels.
- **The top-bar FLAT chip now actually turns green when you connect a flat panel.** The chip was wired to the live flat-panel provider, but the Flat Panel settings panel was the one device panel that still connected through the old wizard-style row (`AlpacaDeviceRow` on `equipmentConnectionProvider`) instead of through that provider — so connecting a flat never told the chip's provider, which had stopped polling after its initial "disconnected" read, and the dot stayed grey. The Flat Panel panel now uses the same shared `EquipmentConnectionCard` as the other eight device panels (connect/disconnect/reconnect on the live `flatPanelProvider`), with a live cover/light readout; connecting — or turning the calibrator light on — now lights the chip and shows the state in the panel. This also gives the flat panel the same disconnected-state **Reconnect** affordance as every other device.
- **§52.1 — switches now auto-reconnect on boot like the rest of your gear.** Switch was the one device type the daemon never re-established on startup: the selection store only remembered one device per type (so a multi-switch rig couldn't round-trip), there was no Switch case in the auto-connect service, and no Switch auto-connect-on-boot toggle. Now every connected switch is remembered independently (by its Alpaca device number), the auto-connect service reconnects them all on boot, and the Switch panel has an **Auto-connect on boot** toggle (default on). So after a daemon/rig restart your switches come back without manual reconnection — matching camera/mount/focuser/etc.
- **The top-bar FLAT (cover calibrator / light panel) chip now shows live status.** It was a hardcoded placeholder that always rendered disconnected — so a connected light panel never lit up at the top. It now watches the flat-panel status provider like the other equipment chips: green when connected, amber while the cover is moving, and routes to the Flat Panel settings panel on tap. (The daemon already fully supported CoverCalibrator/flat devices — discovery, connect, control, auto-connect; only the client chip was static.)
- **§36 — the macOS client now actually runs CEF 149 multi-process, and the native macOS build is no longer broken.** The preceding Sky Atlas change raised the client to **C++20** and documented CEF 149, but left the `webview_cef` submodule pinned at **CEF 130** — whose `libcef_dll_wrapper` doesn't provide the wide-char stream operators (`operator<<(ostream&, char16_t/wchar_t const*)`) the C++20 build links against, so `flutter build macos` failed with *undefined symbols* (client CI runs `analyze + test` only and never compiles the native Runner, so the break slipped through the gate). The submodule is bumped to the CEF 149 multi-process build, and the macOS Runner now embeds the **full five-helper set** (base + `GPU`/`Renderer`/`Plugin`/`Alerts`) Chromium-on-macOS requires — a single helper made the GPU subprocess abort with `gpu_process_host error_code=1003`. All five helpers sign with the committed `Runner/Helper.entitlements`, and `macos/CEF_HELPER.md` is reconciled to the multi-process model. Verified on-device: the client launches multi-process (`gpu-process` + `renderer` + `utility`) and Aladin renders.
- **§36 — the Planning Sky Atlas renders on CEF 149 instead of crashing/blanking.** Moved the embedded Aladin webview to CEF 149 with software-WebGL offscreen rendering and lifecycle fixes: the bootstrap document is written to a temp file and loaded via a short `file://` URL (a self-contained `data:` URL exceeded Chromium's ~2 MB max-URL length once the ~1.9 MB engine is inlined, so CEF silently rejected the navigation and left the atlas blank); the tab is kept alive in an `IndexedStack` so a rebuild can't tear down the OSR browser mid-flight; and CEF is shut down cleanly on app exit (bounded by a timeout so a hung teardown can't wedge the quit). The macOS App Sandbox is disabled on the host — CEF 149's multi-process Mach rendezvous registers a PID-suffixed *global* bootstrap name that the sandbox denies, aborting `CefInitialize`; this is acceptable for the Developer-ID-distributed desktop control app (not Mac App Store) and documented in `macos/CEF_HELPER.md`.
- **§38 — a loaded/imported sequence now wires up its parent chain, so run-engine features that walk to the root work.** A serialized sequence body stores each node's `Parent` as null; on deserialize the parent links weren't re-established, so a running instruction couldn't find its root container — breaking running-item tracking (and therefore skip-current). `SequenceContainer`'s `[OnDeserialized]` hook now re-parents its children (Newtonsoft fires it bottom-up, so the whole tree is wired by the time the root's hook runs).

- **§38 — imported autofocus / center-and-rotate steps now render as first-class blocks in the editor.** Building on the server-side port (#594), the client catalog now knows `RunAutofocus`, `CenterAndRotate` (coordinates + position-angle editable) and the `AutofocusAfterExposures` trigger, so imported plans containing them show real labels/icons instead of generic fallbacks.
- **§38 — autofocus, center-and-rotate and autofocus-after-N-exposures now import from NINA too.** `RunAutofocus`, `CenterAndRotate` (coordinates + rotation angle preserved) and the `AutofocusAfterExposures` trigger were the last high-frequency NINA `$type`s still dropped on import; they now resolve to first-class ported types. (Their run-engine execution into the §59 autofocus / §28 centering services is a follow-up — for now an executed step fails loudly rather than silently skipping focus/centering.)
- **§38 — imported NINA targets, Smart Exposures and dither triggers now render as first-class blocks in the sequence editor.** Building on the import + type-normalization fixes, the client catalog now knows `DeepSkyObjectContainer` (a target block, labelled by its target name — e.g. "T Cas" — rather than a generic container), `SmartExposure` (NINA's per-filter imaging block), and the `DitherAfterExposures` trigger (with its after-N-exposures cadence editable). Imported plans read by target with proper labels and icons instead of unlabeled rows.
- **§38 — an imported NINA sequence now renders with real instruction names in the editor, not generic placeholders.** The importer persisted the raw NINA body, so every node kept its original `NINA.Sequencer.*` type name — but the editor/catalog identify instructions by the daemon's own `OpenAstroAra.Sequencer.*` names, so an imported sequence showed a tree of unlabeled rows. Import now rewrites each `$type` to its canonical OpenAstroAra form (only when it resolves to a real type — an unsupported instruction is left exactly as-is, with all of its data, and reported in the import warnings), so imported sequences read identically to ones built in the editor. The raw NINA upload is still kept verbatim as an audit copy.
- **§38 — importing a real NINA sequence no longer drops the bulk of the plan.** NINA Advanced Sequencer exports are built almost entirely from three `$type`s the daemon didn't recognize — `SmartExposure` (the per-filter imaging block), `DeepSkyObjectContainer` (the per-target block carrying the target name + coordinates) and the `DitherAfterExposures` trigger — so every one degraded to an `Unknown*` placeholder on import and the imported sequence lost its targets and exposures (a 2.8 MB / 228-target file imported as a near-empty tree). All three are now ported as first-class types: a target imports with its name, framing rotation and RA/Dec intact; a Smart Exposure keeps its filter switch, exposure-count loop and dither cadence; and the tree round-trips back to NINA JSON unchanged. (Faithful port — `SmartExposure`/`DeepSkyObjectContainer` subclass the existing `SequentialContainer`, so they also execute as ordinary nested containers.)
- **§36 — the macOS app no longer crashes (SIGSEGV) rendering the Planning Aladin atlas.** CEF 130's renderer was forced **in-process**, and Chromium 130's Rust `fontations` font backend segfaults on certain glyphs — so the whole app died (`Chrome_InProcUtilityThread` → `fontations_ffi…`) instead of, at worst, a recoverable renderer. The `webview_cef` submodule is bumped to the fork's multi-process build (helper-bundle model + the `fontations` workaround) and the **helper subprocess target(s) are wired into the macOS Runner** so CEF runs its renderer/GPU/utility work out-of-process (the stable, default Chromium model). The host gains the JIT / `disable-library-validation` entitlements CEF requires to load its separately-signed framework. (This later moved to CEF 149 with the full five-helper set and the App Sandbox turned off — see the CEF 149 entries above; `disable-library-validation` keeps distribution to Developer-ID / direct, not Mac App Store, which is how CEF apps ship.)
- **§26 — a clean `Debug` build of the daemon no longer fails on current .NET SDKs.** `InvariantGlobalization` was set only in the `Release` configuration, so a Debug build ran with the machine's locale active. Under a newer SDK's analyzer set (10.0.3xx) that turns the whole CA13xx locale-sensitivity family (CA1305/CA1307/CA1308/CA1310 — `IFormatProvider`/`StringComparison`/`ToLowerInvariant`) into build-breaking errors for ~59 `Sqlite*`/parse call sites in the Server project, so `dotnet build -c Debug` failed on those SDKs even though `Release` (and CI, which builds Release) stayed green. The daemon only ever speaks invariant/wire formats (ISO-8601 timestamps, snake_case keys, numeric REST values), so `InvariantGlobalization=true` now applies to **every** configuration: a Debug build behaves identically to the shipped daemon (no machine-locale date/number parsing skew in dev) and the analyzers correctly stand down in Debug too. Root-cause fix — no per-call-site `IFormatProvider` churn, no Debug/Release globalization divergence.
- **§36/§25.5 — the camera/sensor FOV box now draws when framing in Planning.** Frame mode's FOV overlay (and the whole optics-derived framing) silently showed nothing because the Planning tab never loaded the profile's optics — only **Settings → Optics** ever hydrated `opticsSettingsProvider`, so in a Planning-only session it held the zero default, `frameFovBoxProvider` returned null, and the camera's sensor size never appeared on the atlas. The Planning tab now hydrates the profile's optics (focal length + sensor size + pixel scale) from the daemon on mount, so the FOV rectangle/mosaic draws from the user's actual camera + scope. (The FOV math, overlay JS, and framing controls were already complete — only the load was missing.)
- **§25.3 — the top-bar equipment icons are now live + clickable (all devices, not just the guider).** The app-shell equipment status row (CAM / FW / FOC / MOUNT / ROT / FLAT / SW / WX / SAFE / DOME) was hardcoded to a grey "disconnected" dot with no tap handler — only the GUIDE chip was wired — so connecting equipment via AlpacaBridge showed no green dot and the icons did nothing. Each chip now watches its device's live status provider (added by the §25.5 conformance work) and shows a green dot when connected (amber while a move/op is in flight, blue while connecting, red on error), and tapping any chip jumps to that device's **Settings → Options** panel to connect/control it — mirroring how the guider chip already worked. Switch is handled as multi-instance (connected if any switch is); the flat panel has no live status feed yet, so it stays grey but is clickable to its panel.
- **§37 — finishing the wizard "saved" a profile that never appeared.** Tapping **Save Profile** / **Save & Exit** created and activated the profile on the daemon (verified: the file-based store writes atomically), but the wizard only refreshed the client's cached profile list when launched from the Profile-management screen — **not** from the app-shell "Run profile wizard" launcher or the Settings "Open wizard" panel (those passed no `onComplete`). So the save succeeded, the wizard closed, and the stale list made it look like nothing was saved. The wizard now invalidates the profile-list cache itself on a successful save, so the new profile shows up regardless of where the wizard was opened from.
- **§6.4/§37.2 — the wizard can now discover + assign a Switch device.** The equipment-assign screen (Screen 3) showed the **Switch** slot greyed-out with "Multi-switch support is in progress (§6.4)" and no **Choose** button, so a user couldn't pick a switch during initial profile setup. Now that multi-switch is wired end-to-end, Switch discovers and assigns exactly like every other Alpaca slot (it maps to the daemon's `discover/switch` endpoint). The wizard assigns one switch as the profile's default; additional switches are added at runtime from **Settings → Switch**. (Also generalizes the slot model — every slot is now discoverable, removing the special-cased non-discoverable branch.)
- **§70.5 — sequence share-export returned a bogus placeholder for unknown ids + advertised a dead download route (server).** `POST /api/v1/sequences/{id}/share-export` synthesized an empty "Unknown sequence" share (body `{}`) for an id that doesn't exist instead of the `404` the endpoint already advertised, so exporting a deleted sequence looked like it succeeded. It also populated `SequenceShareDto.DownloadUrl` with a `/share/payload` route that was never implemented — diverging from the profile-share contract, where the share JSON rides inline in `Manifest` (the client writes it straight to the chosen `.araseq.json` file) and `DownloadUrl` is null. `ShareExportAsync` now returns null → `404` for an unknown id, and the DTO mirrors `ProfileShareDto` (`DownloadUrl` nullable, null in v0.0.1; `PayloadBytes` is the inline manifest's UTF-8 byte size). Client export UI is the next slice.
- **§6 — the Switch panel stayed stuck on "Connecting" after Add switch (client).** The daemon's switch connect is 202-accepted + runs in the background, so a freshly-added switch first reads back as `connecting`; the panel re-read the list once right after the connect and then never again (the provider is pull-on-demand), so the card sat on **Connecting** until the user re-opened the panel — looking like the connect failed even though the daemon had connected. The panel now polls (1.5 s, no-op when nothing is connecting) while any switch is mid-connect, so a card settles to its real state on its own. (Verified end-to-end against a live AlpacaBridge: both an iOptron iMate PowerBox and a Player One thermal switch connect and report their ports.)
- **§6 — equipment discovery parsed every device to blanks (client).** `DiscoveredDevice.fromJson` read **camelCase** keys (`uniqueId`, `hostName`, `alpacaDeviceNumber`, …) with a comment claiming the daemon emitted camelCase, but the daemon serializes with `SnakeCaseLower` (so the wire is `unique_id`, `host_name`, `alpaca_device_number`, …). Every discovered device therefore came back with a blank name + empty `uniqueId` + device number `0`, breaking the whole discover→select→connect flow for **all** equipment types (the chooser showed nameless rows; a "connection" pointed at a default device). Now reads snake_case (with a camelCase fallback so either shape parses), with a model test pinning the daemon's real wire shape. This was the prerequisite blocking connecting Switch (and other) devices from the client.
- **§29.9 — `GET /api/v1/server/logs/download` no longer 500s on the active log.** The download streamed the live log file while the Serilog sink kept appending to it; `Results.Stream` set `Content-Length` from the file size at open time, so the copy ran past that length (the file grew mid-flight) and Kestrel aborted with `Response Content-Length mismatch: too many bytes written`. The download now serves a snapshot capped to the file's length at open (a small `LengthCappedStream` over the `FileStream`), so `Content-Length` matches the bytes sent and the response completes. This is what surfaced as "Download daemon log" failing in the Support tab.
- **§54 — the in-app daemon log viewer is now selectable/copyable (client).** The Support-tab log rows were plain `Text` widgets, so a user couldn't select or copy a log excerpt. The list is now wrapped in a `SelectionArea`, enabling drag-select across rows and copy (Cmd/Ctrl+C) — so a user can paste a snippet into a bug report without downloading the whole file.
- **§54 — the macOS client can save downloads again (Download daemon log / bug-report bundle).** The sandboxed macOS app was missing the `com.apple.security.files.user-selected.read-write` entitlement, so writing the file the user picked in the save dialog failed with `PlatformException(ENTITLEMENT_REQUIRED_WRITE)`. The entitlement is now granted in both `DebugProfile.entitlements` and `Release.entitlements`, so the OS save dialog can write to the chosen location.
- **§38 — the sequencer toolbar no longer offers an inoperative Pause control.** The daemon's sequence `PauseAsync` is an accepted no-op — the headless execution engine has no pause hook yet, so it deliberately never suspends a run or emits a `paused` event. The toolbar nonetheless showed an enabled **Pause** button (and a dead Run→Resume relabel) while a run was active, so clicking it returned `202 Accepted` but did nothing, with the run-state never reporting `Paused` — misleading the user into thinking the run had paused. Pause/Resume are now removed from the toolbar until the engine grows real run suspension; Run / Abort are unchanged. The run-state model (including the `paused`/`Paused` state) is retained so Pause/Resume can be re-surfaced wholesale when the daemon supports it.

### Removed
- **The dead `VVVV.FreeImage` dependency (and its NU1701 build warning).** `OpenAstroAra.Image` still referenced `VVVV.FreeImage` (a .NETFramework-only package), but nothing used it: the RAW-converter that did was deleted during the port, the §2105 render path is SkiaSharp, and only a vestigial enum label remained. Because it's a framework package consumed on `net10.0`, NuGet emitted **NU1701** on every project that transitively referenced Image (Image/Server/Equipment/PlateSolving/Sequencer) — 5 warnings on every build, previously allow-listed in `Directory.Build.props` (`WarningsNotAsErrors`) to avoid failing the build. Removing the unused `PackageReference` drops a shipped-but-unused native lib from the daemon bundle, makes `NOTICE.md` accurate (FreeImage is no longer distributed), and yields a clean build with **no NU1701**; the now-unnecessary NU1701 allow-list entry is removed so a *new* framework-package dependency surfaces as an error and gets a conscious decision rather than slipping in silently. (One unrelated `NU5104` — Accord.Math prerelease — remains allow-listed.)
- **§36 — the broken `horizon-default` Data Manager download.** It pointed at the dead `data.openastro.net` host (so the download always failed) and was miscategorised: a site horizon — a flat default or a survey — is generated **locally** for the §36 Tonight's-Sky overlay (Meeus altitude formula + below-horizon shading), not fetched as a Data Manager package. Removing it means no broken download ships; the real horizon feature re-introduces horizon data on the Tonight's-Sky side when it's built (tracked in `design/PORT_TODO.md`). The Data Manager's catalog + per-package integrity digests are now injectable into `DataManagerService` (default = the production set), so the download-worker tests run against a self-contained synthetic catalog instead of depending on the production catalog's exact contents.
- **§68.1 — the AlpacaBridge version gate.** The daemon used to probe an AlpacaBridge `GET /version` and gate every equipment connect on it: too-old bridges were blocked outright (503) and "warn-band" bridges connected but raised an advisory (an `equipment.alpaca_bridge_outdated_warn` WS event + a banner on the client's Equipment panels). This was non-conformant — ASCOM Alpaca is open by design and has no such handshake or `/version` endpoint (AlpacaBridge doesn't even serve one), so the probe always failed and the gate was pure friction. Removed end-to-end: the version probe, handshake service, gate, and gate notifier; the 503/warn-band WS event (`WsEventCatalog.EquipmentAlpacaBridgeOutdatedWarn`); the client `AlpacaBridgeWarningBanner` + its `alpaca_bridge_warning_state`; and the gate wiring on every REST `/connect` handler and the boot auto-connect path. Equipment connects now go straight through to the Alpaca client. (Playbook §68 to be reconciled by the maintainer — flagged in `design/PORT_TODO.md`.)
- **§29.9 — `PlaceholderLogService`.** Removed now that the real `LogService` is wired (see Added) — it returned fixture log entries and a 404 download.
- **§54 — `PlaceholderBugReportService`.** Removed now that the real `BugReportService` is wired (see Added) — it returned a synthetic "ready" record and a 404 download.
- **§38 — the superseded read-only sequence display tree.** Deletes `SequenceTree`, `InstructionEditor`, and the `SequenceController`/`selectedNodeIdProvider` state (`state/sequencer/sequence_state.dart`). The Sequencer tab now renders the editable §38 surface (`SequenceEditorTree`/palette/field editor over `sequenceEditorProvider`), so the old lossy `SequenceNode` display widgets had no remaining users. `SequenceNode`/`parseNinaSequenceBody` stay — they still back `getSequence` and the editor tree's depth cap.

### Security
- **CVE-2025-6965 — bundled SQLite updated to a patched build.** `Microsoft.Data.Sqlite` / `Microsoft.EntityFrameworkCore.Sqlite 10.0.x` transitively pull the native `SQLitePCLRaw.lib.e_sqlite3 2.1.11`, which bundles SQLite older than 3.50.2 — affected by CVE-2025-6965 (CWE-197 numeric truncation, possible memory corruption; CVSS 7.2). The daemon now pins `SQLitePCLRaw.lib.e_sqlite3 3.50.3` (SQLite 3.50.3, patched) over the transitive version; the native SQLite C API is backward-compatible with the existing managed provider, so this swaps in fixed SQLite without changing the data layer. The pin can be dropped once `Microsoft.Data.Sqlite` ships a build on the patched lib.

### Added
- **§36 — installed star/DSO catalogs now draw as markers on the Planning atlas (client).** Once a catalog package (HYG stars / OpenNGC deep-sky) is downloaded via the Data Manager, its objects are fetched from `GET /api/v1/data-manager/{id}/catalog` and drawn as a point overlay on Aladin — so the atlas shows the real catalog the user installed, not just the background survey imagery. A new `skyAtlasCatalogProvider` watches the Data Manager package list (so installing a catalog adds its overlay and deleting it removes the markers with no manual refresh) and fetches each installed catalog with overlay-appropriate trimming — HYG capped at magnitude 7 (the brightest few thousand stars, keeping the field legible), OpenNGC count-capped — over a new `araAddCatalog` / `araClearCatalog` JS bridge (`AladinView`); the whole object list rides as one JSON-encoded argument so a star/DSO name can't break out of the literal or inject script. The overlay is stashed + re-applied across tab switches / before the engine finishes init, mirroring the survey + FOV-box wiring. A 404 (catalog not installed / vanished) draws nothing rather than erroring. (This is the catalog-overlay half of the Planning work; the download + serve halves shipped in prior slices.)
- **§36 — serve an installed catalog's objects as JSON for the Sky Atlas overlay (server).** New `GET /api/v1/data-manager/{packageId}/catalog` reads an installed catalog's `catalog.csv` and returns a uniform `[{name, ra_deg, dec_deg, magnitude}]` list — normalizing the very different on-disk formats: HYG is comma-separated with RA in decimal **hours** (converted to degrees), OpenNGC is **semicolon**-separated with **sexagesimal** RA/Dec strings and per-row name/magnitude fallbacks. Optional `?max_mag=` (drop fainter objects — e.g. naked-eye stars only) and `?limit=` keep the overlay payload sane; rows without a resolvable position are skipped; the parse runs off the request thread. Returns `404` when the package isn't a known catalog, isn't installed, or has no `catalog.csv`. The client Aladin overlay that consumes this is the next slice.
- **§36 — the Data Manager downloads real star + deep-sky catalogs (server).** The catalog packages pointed at a dead host (`data.openastro.net`, NXDOMAIN), so no download could ever succeed. They now point at real, **commit-pinned** (immutable) upstream sources: the **HYG** named-star database (`hyg-stars`, a 13.6 MB `.csv.gz`, CC-BY-SA) and the **OpenNGC** deep-sky catalog (`openngc-dso`, a 3.9 MB `.csv`, CC-BY-SA 4.0) — replacing the placeholder Tycho-2/Gaia entries. The download worker now picks the install path by file type (`.tar.gz` archive vs. bare `.csv`/`.csv.gz`, installed as a canonical `catalog.csv`), fails fast on an unrecognised format (a build-gate test catches a curator mistake), and **verifies each download against a known SHA-256 before committing it** — a corrupt/wrong download is rejected *before* the atomic swap, so it can never replace a good install. Attribution added to `NOTICE.md`; a corrected service doc comment (the old one wrongly claimed downloads were deferred). This is the data-acquisition half of the Sky Atlas catalog overlay; serving the installed catalog to the client + drawing it on Aladin follow.
- **§36 — the sky-data installer now accepts bare CSV / CSV.gz catalog files, not only `.tar.gz` (server).** `SkyDataInstaller` gained `InstallFromFileAsync`, which installs a single (optionally gzip-decompressed) catalog file through the **same** atomic stage → swap → rollback-on-failure core + size cap as the existing archive path (the shared logic is now factored into one `InstallStagedAsync`). This is the engine groundwork for the real downloadable HYG/OpenNGC star + deep-sky catalogs (which ship as CSV, not tar archives); the catalog entries + download-worker wiring that call it land in the follow-up. No user-visible change on its own.
- **§36 — sky-imagery survey picker in the Planning atlas (client).** The Planning header gains a **survey dropdown** so the atlas's background imagery can be switched between curated CDS HiPS surveys — **DSS2 colour** (full-sky photographic, the default), **DESI Legacy DR10** and **DECaPS DR2** (deeper, partial-sky colour), and **2MASS** (full-sky infrared) — each labelled with its sky coverage so a partial survey's blank regions read as expected, not a bug. Driven by a new `skyAtlasSurveyProvider` that `AladinView` listens to (a JSON-encoded `araSetSurvey` JS bridge → Aladin's `setBaseImageLayer`); the choice is stashed and re-applied across tab switches / before the engine finishes init, and only pushed when it differs from the bootstrap default so a fresh mount doesn't reload the same imagery. Survey ids verified against the CDS HiPS list. (This is the "survey first" half of the Planning catalog work; the downloadable star/DSO catalog overlays follow.)
- **§36/§25.5 — add a Planning target to a new sequence (client).** Each row in the Planning tab's **Tonight's Sky** list now has an **"Add to a new sequence"** action (`playlist_add`): it creates a fresh server-side sequence named after the object containing a single **Slew to RA/Dec** pointed at the object's coordinates, then confirms with a SnackBar and refreshes the Sequencer library so it appears there. New `inputCoordinatesFromDeg` converter (decimal RA/Dec degrees → NINA `InputCoordinates` HMS/DMS DOM, RA wrap + pole clamp, round-tripping H+M/60+S/3600), a `buildSlewTargetBody` body builder (reuses the instruction catalog's own container + slew `build()` so the result round-trips identically to a hand-built sequence), and a `SequenceClient.create(name, body)` wrapping `POST /api/v1/sequences`. The action is busy-guarded against double-create and disabled when no server is connected. (The Framing-panel "Add to Sequence" button — which needs the Aladin atlas centre bridged back to Dart — remains a tracked follow-up.)
- **§37 — the wizard can pull device settings from connected gear over AlpacaBridge (client).** The telescope (Screen 4), camera (Screen 5), and filter-wheel (Screen 6) wizard screens now each have a **"Refresh from connected …"** button that reads the device's real values off AlpacaBridge into the draft, instead of making the user hand-type what the daemon already knows: the mount's **focal length + aperture** (from the §578 telescope caps), the camera's **pixel size** (from the camera caps), and the wheel's **filter names + focus offsets** (one row per reported slot). Mirrors the shipped Optics-panel "Refresh from connected camera" (#473) — degrades with a clear hint when no device is connected / the mount doesn't report its optics (many don't) / the wheel reports no slots, partial-fills are spelled out, and it writes the **draft** (the wizard persists on Save), not the live profile. New `TelescopeOpticsApi` + `FilterWheelNamesApi`; the uncontrolled fields re-seed on refresh so pulled values show immediately.
- **§25.5.5/§37 — the telescope capabilities now expose focal length + aperture (server).** `TelescopeCapabilitiesDto` gained `focal_length_mm` + `aperture_diameter_mm` (nullable), read from the connected mount's ASCOM `Telescope.FocalLength` / `ApertureDiameter` (which report **metres** — converted to mm at the read), each in its own try/catch since most mounts NotImplement them (→ null, and a non-positive/unconfigured value is treated as not-reported). This lets the §37 wizard auto-fill the optics screen from a connected mount (client "Refresh from connected mount" is the next slice). No route change — they ride the existing `GET /api/v1/equipment/telescope`.
- **§70.5 — export a sequence to a shareable file (client).** The "Load sequence" picker now has a per-row **Export…** action: it calls the daemon's `POST /api/v1/sequences/{id}/share-export`, pretty-prints the returned manifest, and writes it to a user-chosen `.araseq.json` file via the OS save dialog — so a sequence can be shared and re-imported (via the existing NINA/sequence import) on another rig. Mirrors the §70 profile-share export flow (busy-guarded action, fail-loud on a manifest-less response, no dialog dismissal so several sequences can be exported in a row); the filename helper is generalized to emit either `.araprofile.json` or `.araseq.json`. New `SequenceShareExport` model + `SequenceClient.exportShare`.
- **§36.1 — Aladin Lite engine bundled for offline use (client).** The Sky Atlas previously pulled the ~1.9 MB Aladin Lite v3 engine from the CDS CDN at runtime (`<script src="…aladin.cds.unistra.fr/…/aladin.js">`), so a CDN outage, DNS failure, or upstream version change could blank or alter the production atlas. The engine (v3.6.1, with its CSS + WebAssembly core embedded in the single bundle) is now vendored at `assets/aladin/aladin.js` and **inlined** into the CEF bootstrap page at load time — removing the runtime CDN trust + reachability dependency for the engine, search, and overlays entirely (sky-survey *tiles* are still fetched from the CDS HiPS servers, so imagery still needs internet). The bundle is read once and the built `data:` URL is memoized for the process lifetime; a missing-asset failure degrades to the existing "renderer unavailable" state. GPL v3 attribution added to `NOTICE.md` and provenance to `assets/aladin/ALADIN_LICENSE.md` (per §36.11 — the engine runs in a separate CEF process boundary, which the GPL FAQ permits alongside ARA's MPL 2.0).
- **§64 — Live View viewer (client, second slice).** The Imaging-tab Live View toggle is now wired to the real daemon loop (it was a UI-only flag waiting on the server endpoint). Toggling on POSTs `/liveview/start` with the current exposure/gain/binning (exposure clamped to the daemon's ≤15 s cap) and polls `/liveview/frame` on a 250 ms timer, change-detecting on the `X-Live-Session` + `X-Frame-Seq` headers so an unchanged frame isn't re-decoded and a session restart isn't misread as a backward seq; toggling off POSTs `/liveview/stop`. The center `FrameViewer` shows the live JPEG stream (gapless, with a **LIVE** badge and a "Starting…" spinner until the first frame) while running, and reverts to the last-captured preview when stopped. New `LiveViewApi`/`LiveViewClient` + a `LiveViewFrameNotifier` (single in-flight poll timer, cancelled on stop/dispose). Completes the §2105/§64 Live View epic (server LV-1 + client LV-2).
- **§64 — Live View daemon loop (server, first slice).** A new server-side Live View runs a short-exposure loop on the connected camera and renders the latest frame to a JPEG (via the §65 stretch/encode path) for framing and focus — **ephemeral**: never written to FITS, never registered in the §28 catalog. New endpoints under `/api/v1/equipment/camera`: `POST /liveview/start` (exposure/gain/binning), `POST /liveview/stop`, `GET /liveview` (status: active / frame-seq / dimensions / timestamps), `GET /liveview/frame` (latest `image/jpeg`, or `204` before the first frame). The loop shares the capture in-flight gate — acquiring it per frame so a manual exposure interleaves and the two are never on the device at once — and tears down on disconnect/dispose. Client viewer is the next slice. Built on the existing capture seams (`ApplyExposureSettings`/`WaitForImageReady`/`ConvertImageArray`) + `Stretcher`/`JpegEncoder`; no new native dependency.
- **§25.5.5 — Settings → Mount panel now shows live RA/Dec + tracking/park controls (eighth device — completes the single-instance set).** The Mount settings panel was a static stub; it now shows the connected mount's live **right ascension** and **declination** (sexagesimal), parked / at-home flags, a **Slewing…/Unparking…** indicator, a **Tracking** on/off switch (disabled while parked), **Park**/**Unpark** buttons (whichever applies, gated on capabilities), and a **Stop** (abort-slew) panic button — built on the generic engine's `command`/`isBusy` path. Server-side adds the missing **`POST /api/v1/equipment/telescope/tracking`** endpoint over the existing `SetTrackingAsync` (it had no REST route), with the request DTO registered in the AOT source-gen context. The daemon already served the full Telescope DTO + slew/park/unpark/abort; only the client was stubbed and the tracking toggle unrouted. This is the last of the eight single-instance equipment panels to go live.
- **§25.5.5 — Settings → Camera panel now shows live CCD temperature + cooler control (seventh device).** The Camera settings panel was a static stub; it now shows the connected camera's live **CCD temperature**, **cooler power %**, an **Exposing… %** indicator, a **Cooler** on/off switch with a **target-temperature** set-point, and a read-only summary of the sensor (size, pixel µm, mono/colour + Bayer pattern, gain/offset ranges, max binning) — gated on the device's capabilities. Server-side adds the missing **`POST /api/v1/equipment/camera/cooler`** endpoint over the existing `SetCoolerAsync` (it had no REST route), and registers the rotator `reverse`/`sync` request DTOs in the AOT source-gen context (missed in the rotator slice — required so those endpoints deserialize under the slim builder / §71 AOT). Built on the generic engine's `command`/`isBusy` path.
- **§25.5.5 — Settings → Dome panel now shows live shutter/azimuth + controls (sixth device; daemon surfaces dome capabilities).** The Dome panel was a static stub; it now shows the connected dome's live **shutter** state, **azimuth**, at-home and parked flags, a **Slewing…/Shutter moving…** indicator, and **Open/Close shutter**, **Slew to azimuth**, and **Park** controls — each shown only when the device's capabilities allow, and disabled while the dome is moving. Server-side: `DomeDto` now surfaces the capability flags (`can_set_shutter`/`can_set_azimuth`/`can_sync_azimuth`/`can_park`/`can_find_home`) that the service already read but previously kept only for the sequencer mediator (the audit's flagged gap); the slew/park/shutter commands were already wired. Built on the generic engine's `command`/`isBusy` path.
- **§25.5.5 — Settings → Rotator panel now shows live angle + move/reverse/sync (fifth device; daemon gains rotator caps/reverse/sync).** The Rotator panel was a static stub; it now shows the connected rotator's live **sky** and **mechanical** angle, a **Moving…** indicator, a **Reverse** toggle (when the device supports it), a **Move** control (angle 0–360 to the sky or mechanical angle), and a **Sync** control (set the sky-angle reference, e.g. from a plate solve — the §38 framing hook drives it programmatically). Server-side fills the audit's rotator gaps: `RotatorDto` gains a capabilities sub-DTO (`can_reverse`, `step_size`) + a `reverse` runtime flag, and the service adds `POST …/rotator/reverse` and `POST …/rotator/sync` (the move endpoint already existed). Built on the generic engine's `command`/`isBusy` path.
- **§25.5.5 — Settings → Filter Wheel panel now shows live slots + select (fourth device on the generic core).** The Filter Wheel panel was a static stub; it now lists the connected wheel's **own slot names + focus offsets** (hydrated from the device, not local labels), marks the **active** slot, and lets you **select** any slot (disabled while the wheel is moving — the busy fast-poll tracks the change to completion). The local "Slot labels (for sequences)" editor is kept as a distinct section — those names are the user's offline sequence-authoring labels (the §38 editor reads them) and are independent of the connected wheel's reported names. The daemon already served the full FilterWheel DTO; only the client was stubbed.
- **§25.5.5 — Settings → Focuser panel now shows live position + a move control (third device on the generic core).** The Focuser panel was a static stub; it now renders the connected focuser's live **position**, **temperature**, temperature-compensation state, and a **Moving…** indicator, plus a **Move to target** control (a position field clamped to the device's min–max range, disabled while moving). Built on the generic engine with a new reusable `command(subpath, body)` on the device client + a `performAction` hook on the notifier base — the device-control path the remaining panels reuse (filter change, rotator move, dome shutter, cooler, slew). The §37.11 autofocus rows stay as profile references. The daemon already served the full Focuser DTO; only the client was stubbed.
- **§25.5.5 — Settings → Weather panel now shows live sensors (second device on the generic equipment-status core).** The Weather (ObservingConditions) panel was a static stub; it now renders the connected station's live readings — temperature, humidity, **dew point**, pressure, cloud cover, wind speed, **wind gust**, wind direction, rain rate — omitting any sensor the device doesn't implement, plus an "Updated … UTC" timestamp and Connect…/Disconnect. The §35 threshold rows stay as profile references. Server-side adds `dew_point_c` + `wind_gust_ms` to the ObservingConditions DTO + service reads (the panel referenced both). This PR also extracts a shared `EquipmentConnectionCard` (the connect-via-chooser + four async shapes + disconnect/cancel scaffold) and a shared UTC time formatter, and retrofits the Safety Monitor panel onto them — so the remaining single-instance panels (focuser, filter wheel, rotator, dome, camera, mount) reuse one connection surface instead of re-implementing it.
- **§25.5.5 — Settings → Safety Monitor panel now shows the live device (first of the generic equipment-status client core).** The Safety Monitor panel was a static stub (a device chooser + hardcoded policy text) that never read the daemon's `GET /api/v1/equipment/safetymonitor`. It now renders the connected monitor's live state: a **Safe / Unsafe** indicator, the connection-state chip, the last safe⇄unsafe transition time, and **Connect…/Disconnect** controls (the §35 unsafe-action policy rows stay as profile references). Under the hood this lands a reusable client core — a generic `EquipmentDeviceApi`/`EquipmentDeviceNotifier` that handles the status GET (404 → not connected), 202-accepted connect/disconnect, and polls a mid-connect device to settlement — that the remaining single-instance equipment panels (camera, mount, focuser, filter wheel, rotator, dome, weather, flat) will build on to reach the same live parity the Switch panel already has. The daemon already served all of this; only the client was stubbed. Also makes the shared `SettingsRow`/`SettingsSwitchRow` labels ellipsize instead of overflowing a narrow column.
- **§6 — Settings → Switch panel: connect and control multiple switches.** A new **Switch** equipment panel (Settings → Equipment → Switch) lists every connected ASCOM Switch device — each with a live state chip and its ports — and an **Add switch** button that discovers + connects an *additional* switch (the panel is multi-instance, unlike the one-device panels). Per port: a **toggle** for boolean on/off ports, a **slider** for value/PWM ports (writes on release), and a read-only value for non-writable ports; each device has a **Disconnect** action. Failures surface as a SnackBar and never wipe the rest of the list. This makes the multi-switch server work (connect to two+ power/relay boxes) usable end-to-end. The discovery chooser gained an `onPick` hook so a multi-instance type connects the pick instead of the default single-selection write.
- **§6 — client data layer for multiple Switch devices.** Groundwork for the Switch control UI: a `SwitchDevice`/`SwitchPort` model over the daemon's multi-instance Switch surface, a `SwitchApi` (`getAll` / `connect` / per-device `disconnect` / `setValue`), and a `switchListProvider` that reads `GET /api/v1/equipment/switch` (pull-on-demand) for the active server and exposes connect/disconnect/set-value actions that re-read the list. Adds `EquipmentDeviceType.switchDevice` (so Switch is discoverable like the other device types) and a `DiscoveredDevice.toConnectRequestJson` for the connect body. No UI yet — the Switch settings panel + multi-select is the next slice.
- **§6 — connect to multiple Switch devices at once (server).** The daemon's Switch service was single-instance: connecting a second ASCOM Switch silently disconnected the first, so a rig with two power/relay boxes could only ever drive one. `SwitchService` is now multi-instance — it keeps a connection per device, addressed by the switch's **Alpaca device number** (the `{n}` in the §10.6 `/api/v1/equipment/switch/{n}` contract). New REST surface: `GET /api/v1/equipment/switch` lists every connected switch; `GET /api/v1/equipment/switch/{n}` returns one; `POST …/switch/connect` now *adds* a switch instead of replacing it; `POST …/switch/{n}/disconnect` (idempotent — a no-op accept for an unknown number) and `POST …/switch/{n}/value` (out-of-range port → 400; no connected switch at `{n}` → 409) target a specific switch. `GET /switch` lists every *known* switch, including ones left `Disconnected`/`Error` until reconnect or restart (filter on state for live-only). `SwitchDto` now carries its `alpaca_device_number`. The Sequencer's `SetSwitchValue` instruction keeps working against the lowest-numbered connected switch (a per-device selector is a follow-up). Two switches sharing a device number across different Alpaca hosts collide on the address — the later connect wins the slot and the collision is logged (a documented limit of device-number addressing). The client UI for multiple switches is the next slice.
- **§32.4 — the daemon advertises itself over mDNS so WILMA's scan finds it.** The server now publishes a `_openastroara._tcp` service on its bound port (SRV → host:port, plus `uuid`/`nickname`/`version` in TXT) via a new `MdnsAdvertiser` hosted service, so the client's first-run "scan for servers" actually discovers it instead of finding nothing. Previously nothing advertised the daemon — the bundled `Zeroconf` package is browse-only (used for Alpaca device probes) and can't publish — so discovery only ever worked via manual host:port entry. Advertising uses `Makaretu.Dns.Multicast` (MIT, pure-managed), co-exists with the OS mDNSResponder/Avahi on :5353, and is best-effort: a responder that can't bind never blocks startup (manual connect still works). Unregisters on shutdown.
- **§52.1 — equipment auto-connect on boot (daemon side).** The per-device "Auto-connect on boot" setting was persisted but nothing acted on it, so after a daemon restart capture failed with "camera is not connected" until the user reconnected by hand. The daemon now remembers the last device connected per type (`EquipmentSelectionStore` → `equipment-selection.json`, written at the connect chokepoint) and, on boot, re-establishes each device whose profile auto-connect bool is set (`EquipmentAutoConnectService`). Every auto-connect routes through the same REST `/connect` path the UI uses (Alpaca is open by design — no version gate). Best-effort and isolated per device — one device failing never blocks the others or startup, and only devices actually connected before are attempted.
- **§70 — friendlier names for imported profiles.** An imported profile-share template is no longer always named the generic "Imported profile". When the share file carries no donor display name (the current export omits it), the import now derives a non-identifying label from the rig description's effective focal length — e.g. **"Imported — 1626 mm rig"** — so a user who imports several templates can tell them apart at a glance; it falls back to "Imported profile" only when the rig has no usable focal length. The chosen name is also de-duplicated against the existing profiles (case-insensitively), suffixing **" (2)"**, **" (3)"**, … so a second import of the same rig doesn't collide with the first. Both the preview (so the user sees the real name up front) and the commit (authoritative at create time) use the same de-duplicated name.
- **§37 — wizard per-screen validity gate (client).** The setup wizard now disables **Next / Save Profile** while the current screen has a blocking inline validation error (e.g. an out-of-range search radius, autofocus steps, exposure, resume delay, or horizon altitude), with a tooltip explaining why — instead of letting the user advance past a visible red error (the invalid value was already never written to the draft, but the UX was deceptive). Back / Skip stay available so a screen is never a dead end. Implemented via a `wizardStepValidProvider` the validated screens publish to and the shell reads; the controller resets it on each step change.
- **§54 — bug-report bundle action (client).** The Support tab gains a **Send me a bug report** card: it prepares the diagnostic bundle on the daemon (`POST /api/v1/bugreport/prepare`), then shows a **PII disclosure dialog** spelling out exactly what the ZIP contains — the daemon logs, the *full* profile (which may include equipment credentials, API/notification tokens, precise observatory coordinates, and network endpoints), and system info including the device's filesystem path — with its approximate size. Only on the user's explicit confirmation does it download the ZIP (`GET /api/v1/bugreport/download?preparationId=…&acknowledge=pii`) to a chosen path. This satisfies the server-enforced `acknowledge=pii` gate at the UI layer (the disclosure the gate exists to require), closing the §54 client follow-up. Backed by a new `BugReportApi`/`BugReportClient`.
- **§54 — Support tab with a daemon log viewer (client).** A new top-level **Support** tab tails the daemon's §29.9 logs live: a level filter (All / Debug / Information / Warning / Error), a substring filter, a refresh, and a **Download daemon log** action that saves the full newest log file to disk (via the OS save dialog, named from the server's `Content-Disposition`). Backed by a new `LogsApi` over `POST /api/v1/server/logs/tail` + `GET /api/v1/server/logs/download` — the client surface for the §29.9 logs that landed server-side. (The §54 bug-report bundle action is the following slice.)
- **§54 — real bug-report bundle (ZIP download).** "Send me a bug report" now produces a real diagnostic bundle instead of a synthetic placeholder: `POST /api/v1/bugreport/prepare` stages a ZIP under `{profileDir}/bug-reports/` containing the daemon's rolling log files (`logs/`), the active `profile.json`, and a generated `system-info.json` (app version, OS, architecture, runtime), then returns a `ready` record with the real size and a download URL; `GET /api/v1/bugreport/download?preparationId=…&acknowledge=pii` streams it (resolved by the server-minted id embedded in the file name, so it survives a daemon restart and carries no caller-controlled path). Because the bundle carries the daemon logs, the full `profile.json` (possible equipment credentials / tokens / precise coordinates), and the local filesystem path, the download is gated behind a required `acknowledge=pii` query parameter — the server returns `403` without it, so a client must surface a PII disclosure to the user before downloading. The bundle is staged via a temp-file + atomic rename (a crash mid-zip never leaves a half-written archive), capped to the newest 3 log files, retention-pruned to the newest 10 bundles, and is never empty (system-info is always present). Replaces `PlaceholderBugReportService`.
- **§29.9 — real daemon logs (file sink + tail/download).** The daemon now writes its own logs to a rolling Compact-JSON (CLEF) file under `{profileDir}/logs/openastroara-<date>.log` via a Serilog file sink (daily + 50 MB roll, 14 files retained), wired through `UseSerilog`. The §29.9 log endpoints are backed by a real `LogService` over those files (replacing the fixture-returning placeholder): `POST /api/v1/server/logs/tail` streams entries newest-first across the roll boundary (walks the rolling files newest→oldest until the line cap is filled, so a just-rolled near-empty file still returns recent history) with min-level + substring filtering and bounded memory; `GET /api/v1/server/logs/download` streams a whole log file, with the requested name constrained to a bare `*.log` inside the logs dir (no path traversal); `POST /api/v1/server/logs/rotate` records an audit marker and acknowledges (the sink rolls automatically). CLEF lines are parsed back with `System.Text.Json` directly (no reader dependency).
- **§38 — Sequence-editor filter picker (`Switch Filter`).** The `SwitchFilter` instruction's `Filter` field is now a real dropdown (replacing the placeholder) over the configured filter-wheel slot labels (Settings → Filter Wheel). Picking a filter writes a minimal `FilterInfo` (`{$type, _name, _position}`); the daemon's `SwitchFilter.MatchFilter` re-resolves it to the active profile's full `FilterInfo` by name on load, so only the identifying fields are stored. A filter from an imported body whose name isn't a configured slot stays visible rather than blanking, and an empty label set prompts the user to configure filters. (Filter names come from the in-memory slot labels for now; sourcing them from the daemon profile / connected wheel is tracked in design/PORT_TODO.md.)
- **§38 — Sequence-editor altitude conditions (`Until altitude` / `Until above horizon`).** Adds `AltitudeCondition` and `AboveHorizonCondition` to the loop-condition catalog — the first conditions with a nested object, `Data` (a `WaitLoopData`: a target `InputCoordinates`, a degrees `Offset`, and a `Comparator`). A new `waitLoopData` field type renders a composite editor — a comparator dropdown (only the NINA-selectable `Less than` (1) / `Greater than` (3)), a signed offset, and the RA/Dec target (reusing the coordinates editor, now extracted to a shared `_CoordinatesEditor`). Defaults match the daemon (altitude: 30°/LessThan; above-horizon: 0°/GreaterThan); `HasDsoParent` is emitted at its default but hidden (the daemon recomputes it from the parent DSO context). Grounded against `LoopForAltitudeBase`/`WaitLoopData`.
- **§38 — Sequence Validate (toolbar dry-run).** Adds `POST /api/v1/sequences/validate` (server: runs the body through `SequenceSchemaValidator` without persisting, returns `{valid, reason}`) and wires the sequencer toolbar's **Validate** button to it: it dry-runs the editor's working body and reports the result in a SnackBar — green "Sequence is valid." or the first schema problem — so a user can catch an invalid sequence before saving or running. Enabled whenever a sequence is loaded; brackets the shared command busy-fence.
- **§38 — Sequence-editor positional drag (drop between/after rows).** Adds insert-between-rows drop zones to the tree: a thin gap above each row that, on drop, places the dragged subtree just before that row in its parent, plus a trailing gap below a container's last child to append at its end — so a drag can reorder within a container or insert at a precise position, complementing the existing drop-into-container and move-up/down buttons. The index math (post-removal shift on a same-parent reorder, self/descendant + root rejects, append-slot resolution) lives in pure `resolveDropBefore` / `resolveDropInto` / `appendGapContainers` helpers, unit-tested; a hovering gap shows a highlight line (fixed-height so the hover never reflows the list).
- **§38 — Sequence-editor `Until a time` condition (clock time or sky event).** Adds `TimeCondition` to the loop-condition catalog: a `When` dropdown picks **Custom time** (the clamped Hour/Minute/Second clock time) or a sky event — Sunset / Sunrise / Civil/Nautical/Astronomical Dusk & Dawn / Meridian — plus a minutes offset. A new `timeProvider` field type renders the picker and writes the daemon's `SelectedProvider` object (`{$type}`, or null for custom), grounded against the `DateTimeProvider` classes. The Hour/Minute/Second fields grey out under a sky-event provider (the daemon computes the time and ignores them), via a new general `InstructionField.enabledWhen` relevance predicate. First object-valued condition.
- **§38 — Sequence-editor Reconnect Device trigger.** Adds `ReconnectTrigger` to the trigger catalog — a `Reconnect Device` trigger that re-connects a dropped device mid-run, with a `SelectedDevice` dropdown over the grounded device-name set (Camera / Filter Wheel / Focuser / Rotator / Mount / Guider / Switch / Flat Panel / Weather / Dome / Safety Monitor; default Camera). First trigger with an editable field — renders through the existing trigger-card field editor.
- **§38 — Sequence-editor numeric field bounds (clamped entry).** `InstructionField` gains optional `min`/`max`; a bounded `number`/`integer` field now renders the controlled clamped editor (the entry snaps into range and the displayed text is corrected) instead of a free text field. Applied to the loop conditions: `TimeSpanCondition`'s minutes/seconds clamp to 0–59 and hours to ≥ 0, and `LoopCondition`'s iterations to ≥ 1 — so a duration or loop count can't be set to a value the daemon would reject. (Resolves the #531 follow-up.)
- **§38 — Sequence-editor drag-and-drop (reparent into a container).** Tree rows are now drag sources (long-press to start) that drop **onto a container** to move the dragged subtree inside it — drag an instruction into/out of a Sequential/Parallel set, or nest one container in another. The hovered container highlights while a valid drop is in range; the drop is gated by a pure `canReparentInto` policy (rejects dropping onto a leaf, onto itself, into its own descendant, or a no-op back onto its current last position). Built on the #536 `moveSubtree`/`moveNodeTo` core; within-parent ordering stays on the move-up/down buttons. Delivers the "drag and drop to edit the sequence" workflow.
- **§38 — Sequence-editor move/reparent engine (drag-and-drop core).** Adds `moveSubtree` to `nina_dom.dart` — removes the subtree at one path and re-inserts it as a child of another container, compensating for the one destination index that the removal can shift, and rejecting a move of the root or into the node's own self/descendant (which would orphan it). Plus an `isAncestorOrSelf` path helper and the `SequenceEditorController.moveNodeTo` method (container-guarded, clears selection, goes dirty). This is the correctness core for the upcoming drag-to-reorder/reparent UI; every op returns a new body and never mutates the source.
- **§38 — Sequence-editor triggers UI (field-panel Triggers editor).** Selecting a container now shows a **Triggers** section in the field panel (below Conditions): an "add" menu offering **Meridian Flip**, a card per trigger (with any editable fields + a remove button), all writing through the controller's trigger ops so the edit goes dirty and round-trips on Save. Completes the container-depth model — a container can now carry child instructions, a name, loop conditions, **and** triggers — matching NINA's Advanced Sequencer structure end-to-end. (Object-valued conditions and `ReconnectTrigger` remain deferred to their own slices.)
- **§38 — Sequence-editor triggers engine + controller methods.** Adds the raw-body mutation ops for a container's `Triggers` collection — `triggersOf` / `withTriggers` / `addTrigger` / `removeTriggerAt` / `setTriggerField` in `nina_dom.dart` (mirroring the `Conditions` helpers, with the correct `ISequenceTrigger` wrapper `$type`) — and the matching `SequenceEditorController` methods `addTriggerTo` / `removeTriggerFrom` / `setTriggerFieldOn`. Each is container-guarded (a leaf can't carry triggers), keeps the container selected, and goes dirty for Save; every op returns a new body and never mutates the source. Drives the upcoming Triggers editor UI.
- **§38 — "Import" button in the sequencer toolbar (browse a NINA `.json`).** Surfaces the existing NINA-import path as a first-class toolbar action next to New/Load: it opens a native file picker for a `.json`, reads it, and `POST`s it to `/api/v1/sequences/import` (the daemon validates + reports any lossy-translation warnings, then the imported sequence is selected). The import flow was already built and tested in `sequence_import.dart` but was only reachable from inside the Load dialog, so it wasn't discoverable — this wires `pickAndImportSequence` straight to a toolbar button. Lets users moving over from NINA browse to an exported sequence and import it directly.
- **§38 — Sequence-editor trigger catalog (data model).** Adds `trigger_catalog.dart`: a `TriggerDef` catalog for the triggers a container can carry, starting with **`Meridian Flip`** (`MeridianFlipTrigger`). `build()` emits `$type` + `Parent: null` + a fresh empty `TriggerRunner` (a `SequentialContainer` built via the instruction catalog, so the runner is shaped exactly like any user-added container), grounded against the `SequenceTrigger` base. Meridian Flip carries no serialized fields of its own (its parameters come from the profile). Drives the upcoming container Triggers editor. (`ReconnectTrigger` needs its `SelectedDevice` device-name set grounded, so it's deferred to its own slice.)
- **§38 — Sequence-editor loop conditions UI.** Selecting a container now shows a **Conditions** section in the field panel: an "add" menu offering `Loop (iterations)` / `For a duration`, a card per existing condition with its editable fields, and a per-condition remove button — all writing through the controller's condition ops so the edit goes dirty and round-trips on Save. Containers can now genuinely loop (N times or for a duration), completing NINA-style loop control. (Object-valued conditions — `TimeCondition`, altitude — still pending their slice.)
- **§38 — Sequence-editor conditions engine + controller methods.** Adds the raw-body mutation ops for a container's `Conditions` collection — `conditionsOf` / `withConditions` / `addCondition` / `removeConditionAt` / `setConditionField` in `nina_dom.dart` (mirroring the `Items` helpers, with the correct `ISequenceCondition` wrapper `$type`) — and the matching `SequenceEditorController` methods `addConditionTo` / `removeConditionFrom` / `setConditionFieldOn`. Each is container-guarded (a leaf can't carry conditions), keeps the container selected, and goes dirty for Save; every op returns a new body and never mutates the source. Drives the upcoming Conditions editor UI.
- **§38 — Sequence-editor loop-condition catalog (data model).** Adds `condition_catalog.dart`: the catalog of loop conditions a container can carry — `Loop (iterations)` (`LoopCondition`) and `For a duration` (`TimeSpanCondition`) — each with its icon, field schema (reusing `InstructionField`), and a `build()` producing a fresh raw-body condition node (`$type` + integer fields + `Parent: null`, no `ErrorBehavior`/`Attempts` since conditions aren't `SequenceItem`s), grounded against the `OpenAstroAra.Sequencer.Conditions` classes. Drives the upcoming container Conditions editor. (Object-valued conditions — `TimeCondition`'s provider and the altitude conditions' `WaitLoopData` — are deferred to their own slice.)
- **§38 — Sequence-editor container rename.** Selecting a container in the tree now shows a **Name** field in the field panel; editing it writes straight back to the container's `Name`, the edit goes dirty for Save, and the tree label follows the new name live (an empty name falls back to the catalog label). Leaf instructions are unaffected — they carry no `Name`, matching the daemon templates. (Container loop conditions and triggers follow in later slices.)
- **§38 — Sequence-editor containers (Sequential / Parallel instruction sets).** The palette gains a **Containers** section with `Sequential Instruction Set` and `Parallel Instruction Set`. Adding one drops an empty container — built in the daemon's exact template shape (a typed `Strategy`, the NINA default `Name`, and empty `Conditions`/`Items`/`Triggers` `ObservableCollection`s with the correct element-type `$type`s) — so it deserialises and round-trips into NINA identically. Instructions then nest inside the selected container via the existing add/reorder/delete machinery, giving NINA-style hierarchical sequences. A container's own `Name` now shows in the tree in place of the generic catalog label. (Editing the container name + its loop conditions and triggers follow in later slices.)
- **§38 — Sequence-editor coordinates (RA/Dec) field editor.** `Slew to RA/Dec`'s `Coordinates` now have an inline editor — RA `H : M : S`, Dec `± D : M : S` — over the nested `InputCoordinates` object (range-clamped per component, int/double matching the C# field types, `$type` preserved). The binning and coordinates editors share one controlled `_NumField` (clamps + corrects the displayed text so it can't diverge from the model). Filter (SwitchFilter) is the last placeholder, pending connected-filterwheel data.
- **§38 — Sequence-editor binning (X × Y) field editor.** The field panel now edits a `Take Exposure`'s `Binning` inline with two integer fields (factors ≥ 1); each change rebuilds the nested `BinningMode` object (preserving its `$type`) and writes it back via `setNodeField`. Coordinates/filter still get a placeholder pending their own editors.
- **§38 — Sequence-editor reorder (move up/down).** The selected tree row gets move-up / move-down buttons (disabled at the sibling boundaries) that reorder the node among its siblings via a new `SequenceEditorController.moveNode`; selection follows the node and the edit goes dirty for Save. (Drag-to-reorder is a following slice.)
- **§38 — Sequence-editor delete.** The selected tree row shows a delete button (never the root) that removes that node from the working body via `removeNode`; selection clears after, the edit goes dirty, and Save persists the removal. Completes basic CRUD on the sequence tree (add via the palette, edit fields, delete).
- **§38 — Sequence-editor Save.** The toolbar Save button is wired: it enables once the open sequence has unsaved edits and `PATCH`es the editor's working raw body back to the daemon verbatim (so it round-trips), then rebaselines dirty-tracking and confirms with a SnackBar. A `422` surfaces the server validator's rejection message; any other failure is a generic transport error and leaves the edits unsaved for retry.
- **§38 — Sequence-editor live in the Sequencer tab.** The Sequencer tab now renders the NINA-style edit surface — instruction palette + raw-body tree + selected-node field editor (the §38 widgets) — and loads a picked sequence's body into the editor via `getSequenceDetail` → `sequenceEditorProvider`. Replaces the old read-only display-tree panes; the stale-response / load-failure / retry guards carry over unchanged. Users can now select a sequence, add instructions, and edit their fields on the real body. (Save/Validate wiring and drag-to-reorder/delete follow.)
- **§38 — Sequence-editor field panel.** Adds `SequenceFieldEditor` (`sequence_field_editor.dart`): edits the selected node's scalar fields, each control driven by the instruction catalog's `InstructionField` schema (number/integer/text → text field, boolean → switch, int-enum/string-enum → dropdown) and written straight back to the raw body via `setNodeField`. Complex fields (binning/coordinates/filter) get a placeholder pending their dedicated editors; shows a prompt when nothing is selected.
- **§38 — Sequence-editor instruction palette (tap-to-add).** Adds `SequencerPalette` (`sequencer_palette.dart`): the catalog of instructions grouped by category; tapping a tile adds that instruction relative to the current selection via `SequenceEditorController.addInstruction` — into the selected container, after the selected leaf, or appended to the root when nothing is selected — and selects the new node. Disabled until a sequence is loaded. Drag-to-a-specific-position is a following slice.
- **§38 — Sequence-editor tree view.** Adds `SequenceEditorTree` (`sequence_editor_tree.dart`): an indented, selectable list rendering the RAW NINA body held by `sequenceEditorProvider` (not the lossy preview tree). Each row labels/icons its node via the instruction catalog (`instructionForType`), falling back to a container's `Name` or the short `$type` for unknown nodes; tapping a row selects it by `NodePath`. Render + select only — drag-to-reorder and the palette drop target follow in later slices.
- **§38 — Sequence-editor working-state controller.** Adds `SequenceEditorController` (`sequence_editor_state.dart`): a Riverpod `Notifier` holding the RAW NINA body for the sequence open in the editor, with `load`/`insertInstruction`/`removeNode`/`reorder`/`setNodeField`/`select`/`markSaved`. Every edit runs through `nina_dom` (returns a new body, never mutates the source) and dirty-tracking is a deep compare against the loaded original — so Save round-trips faithfully. Bridges the Phase 0 mutation engine + Phase 1 catalog to the upcoming palette/tree/field-editor widgets; path-based selection self-heals (cleared on structural edits, kept across field edits, stale paths ignored).
- **§38 — Sequence-editor instruction catalog (palette data model).** Adds `instruction_catalog.dart`: a hand-authored catalog of the draggable instructions (Take Exposure, Cool/Warm Camera, Switch Filter, Move Focuser absolute/relative, Slew to RA/Dec, Set Tracking, Park/Unpark, Start/Stop Guiding, Dither, Wait, Annotation) grouped by category, each with its icon, editable field schema, and a `build()` that produces a fresh raw-body node — `$type`, field defaults, and the base `Parent`/`ErrorBehavior`/`Attempts` fields — mirroring the daemon's `OpenAstroAra.Sequencer` classes exactly (integer enums, nested `BinningMode`/`InputCoordinates`), so a dropped instruction deserialises and runs unchanged. Drives the upcoming palette + drag-and-drop UI.
- **§38 — Sequence-editor mutation engine (groundwork for the drag-and-drop editor).** Adds `nina_dom.dart`: pure, tested operations over the raw NINA Json.NET DOM the daemon stores verbatim — `childrenOf` (unwraps the `ObservableCollection`/`$values` shape), `withChildren`, `nodeAt`, `setField`, `insertChild`, `removeAt`, `reorderChild` — addressing nodes by a child-index path and always returning a NEW body (never mutating the source). This is the faithful-edit foundation for the upcoming NINA-style editor (add/move/delete/edit instructions on the real body, not the lossy preview tree); the palette + drag-and-drop UI and Save/Validate land in follow-ups.
- **§38.4 — Export a sequence back to a NINA file (ARA → NINA round-trip).** The Sequencer toolbar gains an **Export** action: with a sequence selected it writes that sequence's raw body to a `.json` file you choose, which opens straight back in NINA. Because the daemon stores the body verbatim, the export is faithful — no data loss — so a sequence imported from NINA (or created/edited in ARA) can be carried back out to NINA. ARA's internal `schemaVersion` marker is stripped so the file is pristine NINA JSON, and the output is pretty-printed. Completes the bidirectional migration guarantee alongside Import (a cancelled save is a no-op; a load/save failure surfaces a message).
- **§38.5 — Sequencer Save/Export data layer (faithful NINA round-trip).** Adds `SequenceApi.getSequenceDetail()` (keeps the raw NINA/§38.1 body JSON the daemon stores verbatim — the source of truth for Save/Export) and `updateSequence(id, {name, description, body})` (`PATCH /{id}`, only the supplied fields change; the daemon schema-validates the body and answers 422 when invalid), plus a `SequenceDetail` model. This is the groundwork so a sequence imported from NINA can be saved and exported back out with no data loss — the Save/Export UI lands in a follow-up. (The on-screen tree remains a derived preview; edits will target the raw body so the round-trip stays faithful.)
- **§38.7 — Sequencer "New" creates a sequence from a template.** The Sequencer's **New** toolbar button is live: once connected it opens a picker listing the daemon's starting-point templates (name + category/description), you name the new sequence, and **Create** instantiates it server-side and selects it so it loads straight into the editor tree ready to run. The name field pre-fills from the chosen template (without clobbering a name you've already typed), Create stays disabled until a template is picked and the name is non-empty, and a re-entrancy guard prevents a double-create. Loading / error / no-server / no-templates states are handled, and a failed create surfaces a message instead of failing silently. (Editing the resulting sequence + Save/Validate are still to come.)
- **§38.6/§38.7 — Sequencer template client API (groundwork for "New").** Adds `SequenceApi.listTemplates()` (`GET /api/v1/sequences/templates`) and `instantiateTemplate(name, newName)` (`POST …/templates/{name}/instantiate`), a `SequenceTemplate` model, and a `sequenceTemplatesProvider` bound to the active daemon. This is the data layer behind the upcoming "New" toolbar action (create a ready-to-run sequence from a built-in/saved template); the dialog UI lands in a follow-up. Unknown template → 404 surfaced as an error; a name-less template row is dropped.
- **§38/§28.12 — The Sequencer now tracks a run live.** While a sequence is running, ARA folds the daemon's `sequence.*` WebSocket events (started / progress / paused / resumed / aborted / stopped / complete / failed) into the selected sequence's run state, so the toolbar buttons and the status line (state + frame progress, e.g. "Running — 18/60 frames") update in real time instead of only after you press a control. The fast-changing fields (state, instruction index, frame counts) come from the live stream; the slower details (target name, start time) are preserved from the initial read so a live tick never blanks them. Events for other sequences are ignored.
- **§38.4 — Import a NINA sequence file from the Sequencer.** The Load dialog gains an **Import NINA…** button: pick a NINA `.json` sequence export and ARA uploads it to the daemon (`POST /api/v1/sequences/import`), then selects the new sequence so it loads straight into the editor tree. The imported sequence takes its name from the NINA root (falling back to the file name). If the translation dropped or approximated anything, a warnings dialog lists the dropped instruction types and any per-item notes so it's clear what to review before running; a clean import just confirms with a brief message. A cancelled pick is a no-op and an unreadable / non-JSON file is reported inline instead of being uploaded. Completes the NINA-import round-trip on the client (the server import + body→tree rendering already landed).
- **§38 — Selecting a sequence now loads it into the editor tree.** Picking a sequence in the Load dialog fetches its full body from the daemon and renders the real sequence in the tree view (areas → targets → instructions) instead of the demo placeholder. The body parser understands NINA sequence files (the `$type`/`$id`/`$ref` Json.NET shape NINA exports use), so an imported NINA sequence displays with its containers, targets, and per-instruction parameters; container loop-conditions/dither-triggers are preserved for later display. A load failure leaves the current tree in place. (Editing + Save/Validate still to come.)
- **§38 — Sequencer Run / Pause / Resume / Abort controls are live.** With a sequence selected (via Load), the toolbar now drives the daemon's run lifecycle: **Run** starts it (and becomes **Resume** when paused), **Pause** pauses a running sequence, **Abort** stops an active one — each gated by the sequence's live run state so only valid actions are enabled. The status line shows the current state and frame progress (e.g. "Running — 12/60 frames"), and a failed command surfaces a message instead of failing silently. (Loading the chosen sequence into the editor tree + Save/Validate still to come.)
- **§38 — Sequencer "Load" lists your saved sequences.** The Sequencer tab's **Load** button is live: once connected to a daemon it opens a picker listing the server's saved sequences (newest-first) with a run-state badge and instruction/target counts, and lets you pick one. Behind it: a client `SequenceApi` (list + lifecycle + run-state) over `/api/v1/sequences` with cursor-page handling. Save / Validate stay disabled for now.
- **§37.7 — Setup wizard screen 18 (review + Save) is a real form — the wizard is now complete end-to-end.** The final step shows a read-only, section-grouped summary of everything the wizard collected (profile basics, equipment, per-device setup, imaging tools, safety, site, sky data), each section with an **Edit** button that jumps straight back to the relevant screen. Anything left untouched reads "Not set" so it's clear the built-in default applies, and skipped screens are flagged. Saving is the existing "Save Profile" button. All 18 wizard steps are now real forms — the last placeholder is gone.
- **§37.6 — Setup wizard screen 17 (sky data) is a real form.** The wizard's sky-data step lists the daemon's not-yet-installed data packages (star catalogs, target lists) with sizes and lets you tick which to fetch (Select all / Clear); the chosen packages are **queued for background download via the Data Manager once the profile is saved** (best-effort — a queue hiccup never fails the save). Loading / error / no-server / all-installed states are handled. (An auto-checked "recommended preset" needs a server-side package flag — tracked in PORT_TODO.) Only screen 18 (review + save) remains a placeholder.
- **§37.5 — Setup wizard screen 16 (site & altitude) is a real form.** The wizard's site/altitude step now sets the horizon altitude (the minimum a target must clear to be imaged, 0–90°, validated) and the twilight definition (civil / nautical / astronomical) — bound to the working profile draft and folded into the profile's site section on Save (location itself comes from screen 1). Blank fields keep the profile default. (A separate soft-warning altitude + max-sequence-runtime cap from the §37.5 sketch aren't section-backed yet — tracked in PORT_TODO.) Screens 17–18 stay placeholders.
- **§37.5 — Setup wizard screen 15 (safety) is a real form.** The wizard's safety step now sets what ARA does when conditions become unsafe (Pause & park / Park only / Abort & park / Ignore), whether it auto-resumes when safe again, and the resume delay — bound to the working profile draft and mapped onto the profile's safety section on Save. Blank fields keep the profile default. (The finer §37.5 weather-type-granular actions and alarm sound/vibrate aren't section-backed yet — they stay in Settings → Safety; tracked in PORT_TODO.) Screens 16–18 stay placeholders.
- **§37.4 — Setup wizard screen 14 (imaging defaults) is a real form.** The wizard's imaging-defaults step now collects the default exposure (seconds, validated > 0) and default frame type (Light / Dark / Flat / Bias), bound to the working profile draft; the wizard Save folds them into the profile's imaging-defaults section alongside the gain / offset / binning / cooling already collected on the Camera screen. Blank fields keep the profile default. Screens 15–18 stay placeholders.
- **§37.4 — Setup wizard screen 13 (file saving) is a real form.** The wizard's file-saving step now collects the save directory (USB recommended per §29), file format (FITS / XISF), compression (lossless Rice, or off), and the filename template, bound to the working profile draft; the wizard Save maps them onto the profile's storage section. Blank fields keep the profile default, and the format/compression dropdowns offer "Keep profile default". (Advanced Gzip compression and the disk-space thresholds remain in Settings → Storage.) Screens 14–18 stay placeholders.
- **§37.4 — Setup wizard screen 12 (autofocus) is a real form.** The wizard's autofocus step now collects the routine's exposure (seconds), number of steps, step size (focuser steps), and a "re-run after a filter change" choice, bound to the working profile draft; the wizard Save maps them onto the profile's autofocus section. The numeric fields validate (whole number > 0) with inline errors and preserve the profile default when left blank; the filter-change control offers "Keep profile default / Yes / No". Screens 13–18 (file saving, imaging, safety, sky-data, review) stay placeholders.
- **§37.4 — Setup wizard screen 11 (plate solving) is a real form.** The wizard's plate-solve step is no longer a placeholder: it binds to the working profile draft (ASTAP binary path, star-database path, search radius, downsample factor) and the wizard Save now writes them onto the profile's plate-solve section (`pathOrEndpoint` / `indexDownloadPath` / search radius / downsample). First of the screens 11–18 to land; the rest (autofocus, file saving, imaging, safety, sky-data, review) stay placeholders pending follow-up.
- **§70 — Profile sharing: export (client).** Each profile in the Profiles screen gains an **Export…** action: it renders the profile into a `profile-share-v1` template via the daemon (`POST /api/v1/profiles/{id}/share-export`, which strips all paths / secrets / location / network per §70.1) and saves the JSON to a file you choose, with a sanitized suggested filename (`<profile>.araprofile.json`). Pairs with **Import** to complete the share round-trip entirely in-app — export a profile, hand the file to someone, and they import it as a starting template. Wraps the export endpoint via `ProfileApi.exportProfile` + a `ProfileShareExport` model; a blocking spinner covers the fetch and errors surface inline.
- **§70 — Profile sharing: import (client).** The Profiles screen gains an **Import** action: pick a shared `profile-share-v1` file, see a preview of what it'll create (the proposed name, what the template is/isn't, and the list of things you'll set up yourself — equipment, paths, location, PHD2, notification tokens), and on confirm it's committed into a new **non-active** profile you then make active and wizard your own gear into. Wraps the daemon's `/share-import` (preview) + `/share-import/commit` endpoints via `ProfileApi.importPreview` / `importCommit` + a `ProfileShareImportPreview` model; an unreadable / non-share file is reported inline rather than uploaded. Completes the §70 profile-sharing round-trip (export landed alongside the server import).
- **§70 — Profile sharing: import (server).** `POST /api/v1/profiles/share-import` previews a `profile-share-v1` file (validating the schema, parking the parsed manifest under a single-use token with a 15-minute TTL) and returns the proposed profile name, the rig the template was designed for, and a list of the fields the donor stripped that the recipient must re-enter (equipment, paths, location, PHD2, notification tokens). `POST /api/v1/profiles/share-import/commit` (token in the request body, never the query string, so it can't leak into access logs) then creates a new, **non-active** profile from the template's stripped settings and returns its id (`201 Created`). A file that isn't a recognized share → `422`; an unknown or already-consumed token → `404`; a flood of un-committed previews is capped → `429`. Pairs with the §70 export above; the recipient wizards in their own equipment + paths afterward. (Client import UI lands in a follow-up.)
- **§37/§30 — Profile management UI (client).** A new Profiles screen lists the daemon's saved profiles with the active one badged, and offers **Make active**, **Rename**, and **Delete** per profile plus **Add profile** (which runs the §37 setup wizard). Selecting a profile loads its settings into the live store; renaming and deleting hit the `/api/v1/profiles` CRUD endpoints and refresh the list. Deleting the active or last-remaining profile is refused by the daemon and the reason is surfaced inline. Wired on `ProfileApi` (list/rename/select/delete) + a `profileManagementProvider`.
- **§37 — The setup wizard now saves a profile.** Finishing the wizard (Save Profile, or Save & Exit at any point) creates a new active profile via `POST /api/v1/profiles` and writes the gear screens the user filled in (site, optics/focal length + pixel size, camera imaging defaults, PHD2 guider) onto it through the section endpoints. A blocking spinner covers the round-trip; a failure keeps the wizard open with the entries intact instead of dropping them. (The screens 11–18 sections fill in as those screens land.)
- **§70 — Profile sharing: export (server).** `POST /api/v1/profiles/{id}/share-export` now renders a real `profile-share-v1` template instead of a placeholder. The export strips everything host-, rig-, location-, and secret-specific so a recipient can use the profile as a starting point for their own gear (§70.4): all OS-specific paths (save directory, filename template, ASTAP executable + index paths), push/Telegram notification tokens, the donor's site location (lat/long/elevation/timezone), and the PHD2 host/port/profile. Optics + guide-scope geometry are lifted into a `rig_description` block (what the template was designed for) rather than applied as the recipient's values. The donor's tuning judgement (dithering, autofocus, meridian-flip, safety, diagnostics, stretch defaults) is preserved. Unknown profile id → 404. (Cross-OS path handling uses the "strip all paths" policy — the recipient re-enters their own in the wizard. Import preview/commit land in a follow-up.)
- **§37/§30 — Multiple profiles (server foundation).** The daemon now keeps a set of named profiles under `{profileDir}/profiles/{id}.json` with CRUD over `/api/v1/profiles` (list, create, get, rename, delete, select) instead of a single in-memory active profile. The active profile's live settings still flow through the existing `/api/v1/profile/*` section endpoints; the repository keeps the active profile's file in sync and, on `select`, loads a saved profile's settings into the live store. The legacy single `profile.json` is migrated into the new layout as the initial "Default" profile on first run. Deleting the active or last-remaining profile is refused. (Server foundation for editing/deleting/exporting profiles and cross-OS import; client UI + wizard Save land in follow-ups.)
- **§37 — The setup wizard's gear screens are real forms.** Stages 1–3 of the profile setup wizard (screens 1–10: profile name + site location, AlpacaBridge connect with a "Test connection" probe, live Alpaca discover-and-assign per equipment slot, then per-device setup for telescope / camera / filter wheel / focuser / mount / rotator / guider) are now functional forms bound to the working profile draft — replacing the Phase-12b placeholder bodies. Discovery scans the daemon's Alpaca devices per type and assigns each to a slot (with a "— None" option); the telescope screen derives focal ratio and the camera screen derives image scale (arcsec/pixel) live. The remaining screens (11–18: plate solving, autofocus, file saving, imaging defaults, safety, site, sky-data downloads, review + save) stay placeholders pending follow-up.

### Fixed
- **§28/§50 (v0.1.0) — Recovered-frame capture times are read as UTC, not local.** When the daemon recovers an orphaned FITS frame into the catalog, it reads the capture time from the file's `DATE-OBS` header. FITS defines that value as UTC, but it's usually written without a timezone suffix — and the parser was treating a suffix-less value as **local time**, shifting the stored `captured_utc` by the machine's UTC offset (and stamping it with a non-UTC offset that would have mis-sorted it in the stats time-window queries). A zoneless `DATE-OBS` is now correctly read as UTC, and an explicitly-offset one is converted to UTC, so recovered frames land at the right instant and sort correctly. (No effect on frames captured live through the sequencer — only the orphan-recovery path.)
- **§43-2 (v0.1.0) — Backups dir self-heals crash-only orphan archives at startup.** If the daemon was hard-killed (power loss / SIGKILL) mid-backup, two kinds of dead file could linger under `backups/` forever — a `.tmp-*.zip` staged before its atomic reveal, or a `backup-*.zip` whose `.meta.json` manifest never got written. The snapshot list already hid both, but nothing reclaimed the disk. The daemon now sweeps them on boot (a graceful create still cleans up after itself, and real manifest-backed snapshots are never touched), mirroring the §36-2c sky-data scratch sweep.
- **§55.1 (v0.1.0) — Settings-sync store self-heals orphaned temp files at startup.** The WILMA settings-sync store writes `client-settings.json` atomically via a `.tmp-*` sibling + rename; a hard kill in the window between the write and the rename could strand the temp file. The daemon now reclaims any such orphans on boot (the live settings file is never touched), the same boot-time sweep applied to backups.
- **§50 (v0.1.0) — Stats time-window filters normalize the cutoff to UTC.** The stats queries that take a `since` cutoff (Focus & Temperature, Guiding RMS) and the Calendar's rolling 30-day window now normalize the bound to UTC before comparing it against the stored UTC capture timestamps. A caller passing a non-UTC `DateTimeOffset` would otherwise have filtered against the wrong instant (the comparison is lexicographic on ISO strings). Latent today — all current callers already pass UTC — but it hardens every `since`-taking query via one shared helper.
- **§50.19 (v0.1.0) — Stats Achievements no longer flashes an error if a refresh fails exactly as you switch servers.** The Achievements section had its own copy of the refresh/generation-guard logic (it predated the shared one) that was missing a later fix: a refresh whose fetch *failed* right as the active server changed would rethrow a now-irrelevant error and could briefly flash a stale banner over the new server's records. Achievements now uses the same `StatsRefreshMixin` as the other seven Stats sections, which discards that stale failure — bringing all eight to identical, tested behavior.
- **§63 — A guider RPC call can no longer hang the connect forever on a silent/misbehaving guider.** `PHD2Guider.SendMessage` read the JSON-RPC response with `ReadLineAsync()`, but `TcpClient.ReceiveTimeout` only governs *synchronous* reads — so if the guider never returned a response with the expected id (a half-broken or wrong-version guider), that call and the whole connect handshake would block indefinitely. The async read is now bounded by the request's `receiveTimeout`, so a non-responsive call fails and surfaces instead of stalling. Surfaced by the §42.2 virtual-observatory bench.
- **§63 — Guider event listener no longer busy-polls the OS TCP table (could crash on macOS).** The guider's PHD2 event-stream reader (`PHD2Guider.RunListener`) previously detected a dropped connection by enumerating the OS active-TCP-connection table (`GetActiveTcpConnections()`) every 500 ms and also spun on `NetworkStream.DataAvailable`. That table enumeration could throw on macOS (null/duplicate local endpoints), tearing the listener down right after connect, and the 500 ms poll added latency + CPU churn. It's now a proper async line reader — events arrive as soon as the guider sends them, and end-of-stream (the guider closing the socket) is the disconnect signal that drives §63.3 crash-recovery. No behavior change against a real guider; removes a macOS-only failure path and the busy-loop.

### Changed
- **§38 — sequence-editor palette tiles are full-width rows.** The instruction palette renders as a vertical list of full-width tiles (ellipsizing long labels) rather than a chip `Wrap`, so it fits the narrow side pane without overflow.
- **§36 (v0.1.0) — macOS Sky Atlas renders; webview_cef on CEF 130 + Swift Package Manager, vendored as a submodule.** The embedded Aladin atlas (Planning tab) now renders on macOS. The `open-astro/webview_cef` fork was modernized — CEF 103 → **130.1.2** (matching Linux), **Swift Package Manager** support (resolving Flutter 3.44+'s "does not support Swift Package Manager" warning; CEF ships as `.xcframework` binary targets), and offscreen **WebGL via SwiftShader** (windowless CEF previously passed `--disable-gpu`, which rendered Aladin Lite v3 as a black rectangle). The client now vendors the fork as a git submodule at `client/openastroara_client/packages/webview_cef` (path dependency) instead of a git-SHA pin; the macOS Runner project moved from CocoaPods to Swift Package Manager (webview_cef was the only pod). The CEF binaries are git-ignored — run `packages/webview_cef/macos/setup_cef.sh` once after checkout (`git submodule update --init`) to stage them before a macOS build. Windows stays on CEF 101 pending a separate migration (open-astro/webview_cef#2).
- **§43-2b (v0.1.0) — Backup restore screen waits for the real outcome.** The Backup & Restore screen now polls the daemon's `clone-status` after starting a restore instead of reporting "Restore complete" the instant the request is accepted. It shows the actual result once the background worker finishes — including a **worker-side failure** (e.g. an extract/swap error) that the 202 response couldn't have revealed — and times out gracefully if the restore never reports a terminal state. Pairs with the §43-2b(a) server worker.
- **§43-2b (v0.1.0) — Backup restore runs on a background worker with live progress.** Restoring a snapshot (`POST /api/v1/backup/restore-zip`) now returns `202 Accepted` immediately and performs the extract + atomic swap on a background worker; `GET /api/v1/backup/clone-status` reports the worker's live state (`idle` → `running` → `done`/`failed`) instead of always `idle`. Validation that can fail fast (unknown snapshot → 404, corrupt-archive checksum → 422, unsupported source → 422) still happens synchronously before the 202, and a second restore while one is already running is refused with **409 Conflict** (one restore at a time — both mutate the live profile). The atomic all-or-nothing rollback behaviour is unchanged. (Still deferred to later §43-2b slices: remote/cloud restore sources and the frame-metadata/logs areas.)
- **§50 (v0.1.0) — Stats timestamp parsing is unified + hardened.** All six §50 stats models now share one `parseStatsUtc()` helper for wire timestamps. Four of them (Overview, Targets, Best Frames, Achievements) previously used a form that would mis-shift a timezone-less timestamp by the client's local offset; they now reinterpret it as UTC like the daemon intends. No user-visible change today (the daemon always sends offset-bearing timestamps), but it removes the latent drift and de-duplicates six copies into one tested helper.
- **§50 (v0.1.0) — Stats queries get a light-frame index.** Added a partial covering index `idx_frames_light_captured` on `frames(captured_utc) WHERE frame_type = 'light'`, so the Stats dashboard's light-frame, time-ordered queries (targets, calendar, frame-quality, best-frames, focus-temp) stop treating the `frame_type = 'light'` filter as a residual scan over the full catalog. Additive + idempotent — a database from an earlier build picks it up at next startup; no behavior change, just faster stats as the frame catalog grows.
- **§50 — Stats refresh keeps your data on screen (whole dashboard).** Manually refreshing any Stats section — Overview, Targets, Best Frames, and the Frame Quality / Guiding RMS / Calendar charts — no longer blanks it to a "Loading…" placeholder and back; the last-loaded data stays visible while the header spinner runs, and if the refresh fails the section keeps showing it (tile sections with a subtle "Couldn’t refresh — showing the last loaded …" banner, charts with a small "Stale" overlay chip) instead of dropping to a full error/retry screen. All six sections now share a `StatsRefreshMixin` (the §50.19 Achievements section's pattern), which also correctly discards a stale result — success or failure — when the active server changes mid-refresh.
- **§63.1 — The daemon connects to the guider as a service instead of launching it.** When you connect the guider, ARA core now connects to the already-running `openastro-guider`/PHD2 service on its host/port (default `localhost:4400`) — the normal headless deployment where the Pi boots the guider, ARA core, ASTAP, and the Alpaca bridge as services at power-on. If the guider isn't reachable yet, ARA asks the service manager to start it (`systemctl start`, via the same supervisor that handles §63.3 crash-recovery) and waits briefly, rather than spawning a local PHD2 process. The inherited NINA-desktop behavior — launching `phd2.exe` and waiting for its GUI window (`WaitForInputIdle`) — has been retired; it could not work on a headless daemon and blocked connecting to a guider running on localhost. Off-systemd (dev/CI) the start request is a no-op, so ARA simply connects to whatever guider is already listening.

### Added
- **§36/§25.5 (v0.1.0) — Tonight's Sky list in the Planning tab.** Switching the Planning tab to **Tonight's Sky** now shows a ranked side list of the curated objects best-placed above your site's horizon right now — each with its current altitude, transit (highest-possible) altitude, type, and magnitude — and **tapping one recenters the atlas on it**. A refresh button re-ranks for the current time. (Needs your site location set in Settings → Safety → Site.) Consumes the `GET /api/v1/planning/tonight` ranking below.
- **§36/§25.5 (v0.1.0) — Tonight's Sky ranking (server).** New `GET /api/v1/planning/tonight` ranks a curated deep-sky starter list by how high each object currently sits above your site's horizon (from the profile's latitude/longitude + horizon setting), best-placed first — the data behind the Planning tab's **Tonight's Sky** mode. Each entry carries its current altitude, its highest-possible (transit) altitude for your latitude, and J2000 coordinates so the atlas can recentre on it. Altitudes use a self-contained sidereal-time computation (no external ephemeris). The full smart-culled catalog lands later.
- **§36/§25.5 (v0.1.0) — "Refresh from connected camera" on the Optics panel.** Settings → Imaging → Optics now has a button that pulls the connected camera's sensor size and pixel pitch straight into the form (and saves them), so you can re-fill the geometry on demand — handy after swapping cameras with the panel open. If no camera is connected (or it doesn't report its sensor) it tells you instead of writing junk; your focal length and reducer are left untouched.
- **§36/§25.5/§30.7 (v0.1.0) — Connecting your camera auto-fills the Planning FOV.** When you connect a camera, the daemon now reads its sensor size and pixel pitch and caches them into the profile's optics section, so the Planning Frame field-of-view box works without you typing those in — just set your focal length (and reducer, if any). If you later connect a different camera, the cached sensor geometry is refreshed to match it. Your focal length and reducer are never overwritten (they come from the telescope / your manual entry), and a camera that doesn't report its geometry is left alone.
- **§36/§25.5/§47 (v0.1.0) — Planning Frame mode draws a mosaic panel grid.** With Frame mode on, the framing controls' panel grid (columns × rows + overlap %) now draws as a live mosaic of FOV rectangles on the atlas — each panel one field of view, adjacent panels overlapping by the set percentage, the whole grid rotated to the framing position angle and tracking the view as you pan/zoom. A 1×1 grid is the single FOV box. (The "Add to Sequence" / "Build Mosaic Sequence" output is the next slice — it needs the server mosaic service.)
- **§36/§25.5 (v0.1.0) — Planning Frame mode draws your rig's field of view on the atlas.** The Planning tab's **Frame** toggle now overlays the actual field-of-view rectangle your camera + telescope see, drawn on the Aladin sky and tracking the view as you pan/zoom. It's driven by a new **Settings → Imaging → Optics (FOV)** panel where you enter focal length, reducer/barlow factor, sensor width/height, and pixel size — the panel shows the resulting field of view and pixel scale live as you type, then persists to the daemon (`/api/v1/profile/optics`). Until the optics are set the overlay stays hidden (no fake box). The mosaic grid + "Build Sequence" output land in the next slices.
- **§36 (v0.1.0) — Sky Atlas search moves the atlas + webview_cef forked for Flutter 3.44.** The Sky Atlas universal-search bar now drives the embedded Aladin Lite: a submitted target name (Simbad/Sesame) or coordinates recenters the atlas via Aladin's `gotoObject` (a search made before the browser finishes loading is applied once it's ready). Also fixes a build blocker found while wiring this up: upstream `webview_cef` no longer compiles against Flutter 3.44+ (missing `TextInputClient.onFocusReceived`), so the app now depends on the **[open-astro/webview_cef](https://github.com/open-astro/webview_cef) fork** (pinned by commit) carrying a one-line shim — the §36 embed builds on-device again. See PORT_DECISIONS.md.
- **§36 (v0.1.0) — Sky Atlas embeds Aladin Lite (in-app, all desktops).** The Sky Atlas tab's placeholder is replaced with a real embedded **Aladin Lite (CDS)** sky atlas, rendered in-tab via `webview_cef` (a Chromium/CEF browser composited into the Flutter widget tree) so it works identically on macOS, Windows, and Linux through one code path. The view owns the browser lifecycle with loading and "atlas unavailable" fallback states. This is the first §36 Aladin slice; the universal-search "goto", the Tonight's-Sky projection wiring, and offline-bundling the Aladin Lite JS (§36.1) follow. See PORT_DECISIONS.md for the cross-desktop webview engine choice + trade-offs.
- **§50.4 (v0.1.0) — Stats Focus & Temperature scatter is now live.** The Stats dashboard's **Focus & Temperature** visualization is wired to the daemon's `GET /api/v1/stats/focus-temp` instead of the Phase-12g demo that derived points from the in-memory library. It now plots one point per captured frame that recorded a focuser position — sensor temperature (°C) against focuser step position — with the server-computed Pearson r² in the subtitle (how strongly the two track, for §37.4 temp-comp setup), and loading / error+retry / no-server / no-positioned-frames states plus a manual refresh. This is the last of the §50 Visualizations charts to come off demo data; all six are live now. Completes §38 Focus-Temp (column → capture writes → query → chart).
- **§50.4 (v0.1.0) — Focus & Temperature endpoint is live.** `GET /api/v1/stats/focus-temp` now returns real data instead of an empty stub: every catalog frame that recorded a focuser position becomes a (sensor temperature, focuser position) point, plus a Pearson r² correlation gauging how strongly the focus point tracks temperature (the temp-comp slope). The r² is null when it isn't defined (fewer than two points, or zero variance in either axis). Builds on the §38 `focuser_position` column; the client scatter chart is the last slice.
- **§38 (v0.1.0) — Captures record the focuser position.** When a focuser is connected, a manual capture now stamps its step position into the FITS `FOCUSPOS` header and the catalog's `focuser_position` column, so the §50.4 Focus & Temperature view has real data to plot (the position is simply omitted when no focuser is connected). The position is snapshotted from the focuser at capture time via the shared `IFocuserMediator`. Builds on the §38 catalog column; the `/stats/focus-temp` query and the client chart follow.
- **§38 (v0.1.0) — Catalog records per-frame focuser position.** The frame catalog gains a `focuser_position` column so the daemon can persist the focuser step position with each captured frame — the data the §50.4 Focus & Temperature view needs (it's an intentional empty stub until this is populated). The column is additive: a fresh database gets it from the table definition, and a database created by an earlier build is brought forward with an idempotent `ADD COLUMN` at startup (v0.0.1 ships no migrations runner, so this is the sanctioned additive-column path). The FITS scanner now reads `FOCUSPOS`/`FOCPOS` from recovered files into the column. First slice of §38 Focus-Temp; the capture path writing the keyword, the `/stats/focus-temp` query, and the client chart follow.
- **§50.6 (v0.1.0) — Stats Calendar heatmap is now live.** The Stats dashboard's **Calendar Heatmap** is wired to the daemon's `GET /api/v1/stats/calendar` instead of the Phase-12g demo (integration summed from the in-memory library). It now shades each day in the rolling 49-day window by that night's real integration minutes (server-aggregated per-day frame counts + integration hours + targets), with loading / error+retry / no-server states and a manual refresh. Third of the §50 Visualizations charts to come off demo data; the Focus & Temperature scatter is the last, deferred until the daemon persists per-frame focuser position (§38 — its `/stats/focus-temp` endpoint is currently an intentional empty stub).
- **§50.7 (v0.1.0) — Stats Guiding RMS chart is now live.** The Stats dashboard's **Guiding RMS Trend** visualization is wired to the daemon's `GET /api/v1/stats/guiding` instead of the Phase-12g demo (per-session RA/Dec from the in-memory library). It now plots the per-frame total guiding RMS (arcsec) chronologically, with mean + p95 in the subtitle, dots on sparse series, a date-labeled x-axis, and loading / error+retry / no-server / no-guiding-data states plus a manual refresh. (The daemon nulls the RA/Dec breakdown until separated columns exist, so today it's a single total-RMS line.) Second of the §50 Visualizations charts to come off demo data.
- **§50.10 (v0.1.0) — Stats Frame Quality chart is now live.** The Stats dashboard's **Frame Quality** visualization is wired to the daemon's `GET /api/v1/stats/frame-quality` instead of the Phase-12g demo that approximated quality with per-session average HFR. It now renders the canonical composite-score distribution: a 10-bucket histogram of frame composite quality (0–1, higher = better — HFR + star count + background), with the mean in the subtitle, color-banded bars, and loading / error+retry / no-server / no-scored-frames states plus a manual refresh. First of the §50 Visualizations charts to come off demo data.
- **§50 (v0.1.0) — Stats Overview is now live.** The Stats dashboard's **Overview** section is wired to the daemon's `GET /api/v1/stats/overview` instead of the Phase-12g in-memory demo data: real catalog totals for the active profile — sessions, frames, light frames, total integration, unique targets, and last-imaged date — with loading / error+retry / no-server / no-frames-yet states and a manual refresh. (The Nights and Avg-HFR demo tiles drop from Overview — nights live in the §50.19 Achievements section, HFR in the Frame-Quality chart.)
- **§50 (v0.1.0) — Stats Best Frames is now live.** The Stats dashboard's **Best Frames** section is wired to the daemon's `GET /api/v1/stats/best-frames` instead of the Phase-12g demo list: the catalog's top frames ranked by composite quality score (best first), each showing target, score, filter, and capture date, with loading / error+retry / no-server / no-scored-frames states and a manual refresh. This is the last Stats-dashboard section to come off demo data — Overview, Targets, Best Frames, and Achievements are all live now. The Phase-12g demo `stats_state.dart` (the last in-memory `bestFramesProvider`) is removed.
- **§50 (v0.1.0) — Stats Targets is now live.** The Stats dashboard's **Targets** section is wired to the daemon's `GET /api/v1/stats/targets` instead of the Phase-12g demo rollups: real per-target frame count, total integration, mean composite quality score (when scored), and last-imaged date for the active profile, busiest first — with loading / error+retry / no-server / no-targets states and a manual refresh. Reuses the same targets endpoint the AstroBin export picker already consumes; integration-time + date rendering is now a shared `stats_format` helper used by both the Overview and Targets sections.
- **§55.1 (v0.1.0) — Settings-sync client data layer.** Client groundwork for multi-device settings sync: a `ClientSettings` model (the opaque preferences map + last-updated time), a `ClientSettingsApi` (fetch / replace over `/api/v1/client-settings`), and a `clientSettingsProvider` Riverpod `AsyncNotifier` bound to the active server — it reads the synced preferences on connect and offers `save` (replace the whole blob) + `merge` (update only a feature's own keys, last-write-wins per key) actions that land the server's stored view and throw on failure. No UI yet; specific preferences opt into the synced blob as features adopt it. Consumes the §55.1 server store.
- **§55.1 (v0.1.0) — Multi-device WILMA settings sync (server store).** New `GET`/`PUT /api/v1/client-settings` persist an opaque UI-preferences blob per profile under `{profileDir}/client-settings.json`, so a user's other devices can pick up the same WILMA layout/theme on connect. The server treats the blob as an opaque JSON object (the client owns its shape) and the store is **last-write-wins** (the §67 trusted-LAN single-user model). Writes are atomic (temp file + rename) and serialized so two devices saving at once can't tear the file; the blob is capped at 256 KiB, a non-object or oversized `PUT` is a 400, and a corrupt/hand-edited file degrades to an empty object rather than wedging the client. Server slice; the WILMA client wires read-on-connect / write-on-change next.
- **§36 (v0.1.0) — Sky-data: conditional updates (skip re-downloading an unchanged package).** Re-downloading an already-installed sky-data package (a force reinstall — "check for updates") now issues a conditional HTTP GET using the remote `Last-Modified` the original install recorded. If the package is unchanged the CDN answers **304 Not Modified**, and ARA keeps the existing install untouched and reports the download complete (up to date) instead of re-fetching a multi-GB archive. A fresh install fetches unconditionally and records the validator for next time; an install that predates this (no stored validator) simply re-downloads as before. First slice of the §55.1 "survey downloader polish" line.
- **§50.19 (v0.1.0) — Stats: AstroBin export from the app.** The Stats dashboard's **Export CSV** menu gains an **AstroBin (per target)…** entry that opens a target picker (real targets from the live daemon's `/api/v1/stats/targets`, busiest first, each with frame count + integration hours). Picking a target opens its AstroBin acquisition CSV (`/api/v1/stats/export/astrobin?target=…`) in the browser / save dialog. Loading / error+retry / no-server / no-targets states are handled; the launch is restricted to http(s) (an allowlist guards against a rogue server steering into a `javascript:`/`data:` scheme). Consumes the §50.19 server export slice.
- **§50.19 (v0.1.0) — Stats: AstroBin acquisition export.** New `GET /api/v1/stats/export/astrobin?target=<name>` streams an AstroBin-ready acquisition CSV for one target: light frames grouped per (night, filter, sub-length, gain, sensor-cooling) with the sub count, in AstroBin's documented long-exposure import column layout (`date,filter,number,duration,…,gain,sensorCooling,…`). Darks/flats/bias and site/optical columns are left blank for the user to fill in on AstroBin (a calibration set can't be attributed to a specific filter/sub-length row). A missing `target` returns 400; a target with no light frames returns 404. Server slice; the Stats view's export menu wires to it next.
- **§50.19 (v0.1.0) — Stats: Achievements & milestones.** New `GET /api/v1/stats/achievements` returns light-gamification records aggregated from your existing light-frame catalog (no new instrumentation): total nights imaged, longest + current consecutive-night streaks, your biggest single night (hours), total integration hours, unique targets, total light frames, and first-light date — plus a set of milestone badges (integration-hour / target-count / night-count / frame-count tiers) each with its threshold, current value, and achieved flag. Dark/flat frames are excluded; "nights" are distinct capture days. Server slice; the WILMA Stats view wires to it next.
- **§50.19 (v0.1.0) — Stats: Achievements view.** The Stats dashboard gains an **Achievements** section wired to the live daemon (`GET /api/v1/stats/achievements`): record tiles for current + longest night streaks, nights imaged, your biggest single night, total integration, unique targets, light frames, and first-light month, plus a grid of milestone **badges** — each achieved badge highlighted, each locked one showing a progress bar and current/threshold. Loading / error+retry / no-server / no-frames-yet states are handled, with a manual refresh. A footnote notes the two honest caveats: nights use the UTC calendar day (a session crossing midnight UTC counts as two), and target badges only count frames that recorded a target name. Consumes the §50.19 server slice.
- **§43 (v0.1.0) — Backup & Restore screen.** Settings → Storage now has an **Open Backup & Restore** button that opens a full-screen manager: it lists the active server's profile-config snapshots (date, size, captured areas), **Create backup** makes a new one, each row can be **Download**ed (opens the snapshot's zip), and **Restore** opens a destructive-action confirmation that lets you pick which captured areas (profile settings / sequences) to overwrite before restoring — Restore stays disabled until at least one area is selected. Loading / error+retry / no-server / empty states included. Consumes the §43 client data layer.
- **§43 (v0.1.0) — Backup client data layer.** Client groundwork for the Backup & Restore screen: a `BackupSnapshot` model, a `BackupApi` (list snapshots / create backup / restore selected areas over `/api/v1/backup/*`, plus an absolute-download-URL helper), and a `backupSnapshotsProvider` Riverpod `AsyncNotifier` bound to the active server with create + restore actions (create re-reads the list so the new snapshot appears; both surface the daemon's 4xx as thrown errors the UI can show). No UI yet; the Backup & Restore screen wires to these next.
- **§43-2a (v0.1.0) — Backup: restore a snapshot.** `POST /api/v1/backup/restore-zip` now actually restores — it validates the chosen snapshot against its stored SHA-256, then rolls the selected areas (`restore_profiles` → `profile.json`, `restore_sequences` → the `sequences/` tree) back to that snapshot. The swap is **atomic with rollback**: each live area is moved aside, the snapshot's copy is moved in, and the set-aside originals are deleted only once *every* selected area has landed — if any area fails, the already-swapped ones are rolled back, so a failed restore never leaves a half-old/half-new profile. A corrupt archive is refused before any live config is touched; an unknown snapshot returns 404, and a non-local source or no-area-selected returns 422. The restore source is a local snapshot URL (the one `GET /snapshots` hands back); restoring from a remote/cloud URL and live restore-progress reporting come in §43-2b. Replaces the prior 501 stub.
- **§43-1 (v0.1.0) — Backup: real ZIP snapshots of your profile config.** `POST /api/v1/backup/create-zip` now actually packages your profile configuration — `profile.json` plus the whole `sequences/` tree — into a self-contained `.zip` under `{profileDir}/backups/`, with a sidecar manifest recording its SHA-256, size, and which areas it captured. `GET /api/v1/backup/snapshots` lists what's been backed up (newest first, each with a download link), and the new `GET /api/v1/backup/snapshot/{id}/download` streams a snapshot back. Packaging is crash-safe (it writes to a temp file and renames it into place, so a reader never sees a half-written archive) and only claims an area that's actually present. Replaces the placeholder that returned one fixed sample snapshot. Restoring a snapshot (which overwrites live config) and its progress reporting are deferred to §43-2 — the restore endpoint honestly returns **501 Not Implemented** until then rather than a misleading success.
- **§36-2c (v0.1.0) — Sky-data scratch sweep on startup.** The daemon now reclaims orphaned `.staging-*` / `.backup-*` extraction scratch directories under the sky-data root at boot. These only linger when a download worker is hard-killed mid-extract (a crash) — a normal finish removes its own scratch — so the sweep cleans up what a graceful drain can't catch, on a daemon restart. Real package directories are never touched.
- **§36 (v0.1.0) — Data Manager screen (Sky Atlas).** The Sky Atlas "Data Manager" modal is now functional: it lists the active server's curated sky-data catalog grouped by category, shows each package's size and on-disk install state, and totals installed bytes in the header. Each row drives the real backend — **Download** a not-installed package (with a live progress bar fed by the `data_manager.download.*` WebSocket stream), **Cancel** an in-flight download, or **Remove** an installed one; a failed/stalled download surfaces its reason inline, and a download error (e.g. already-installed 409) shows a snackbar. Replaces the placeholder tab stub.
- **§36 (v0.1.0) — Data Manager client data layer.** Client groundwork for the Sky Atlas Data Manager screen: a `DataPackage` model, a `DataManagerApi` (list packages / start download / cancel / delete over `/api/v1/data-manager/*`), and two Riverpod providers — the package catalog (loaded from the active server, with download/cancel/delete actions) and a live download-progress map folded from the `data_manager.download.{progress,complete,failed}` WebSocket stream (a completed download re-reads the catalog so the row flips to installed). No UI yet; the Data Manager modal wires to these next.
- **§36-2b-2 (v0.1.0) — Data Manager sentinel-aware inventory.** The Data Manager's package listing and state now treat a package as installed only when its `.installed` completion sentinel is present — a torn or interrupted install (files on disk, no sentinel) reads as *not* installed rather than appearing complete. `InstalledUtc` is taken from the sentinel's write time (the install-complete stamp) instead of the directory's mtime (which changes on any later child write), and the sentinel file is excluded from the reported on-disk size.
- **§36-2b (v0.1.0) — Data Manager download engine.** The Data Manager now actually downloads sky-data packages. `POST /api/v1/data-manager/download` starts a background worker that streams the package's `.tar.gz` from its source URL through the §36-2a installer (extract → atomic swap → sentinel), emitting `data_manager.download.progress` / `.complete` / `.failed` WebSocket events as it goes; `GET /state` reports in-flight downloads with live byte/percent progress; `POST /cancel/{id}` stops a running download (and 404s for an unknown/finished id). One active download per package (a duplicate request returns the running one's id). Extraction is capped at the catalog's advertised size + headroom as a zip-bomb guard, and a download that fails after the in-place swap began reports possible data loss distinctly. The HTTP fetch sits behind an `ISkyDataFetcher` seam so the worker is fully unit-tested without the network.
- **§36-2a (v0.1.0) — Sky-data install engine (safe + atomic tar.gz extraction).** Internal groundwork for the Data Manager download engine: extracting a downloaded `.tar.gz` sky-data package into its install directory now goes through a hardened installer that (a) rejects any archive entry resolving outside the destination (tar-slip / path traversal) and skips link/device entries, (b) stages extraction into a sibling temp directory and only swaps it into place once complete, and (c) writes an `.installed` sentinel so a reader can always tell a finished install from one torn by a crash or cancel. A failed or cancelled install leaves no partial directory and preserves any prior install. Consumed by the §36-2b download worker (HTTP fetch → progress events → cancellation).
- **§36 (v0.1.0) — Data Manager: real sky-data package inventory.** The Data Manager now reflects what's actually on disk instead of a fixed sample list: it scans the data root (`{profileDir}/sky-data`) to report which curated packages (star catalogs, horizon profiles — HiPS sky surveys once the §36-2 download engine lands) are installed, their measured on-disk size, and install time; deleting a package frees its directory. Package ids are validated against the catalog so a delete request can never escape the data root. The download/cancel half is acknowledged but deferred to §36-2. First v0.1.0 feature.
- **§68.1 — AlpacaBridge version gate on equipment connect.** ARA now verifies the AlpacaBridge equipment hub's version before connecting a device: on each Alpaca-device connect it probes the bridge's `GET /version` and, if the bridge is older than the minimum supported **1.2.0**, refuses the connect with **HTTP 503** and `code: "alpaca_bridge_outdated"` rather than silently driving your gear through an untrusted hub. An acceptable or newer bridge connects normally; an unreachable/no-`/version` bridge is treated as a transient miss and re-probed on the next attempt (a device that was discovered is reachable, so it proceeds). The handshake is cached ~30s so a burst of connects doesn't re-probe. The guider is unaffected — it's the PHD2 daemon, not an Alpaca device. When the bridge is in the **1.2–1.5** band it connects normally but emits an `equipment.alpaca_bridge_outdated_warn` advisory event (carrying the bridge version + minimum/recommended thresholds) so the client shows a dismissible "AlpacaBridge x.y detected — z.0+ recommended" banner on the Equipment screen — purely advisory, best-effort, never blocks the connect. Dismiss is per-version, so a later warn carrying a different bridge version re-shows. (§68-a/§68-b + client §68.2)
- **§42.2 — Virtual-observatory bench: Linux/arm64 lane (bench-5).** A hermetic `docker compose` lane (`bench/`) that builds and runs the hardware-free bench suite — `AlpacaFaultProxyTest`, `FakeGuiderTest`, `GuiderFakeIntegrationTest` (29 tests) — inside a `linux/arm64` .NET 10 SDK container, exercising the bench on the same kernel + runtime family the §13 Raspberry Pi deployment targets rather than only the developer's macOS host. Primarily this keeps a standing Linux check on the `AlpacaFaultProxy` connection-drop fault, whose partial-response→reset mechanic is kernel/runtime-sensitive (per the `design/PORT_TODO.md` bench-5 note). The image copies the source in rather than bind-mounting, so the Linux build never contaminates the host's `osx-arm64` build tree; `bench/run.sh` builds + runs it and exits with the test pass/fail code. Verified green (29/29) on colima/aarch64. Not shipped in any daemon artifact.
- **§42.2 — Virtual-observatory bench: guider fault scenarios (bench-4).** Drives the **real** `GuiderService`/`PHD2Guider` through two §42.2 device-fault paths against the bench's `FakeGuider`, no hardware: (1) a **lost guide star** mid-session — the fake emits a PHD2 `StarLost` event and the daemon reflects it as the `star_lost` runtime state while keeping the link **Connected** (a guiding-quality fault, not a disconnect); and (2) a **dropped guider link** — the fake forcibly closes the live socket (new `FakeGuider.DropConnections` bench primitive; the listener stays up), the daemon's listener turns the EOF into `PHD2ConnectionLost`, moves the session to **Error**, and begins §63.3 recovery. Builds on bench-3's lifecycle. Not shipped in any daemon artifact.
- **§42.2 — Virtual-observatory bench: real-client guider lifecycle (bench-3).** Drives the **real** `GuiderService`/`PHD2Guider` against the bench's `FakeGuider` over the loopback PHD2 JSON-RPC protocol (no C++ daemon) through the full lifecycle: connect to the already-listening fake on localhost → run the §63.4/.5 handshake → reach **Connected** → reflect a live `AppState` event into the runtime guiding state → accumulate **RMS** from `GuideStep` events → disconnect. This is the scenario that surfaced the three guider-path fixes (connect-as-service, read-driven listener, SendMessage timeout); with those in, it now passes end-to-end in ~0.5s and is the foundation for the §42.2 fault scenarios.
- **§42.2 — Virtual-observatory test bench: fake PHD2 guider (bench-2).** Adds `FakeGuider` to the `OpenAstroAra.TestHarness` assembly — a scriptable TCP fake of the openastro-guider / PHD2 event server. It speaks the PHD2 JSON-RPC line protocol (greets each connection with `Version` + `AppState`, answers RPC requests with canned/overridable results keyed by method, and broadcasts events to connected clients), with `PhdEvents` builders for the notifications the ARA client consumes (StartCalibration/CalibrationComplete, StartGuiding, GuideStep, Settling/SettleDone, StarLost, Paused/Resumed, GuidingDithered). This lets the bench drive the real ARA guider client through calibrate → guide → dither → star-lost flows without the C++ daemon. +7 protocol tests. Builds on bench-1; the end-to-end meridian-flip + fault scenarios come next. Not shipped in any daemon artifact.
- **§42.2 — Virtual-observatory test bench: Alpaca fault proxy (bench-1).** New internal `OpenAstroAra.TestHarness` assembly seeds a hardware-free test bench for the §42.2 fault-recovery and autonomy scenarios. Its first component, `AlpacaFaultProxy`, is a loopback reverse proxy that sits between the daemon and a real Alpaca device (the ASCOM OmniSim) and can inject the transport-level fault modes a real driver exhibits — an Alpaca error, an HTTP failure, a dropped connection, an unresponsive hang, or a motion that never settles (response-value rewrite) — scoped per device/method with one-shot or sticky lifetimes. This lets the recovery flows be driven against simulated faults with no physical hardware; the fake guider and end-to-end scenarios build on it next. Not shipped in any daemon artifact.
- **§63.6 — Build the guider's dark library & defect map from the app.** The guider dialog (tap the GUIDE chip) gains a **Calibration…** button when connected, opening a calibration dialog: for the dark library and the defect map it shows whether each is built and loaded (plus the loaded-dark count + exposure range), a **Build**/**Rebuild** button, and an enable/disable switch. Build runs in the background on the daemon (202-Accepted); the dialog reflects the result on refresh. Previously these were reachable only via the daemon's REST API. (Live build-progress over the WS stream is a follow-up.)
- **§63 — Connect the guider from the app.** The GUIDE chip in the top equipment bar is now live: its status dot reflects the daemon's PHD2 guider — grey when disconnected, blue while connecting, green when guiding, amber while calibrating/dithering, red on a lost star or error. Tapping it opens a dialog showing the link state, guiding phase, active profile, and RMS, with **Connect** / **Disconnect** / **Refresh** actions (connect targets the local guider at `localhost:4400`). Previously the guider was only reachable from the daemon's REST API. The dark-library/defect-map build UI builds on this next.
- **§51 / §60.9 — The §51 health indicator + diagnostics panel are now live.** The always-visible health pill (Imaging tab) and the diagnostic event panel now roll up the live `diagnostics.*` WS stream instead of showing a fixed "not connected" placeholder: a raised issue turns the pill amber/red with an "_N_ open issue(s)" label and logs an actionable one-line entry, an auto-action is shown with what the daemon did, and a cleared issue drops out of the roll-up and returns the pill to *nominal* once the last one clears. When no server is saved the panel reads *not connected*. Consumes the §60.9 server-side producer; the worst open severity drives the overall colour.
- **§51 / §60.9 — Diagnostics now stream live over WebSocket.** When the daemon raises a diagnostic (e.g. the §29 low-disk warning) it emits a `diagnostics.issue_detected` (or `diagnostics.auto_action_taken`) WS event, and clearing it emits `diagnostics.cleared` — so a connected client's diagnostics view can update in real time instead of only on a manual refresh. Best-effort: a failed publish is logged and never blocks the diagnostic write.
- **§60.9 — Live server-connection indicator (Phase 12c.3).** The status bar now shows the real WebSocket link to the daemon — *Connecting…*, *Connected*, *Reconnecting…*, or *Disconnected* — instead of a fixed "Disconnected" placeholder, so you can see at a glance whether the app is receiving live updates. Backed by the new §60.9 WS event-stream client (auto-reconnect with resume), this is the first consumer of the live event stream; per-feature consumers (diagnostics, guider build progress) build on the same stream.
- **§63.6 — Enable/disable the guider's calibration artifacts over REST (guider-e-4c-c).** Two endpoints toggle whether the guider applies its loaded calibration: `POST /api/v1/equipment/guider/darklibrary/enabled` (dark subtraction) and `POST /api/v1/equipment/guider/defectmap/enabled` (bad-pixel correction), each taking `{enabled}` and returning the updated calibration status. So a user can turn off dark subtraction or defect-map correction without deleting the files. Toggling with no guider connected returns 409; a daemon rejection (e.g. enabling with no camera connected) returns 422 with the reason.
- **§63.6 — Build a guider defect (bad-pixel) map over REST (guider-e-4c-b-2).** `POST /api/v1/equipment/guider/defectmap/build` captures a master dark and builds the guider's bad-pixel map for the active profile (202-Accepted; runs in the background and reports `guider.defect_map.started` / `…complete` / `…failed` over the WebSocket stream — same no-progress-bar contract as the dark-library build). A bad request (frame count outside 1–50 or exposure outside 1–600000 ms) is rejected with 400; building with no guider connected returns 409. Dark-library and defect-map builds share one in-flight gate — the daemon can only run one calibration capture at a time, so starting a second build while one is running returns 409. The existing `GET …/darklibrary/status` already reports defect-map existence/load state. The cover-the-scope build UI is the next follow-up.
- **§63.6 — Build a guider dark library over REST (guider-e-4b-2).** New endpoints let the app build the guider's dark-frame library and check its calibration-files state: `POST /api/v1/equipment/guider/darklibrary/build` dispatches the build (202-Accepted; it runs in the background and reports `guider.dark_library.started` / `…complete` / `…failed` over the WebSocket stream — there's no progress bar because the guider's build is a single blocking call with no progress events), and `GET /api/v1/equipment/guider/darklibrary/status` returns whether a dark library / defect map exists, is loaded, and the loaded-dark count + exposure range. A bad request (frame count outside 1–50, exposure bounds outside 1–600000 ms, or min > max) is rejected with 400; building with no guider connected returns 409. The cover-the-scope build UI is the next follow-up.
- **§63.6 — Dark-library build on the guider client (guider-e-4b, internal).** The PHD2 guider client gained the methods that drive the daemon's dark-library / calibration-files surface: build a dark library for the active profile, and read the calibration-files status (which dark library / defect map exist, whether they're loaded, and the loaded-dark stats). Frame count (1–50) and exposure bounds (1–600000 ms, min ≤ max) are validated before the request leaves the client, so a bad request gets a clear error instead of the daemon's opaque code. Not yet reachable from the app — the REST endpoint + the cover-the-scope build UI are the next follow-ups. Note: `build_dark_library` is a blocking call with no progress events, so the service layer will report start/finish rather than a granular progress bar.
- **§63.4 — Each profile gets its own guider profile (guider-e-3).** On connect, ARA now selects a PHD2 profile named after your active ARA profile (`ara-<slug>-<id>`, e.g. *C14 on CEM120* → `ara-c14-cem120-a3f8e1c2`), creating it automatically the first time. The short id suffix guarantees two ARA profiles with similar names never share one PHD2 profile. Because PHD2 keeps calibration and the dark library per profile, switching ARA profiles now keeps each rig's guider state separate instead of sharing one PHD2 profile — and the §63.5 engine settings push lands in the right profile. If you've set an explicit PHD2 profile ID in your guider settings (advanced override), that still wins. Best-effort: a profile RPC the daemon rejects is logged and never blocks the connection. Needs a connected guider to take effect.
- **§29 — Low disk-space warning.** A background monitor watches free space on your image save volume and, when it runs low, raises a diagnostic (Yellow under ~10 GB free, Red under ~2 GB) and — if you've enabled the *Low disk space* notification — an inbox alert, so an unattended session doesn't silently die on a full disk. It only warns: it never blocks a capture or deletes anything, and the warning clears itself once space frees up. The warning/critical thresholds are configurable under **Settings → Session → Storage** (default 10 GB / 2 GB free); critical must stay below warning, and a mis-set pair falls back to the defaults.
- **§29 — Optional hard stop on critically-low disk.** A new safety policy (**Settings → Safety → Policies → On critically-low disk**) chooses what happens when free space hits the critical threshold: *Warn only* (default — the existing diagnostic + notification) or *Abort the running sequence* (also halts any in-progress sequence so it doesn't keep capturing into a near-full disk, with a critical notification recording the stop). Nothing is ever deleted.
- **§63.5 — Edit the guider engine settings in the app.** Settings → Equipment → Guider gained a **Guider engine** section to set the guide focal length, pixel size, RA/Dec aggressiveness, minimum move, and Dec guide mode — the values the daemon pushes to PHD2 on connect. Each has inline help and is findable via ⌘K search. Completes the §63.5 chain (profile → on-connect push → in-app editor).
- **§63.5 — Guider engine settings now pushed to PHD2 on connect.** The guider-engine parameters you set in your profile (guide focal length, pixel size, RA/Dec aggressiveness, minimum move, Dec guide mode) are now sent to the guider daemon when it connects — so a fresh PHD2 profile automatically picks up your scope/camera and guiding aggressiveness instead of using its own defaults. Each setting is applied best-effort (a value a given PHD2 build doesn't accept is logged and skipped, never blocking the connection). Needs a connected guider to take effect.
- **§63.5 — Guider engine settings in your profile.** The PHD2 profile section gained the core guiding-engine parameters — guide-scope focal length, guide-camera pixel size, RA/Dec aggressiveness, minimum move, and Dec guide mode — saved and persisted alongside the existing connection/dither settings. ARA now owns these as the source of truth; pushing them to the guider daemon on connect (and the wizard screen to edit them) is the next step.
- **§39 — Calibration matching from your real frames.** The calibration view now reflects what you actually shot: the daemon derives each imaging session from the frame catalog, summarizes the filters used (with frame counts and mean exposure), and tells you whether you already have matching flats (by filter) and darks (by exposure + gain + sensor temperature) — replacing the old fixed sample data. Asking for matching flats generates a per-filter capture plan (default 20 frames each, overridable). Dark matching buckets temperature to the nearest whole degree, so a cooled camera's darks must be shot at the light's set-point to count; uncooled cameras (which record a 0 °C sentinel) are unaffected.
- **§18.I — Plate-solve from the Imaging tab.** The app now has a **Solve frame** button under the frame viewer: after you capture a frame, one tap runs it through ASTAP and shows the solution inline — RA, Dec, image rotation, and plate scale (or a clear "no solution / not configured" message). Previously the solve endpoint was reachable only by hand (curl/API). Capturing a new frame clears the previous result.
- **§58.4 — Automatic meridian flip.** When a target crosses the meridian mid-sequence, the daemon now runs the full flip recovery automatically: it stops guiding, waits out the configured pause window, flips the mount, plate-solves and re-centers on the exact target, then resumes guiding and settles — and verifies the mount's pier side afterward. A failed flip halts the sequence safely (tracking restored, guider resumed) rather than imaging on a mis-aimed scope. The meridian-flip trigger is now wired into the sequencer, so a saved sequence with a flip trigger actually executes it. Re-focus after the flip is skipped for now (the live autofocus sweep is still pending) and is logged when configured. Needs a connected mount + a configured plate-solver; mount-gated for live validation.
- **§28 — Slew-and-center service.** The daemon can now center the mount precisely on a target: it captures, plate-solves, syncs the mount to the true position, and re-slews — repeating until the target is within the configured centering tolerance. This is the building block auto-framing and the post-meridian-flip re-centering use. Built on the §18.I plate-solver; needs a connected mount + a configured solver to run.
- **§18.I — Plate-solve a captured frame over REST.** A new endpoint (`POST /api/v1/platesolve/frames/{id}/solve`) takes a frame from your catalog, runs it through the plate-solver, and returns the frame's true sky coordinates, image rotation, and pixel scale. This makes the plate-solve service reachable from the client / scripts — capture a frame, ask the daemon to solve it. (Requires a solver backend such as ASTAP installed and configured.)
- **§18.I — Plate-solving service (image → astrometric solution).** The daemon can now plate-solve a captured frame: given an image (and optionally the mount's approximate position to seed a fast non-blind solve), it runs the solver configured in your profile and returns the frame's true sky coordinates, orientation, and pixel scale. The solver backends (ASTAP and others) were already ported but weren't reachable from anywhere — this wires them up. ASTAP integration is purely via its command-line tool (no network API), so once the ASTAP binary + star database are installed and pointed to in settings, solving works with no further code. This is the foundation the auto-centering and meridian-flip re-centering build on.
- **§58 — Meridian flip trigger (decision logic).** A sequence can again decide *when* a German-equatorial mount needs to flip across the meridian: the re-ported trigger watches the mount's reported time-to-flip and, accounting for the configured pause-after-meridian / max-wait windows, the next exposure's length, and (when available) the reported side-of-pier, decides whether a flip is due — skipping cleanly when the mount is disconnected, parked, at home, or not tracking. The actual flip-and-recover workflow (slew, plate-solve re-center, re-focus, restart guiding) is the next follow-up; until it lands, a sequence that reaches a real flip stops loudly rather than silently continuing un-flipped (a safety choice — a skipped flip can swing the optics into the mount).
- **§59 — Autofocus curve fitting (Classic AF: parabolic, hyperbolic, trendlines).** The autofocus math foundation: given a set of `(focuser position, HFR)` samples from a focus sweep, the daemon fits the focus curve and reports the predicted best-focus position, the HFR it expects there, and a fit-quality R² score — plus honest flags for "this isn't actually a focus minimum" (a flat or inverted curve) and "the minimum is outside the range you sampled, re-centre and re-run." All three classic methods are available: a weighted parabola (samples with more detected stars count more), a hyperbola (the classic focus-V model), and trendlines (a straight line through each arm of the V, intersected — robust when the curve bottom is rounded or flat). A best-of selector keeps the parabola unless its fit quality drops below threshold, then falls back to the hyperbola. Pure managed math, identical across platforms, built on the §2105 HFR measurement. The live focuser sweep that feeds it is the next follow-up.
- **§2105 — Star detection + HFR on captured frames.** The image pipeline can now measure a frame's stars headlessly: a from-scratch detector estimates the sky background (median + MAD), thresholds at a sensitivity-driven level, groups bright pixels into stars, and reports the count plus the average Half-Flux-Radius (HFR) — the focus-quality metric that flows into the FITS `HFR`/`StarCount` keys. Sensitivity (Normal/High/Highest) trades faint-star recall for noise rejection, and an optional median pre-filter cleans noisy subs. Pure managed code (no native OpenCV dependency), so it runs identically across Linux/macOS/Windows. This unblocks autofocus (§59), which needs per-frame HFR.
- **§63.3 — Guider crash detection + auto-restart (guider-d).** When the link to the guider daemon drops mid-session (it crashed), the daemon now actively helps it recover instead of just going dark: it polls the `openastro-phd2` systemd unit's status on a backoff schedule (1→5→15→30→60→120 s), waits out systemd's own `Restart=on-failure`, nudges a `systemctl restart` if systemd has given up, and — if it never comes back — raises a Critical notification + a Red diagnostic so you know guiding is unavailable. On non-systemd hosts (e.g. a dev box) it degrades to a safe no-op. The systemctl I/O is isolated behind a seam and the decision tree is fully unit-tested.
- **§25.5 — Manual capture from the Imaging tab ("Take One").** The Imaging tab's "Take One" button now fires a real exposure on the connected camera and the center pane displays the resulting frame (zoom/pan) — previously both were placeholders. Take One posts the current exposure/gain/offset/bin to the daemon, waits for the background capture to land in the catalog, then renders the §65 stretched preview. Adds `CameraExposureApi` + `FramesApi` client services and a macOS dev-run guide (playbook §23.1) with a build-time `libcfitsio` copy so captures work out of the box on the dev box.
- **§65 — Color previews for one-shot-color (OSC) cameras.** Previews and thumbnails of frames from an OSC sensor now render in color instead of a gray Bayer mosaic. The capture path detects the sensor's color-filter array (ASCOM `SensorType`/`BayerOffsetX/Y`), stamps the resolved pattern as a FITS `BAYERPAT` header, and the §65 preview pipeline super-pixel debayers on the fly with per-channel stretch. The **stored FITS stays the raw, undebayered mosaic** so downstream stackers debayer it themselves — color happens only in the JPEG you look at. Monochrome frames are unaffected.
- **§63 — Sequences drive the real guider (guider-c).** The sequencer's guiding instructions (Start Guiding / Stop Guiding / Dither) now command the live PHD2 guider instead of no-op stubs: `GuiderService` also serves the sequencer's guider mediator, so a saved sequence can dither between frames and start/stop guiding against the connected openastro-guider daemon.
- **§63 — Real PHD2 guiding (guider-a).** The guider endpoint now drives a real openastro-phd2 instance instead of a placeholder: connect to PHD2 (host/port from your profile), observe its live state (stopped/calibrating/guiding/paused/star-lost) plus rolling guiding RMS (total/RA/Dec), and start/stop guiding or dither over REST. Built on the re-ported PHD2 JSON-RPC client.
- **§14e — Sequences capture for real (`TakeExposure`).** The capture instruction is back: saved sequences (including NINA imports and the shipped starter templates) now expose through the connected camera and land their frames in the catalog with correct frame types, filter names and per-target attribution — the same pipeline the manual capture endpoint uses, serialized so sequence and manual captures can't collide.
- **§14e — Real camera captures land in the frame catalog.** The camera endpoint now drives a connected Alpaca camera end-to-end: start an exposure over REST and the daemon exposes, downloads the image, writes a real FITS file into your configured save directory and registers it in the frame catalog — so the frame list, preview, thumbnail and download endpoints serve it immediately. Cooler control and exposure abort drive the live device too.
- **§14e — Real cross-epoch coordinate transforms on Linux/macOS.** The SOFA/NOVAS astrometry natives now build from the vendored C sources for non-Windows hosts (`scripts/build-astrometry-natives.sh`) and resolve automatically, so sequence slews to J2000 targets are precession-corrected for JNOW mounts instead of falling back to untransformed coordinates. CI exercises the real transform path on every run.
- **§38.7 — Starter sequence templates ship with the daemon.** Three ready-to-instantiate templates (`lrgb-dso`, `narrowband-shoo`, `comet`) are seeded into the profile's sequence-template library on first run: per-filter blocks with loop counts and filter changes wired to the live filter wheel, plus capture nodes that activate automatically once the camera capture path lands. User-edited copies are never overwritten; deleting one re-seeds the pristine copy on the next start.
- **§14e PR20 — Sequence instructions read your edited settings (profile source-of-truth).** The daemon's live NINA profile now hydrates from `profile.json` (the settings the client edits over REST) at startup and on every save — so executing instructions see your actual site coordinates, PHD2 host/dither/settle values, autofocus sweep parameters, save directory/filename template/format, plate-solve tuning and meridian-pause policy instead of compile-time defaults.
- **§14e PR19 — `SwitchFilter` sequence instruction drives the live filter wheel.** `FilterWheelService` now also serves the Sequencer's `IFilterWheelMediator` (one singleton backs both the REST surface and the mediator). On connect, the wheel's filter list is imported into the active profile (device names/offsets for new positions; existing entries with user-edited autofocus settings preserved), so sequences referencing filters by name/position resolve against real hardware; `ChangeFilter` rotates the wheel and blocks until the target slot is reported.
- **§14e PR18 — `SetSwitchValue` sequence instruction drives the live switch hub.** `SwitchService` now also serves the Sequencer's `ISwitchMediator` (one singleton backs both the REST surface and the mediator): `GetInfo()` exposes the hub's writable ports as `IWritableSwitch` wrappers with their real minimum/maximum/step (so sequence validation checks against actual hardware ranges), and `SetSwitchValue` writes the selected port on the connected Alpaca device through a cancellation-aware bounded call instead of a no-op stub.
- **§14e PR17 — Telescope sequence instructions drive the live mount.** `TelescopeService` now also serves the Sequencer's `ITelescopeMediator` (one singleton backs both the REST surface and the mediator), so `SetTracking`, `UnparkScope`, `ParkScope`, `FindHome` and `SlewScopeToRaDec` execute against the connected Alpaca mount instead of no-op stubs. Slews transform the sequence target into the mount's native coordinate system (J2000 vs JNOW) before the goto, and every long op blocks on a bounded wait for its terminal condition (settled-on-target, parked, unparked, at-home) so instruction success reflects what the mount actually did.
- **Phase 12h.5c — Wire `AlpacaDeviceRow` on 7 remaining equipment panels.** Focuser, Filter Wheel, Rotator, Flat Panel, Dome, Weather, Safety Monitor — all now have an interactive Alpaca chooser. Guider panel intentionally skipped because PHD2 is its own protocol (host/port/profile, not Alpaca). `AlpacaDeviceRow` gained an optional `hint` parameter so each panel can surface its per-device context: rotator/dome/weather/safety_monitor flagged "Optional", flat panel keeps its §10.6 "Alpaca exposes flat panels as CoverCalibrator" note, weather notes "feeds §35 safety policies", safety_monitor notes "recommended for unattended operation". 9 of 10 equipment panels now have full §52.2 chooser UX.
- **Phase 12h.5b — Lift `AlpacaDeviceRow` to shared widget + wire mount panel + polish.** `_AlpacaDeviceRow` from camera panel promoted to `lib/widgets/equipment/alpaca_device_row.dart` as a `ConsumerWidget` (reads selection directly from `alpacaSelectionProvider`, no `selectedName` parameter passing). Mount panel becomes the second consumer. Polish from #125 self-review: dropped PascalCase JSON fallbacks (daemon's System.Text.Json defaults camelCase per `Program.cs`); added `assert(false, ...)` on `_parseType` unknown-device-type fallback so future daemon additions surface in debug builds instead of silently misclassifying as camera.
- **Phase 12h.5a — Alpaca device chooser foundation + camera wiring.** First piece of §52.2. Wires against the **existing functional** Phase 6 `/api/v1/equipment/discover/{type}` daemon endpoint (real Alpaca UDP discovery). Components: `DiscoveredDevice` model (mirrors daemon DTO + `pathSegmentFor` mapping for §10.6 device-type conventions), `EquipmentDiscoveryApi` dio service, `AlpacaSelectionNotifier` in-memory selection, `showAlpacaChooserDialog` modal with name/URL/device# + re-scan + empty/error states. Camera panel rewired: read-only "Not selected" row replaced with interactive `_AlpacaDeviceRow` ("Not selected" + Choose…" button). Other 9 panels follow in 12h.5b-j.
- **Phase 12h.4 — §63 PHD2 settings state + full guider panel migration.** Eleventh panel migration. New `Phd2Settings` model + `phd2SettingsProvider` holds 10 PHD2-internal fields (host, port, profile, dither enabled/every-N/pixels, settle pixels/time/timeout, force-calibration-each-session). 10 settings registered + 4 Help entries on the non-obvious controls (dither amplitude, settle threshold, settle timeout, force-calibration tradeoff). Retired the placeholder `guider.dither_pixels` starter entry from 12h.3a — superseded by the proper `eq.guider.*` namespace. Panel converted from 12 read-only rows to fully editable. The §35 meridian-flip re-cal-guider toggle is now surfaced as a read-only ref (edit in Safety → Policies) to keep one source of truth. 7 new tests, total 157.
- **Phase 12h.3l — Visible search affordance in AppShell top bar.** Per §61.1 the WILMA top bar is supposed to carry a magnifying-glass icon on the right side as the always-visible companion to the ⌘K shortcut. Added the icon to `_TopEquipmentBar` with platform-correct tooltip (`⌘K` on macOS, `Ctrl+K` elsewhere). Opens the same `showCommandPalette` dialog as the keyboard shortcut. Equipment chips wrapped in `Expanded(SingleChildScrollView)` so the search icon sits stably on the right regardless of scrolling. The Settings shell's panel-header search button (#65 era) remains as §61.1's panel-scoped second affordance.
- **Phase 12h.3k — Plate Solve + Diagnostics Mode bulk-registered + help icons wired (final 12h.3 panel batch).** Tenth and final panel migration of the 12h.3 sequence. 11 Plate Solve fields registered (engine, path, index path, search radius, downsample, timeout, blind fallback, center after slew, sync, max iterations, convergence tolerance) + 6 Plate Solve help entries (engine tradeoffs, search radius strategy, downsample speed-vs-accuracy, blind fallback rationale, max iterations tuning, convergence-tolerance guidance). Also **fixed the pre-existing `diagnostics.mode` registry entry and help body** — both had stale enum values from 12h.3a starter ("Balanced"/"Aggressive") that don't match the actual `DiagnosticsMode` enum (`notifyOnly`/`pauseOnCritical`/`abortOnCritical`); updated to correct values + a richer help body that distinguishes diagnostics from §35 safety events. Plate Solve panel migrated: inline engine dropdown → `SettingsDropdownRow`; `AraColors` import dropped. 6 helpKey wirings on the plate-solve panel rows.
- **Phase 12h.3j — Autofocus bulk-registered + help icons wired.** Ninth panel migration. All 12 autofocus fields registered (method, steps, step_size, exposure_seconds, binning, af_filter, run_after_filter_change, trigger_temp_delta_c, trigger_hfr_drift_pct, every_n_hours, abort_sequence_on_af_failure, restore_position_on_failure). 6 Help entries on the genuinely non-obvious controls: autofocus method (V-curve vs brightest-star HFR vs FWHM), step count (sweet spot + when to deviate), step size (full CFZ calculation + tuning from observed curve shape), temperature-trigger threshold (focuser-tube thermal-expansion rationale), HFR-drift trigger, periodic backstop. Panel: inline method dropdown → `SettingsDropdownRow`; `AraColors` import dropped.
- **Phase 12h.3i — Safety Policies bulk-registered + help icons wired.** Eighth panel migration. All 12 safety-policy fields now registered (previous 3 starter entries from 12h.3a expanded; 9 new entries for meridian flip + altitude limit + guider lost). 6 new Help entries on the non-obvious controls: auto meridian flip (GEM-vs-fork rationale, what auto-flip actually does), meridian pause (mechanical settle time by mount type), altitude-limit action (skip/pause/abort tradeoff), guider lost action (PHD2 retry semantics), guider retry timeout (60s default + cross-link to §54 plate-solve-failed trigger). Cross-links between safety + notifications + site preferences are now meaningful. Fixed `resume_delay` default mismatch (registry had `5`, model defaults to `10`). 6 helpKey wirings on the existing editable safety_policies_panel rows.
- **Phase 12h.3h — Equipment auto-connect bulk-registered + help icons wired.** Seventh panel migration. 10 individual `eq.<type>.auto_connect` settings (camera/mount/focuser/filter wheel/rotator/guider/flat panel/dome/weather/safety monitor) so ⌘K "guider connect" / "dome boot" / "weather auto" each resolve to the right panel. Defaults split between connect-by-default (core imaging chain) and manual-connect (devices with hardware side effects on connect: guider starts PHD2, dome may actuate shutter, weather starts polling). 1 shared Help entry (`eq.auto_connect_on_boot`) wired on the toggle in all 10 panels — the body covers the per-device side-effect rationale so users understand which to leave off.
- **Phase 12h.3g — Filter Wheel labels bulk-registered + help icon wired.** Sixth panel migration. Single `eq.filterwheel.slot_labels` setting (complex type) covering the whole 8-slot list — one entry vs 8 individual slot settings since slots are uniform and the slot count is hardware-reported at connect. One Help entry explains where labels flow (`$$FILTER$$` tokens, FITS header, sequence blocks, §29.2 calibration indexing) + mono LRGB / narrowband / photometric naming conventions. Panel renders the ⓘ icon on Slot 1 only — asymmetric on purpose to avoid noise per §69.1.
- **Phase 12h.3f — Filenames bulk-registered + help icons wired.** Fifth panel migration. 2 settings (`date_separator` enum + `compress_darks_and_bias` toggle) and 2 help entries — both fields are non-obvious enough to warrant help. Date-separator help explains the `/`-as-subdirectory vs `_`/`-`-as-flat-filename tradeoff + the Windows-safety rationale for dash. Dark/bias compression help cites the typical 8-15x RICE ratio + the 5+ GB library size that makes it worth a dedicated toggle. Panel widgets: inline `DropdownButtonFormField<DateSeparator>` swapped for `SettingsDropdownRow`; unused `AraColors` import dropped.
- **Phase 12h.3e — Site preferences bulk-registered + help icons wired.** Fourth panel migration. All 10 site fields (name, lat/lon, elevation, time zone, custom-horizon toggle, default horizon altitude, Bortle class, typical seeing, twilight definition) registered in `settings/registry.dart`. 5 Help entries on the non-obvious controls: custom horizon polygon (§36.8 measurement workflow), default horizon altitude (rationale + suggested values), Bortle class (1-9 scale with site-type ranges + learnMoreUrl), typical seeing (FWHM ranges + how it feeds §50 quality scoring), twilight definition (civil/nautical/astronomical with use-case for each). Panel migrated: inline `DropdownButtonFormField<TwilightDefinition>` swapped for shared `SettingsDropdownRow`; unused `AraColors` import dropped.
- **Phase 12h.3d — Notifications bulk-registered + help icons wired.** Third panel migration. All 12 notifications fields (5 channels + 7 triggers) registered in `settings/registry.dart` so ⌘K finds them by intent ("pushover", "telegram", "alarm", "disk space", etc). 5 Help entries on the genuinely non-obvious controls: Pushover token (sign-up flow + learnMoreUrl), Telegram bot token (`@BotFather` setup + learnMoreUrl), critical-diagnostic semantics (§51 severity-level explainer), plate-solve-failed retry budget (×N comes from §35 guider-retry-timeout), and the disk-space-low ~10 GB threshold. Panel migrated: local `_TokenField` `StatefulWidget` retired (~60 LoC) in favor of the shared `EditableTextRow` from 12h.3a.
- **Phase 12h.3c — Storage bulk-registered + help icons wired.** Second panel migration. All 4 storage fields (save directory, file format, compression, filename template) now in `settings/registry.dart`. 3 Help entries on the non-obvious controls: file format (FITS/XISF/RICE/gzip tradeoffs with downstream-tool compatibility), compression (RICE vs gzip speed-and-ratio comparison), filename template (full token reference — `$$DATEMINUS12$$`, `$$DATETIME$$`, `$$IMAGETYPE$$`, `$$FILTER$$`, `$$EXPOSURETIME$$`, `$$TARGETNAME$$`). Save directory left tooltip-free per §69.1 (self-explanatory). Panel widgets switched to shared `SettingsDropdownRow` for both enum dropdowns; ~30 LoC of inline `Padding`/`Row` boilerplate removed.
- **Phase 12h.3b — Imaging Defaults bulk-registered + help icons wired.** First panel migration after 12h.3a foundation. All 8 imaging-defaults fields (exposure, gain, offset, bin, frame type, cooler target, cooler ramp, warmup) registered in `settings/registry.dart` with description + ≥4 keywords + path metadata, so ⌘K finds them by intent. 5 new `Help` entries on the non-obvious controls (gain, offset, bin, ramp rate, warmup) — covers the CMOS/CCD differences, the thermal-shock rationale, and the offset/black-level explanation. Panel widgets switched to shared `SettingsDropdownRow` + `SettingsSwitchRow`. Registry-gate CI passes.
- **Phase 12h.3a — Smart Settings Search + Help Registry foundation.** Rebuilds the §61 (smart settings search) + §69 (help tooltip registry) infrastructure after Augment's #107 was closed for scope overreach. Lands the foundation only — bulk-registration of all ~200 settings + per-panel migration is split into 12h.3b through 12h.3N follow-up PRs. This sub-PR includes:
  - **Data shapes.** `lib/settings/registry.dart` (`Setting` + `SettingType` + `settingsRegistry` list, 7 starter entries). `lib/help/registry.dart` (`Help` + `helpRegistry` map, 6 starter entries).
  - **Search engine.** `lib/state/settings/settings_search.dart` — per-panel keyword corpus + scored entries + fuzzy intent matching. Plus `lib/state/settings/settings_nav.dart` mapping the settings panel tree.
  - **Widgets.** `lib/widgets/command_palette.dart` (⌘K palette dialog wired through existing `app_shell.dart` shortcut) + `lib/widgets/help_icon.dart` (ⓘ tooltip + modal). `editable_field.dart` and `settings_row.dart` now accept an optional `helpKey: '...'` arg that renders the ⓘ icon when set; backwards-compatible (existing callers unchanged).
  - **§61.4 / §69.4 enforcement gates.** New `scripts/check-settings-registry.mjs` + `scripts/check-help-registry.mjs` — fail PRs whose registry entries have empty descriptions, missing keywords, or duplicate IDs. Wired into CI as a new `registry-gate` job. Pre-commit hook integration ships in a follow-up infra PR.
  - **PR template.** Rewrote `.github/pull_request_template.md` for ARA with §61.4 + §69.4 checkboxes (Layer 3 of the four-layer enforcement spec).
- `CHANGELOG.md` at repo root (Keep-a-Changelog format) with backfilled history through `phase-10.5-complete` + the going-forward `[Unreleased]` convention. Convention is reminded by `.claude/skills/port-driver/SKILL.md` step "Open the PR".
- **Phase 11 scaffold** — Flutter WILMA client at `client/openastroara_client/` (`org.openastro.openastroara`, platforms = macos/windows/linux per §18.G mobile-deferred-to-v0.1.0). Pinned Flutter 3.44.0 via `.flutter-version` + pubspec `environment.flutter`. Runtime deps: dio, web_socket_channel, multicast_dns, riverpod, flutter_riverpod, flutter_secure_storage, file_picker. First-run skeleton: `lib/models/server.dart` (AraServer), `lib/services/server_discovery_service.dart` (mDNS scan `_openastroara._tcp.local`), `lib/services/server_api.dart` (dio /api/v1/server/info handshake), `lib/state/server_state.dart` (Riverpod 3.x Notifier-based providers), `lib/screens/first_run_screen.dart` (discovery list + manual entry + handshake panel), `lib/main.dart` (Material 3 dark theme entry). `flutter analyze` clean.
- **Phase 12a — App shell + global infrastructure.** Material 3 dark theme using the §25.2 color tokens (`lib/theme/ara_colors.dart` + `lib/theme/ara_theme.dart`). `AppShell` with `NavigationRail` (5 tabs: Imaging / Framing / Sequencer / Sky Atlas / Options, all stubbed pointing at Phase 12c-12h follow-ups), top equipment bar with §25.3 device-type chips (CAM / FW / FOC / MOUNT / ROT / GUIDE / FLAT / SW / WX / SAFE / DOME — disconnected until Phase 12c wires the Alpaca chooser), bottom status bar with global `StatusIndicator` + bug-report `?` icon stub. `StatusIndicator` widget (§51 health-pill + §53 a11y semantics). `EquipmentChip` widget — the per-device visual primitive. Saved-server persistence via flutter_secure_storage (`SavedServerService` + `SavedServersNotifier` AsyncNotifier); the new `_RootRouter` in `main.dart` routes to FirstRunScreen if no servers saved or AppShell otherwise. FirstRunScreen got a "Save & continue" button — taps it after a successful handshake, writes to the saved-server store, the router auto-swaps to AppShell. Storage hardened against keyring failures so Linux without libsecret degrades to "no saved servers" instead of stranding the user on the error route.
- **Phase 12b — Profile wizard shell.** §37 18-screen / 7-stage wizard scaffold. `ProfileDraft` model (`lib/models/profile_draft.dart`) covers every wizard field — telescope/camera/filter wheel/focuser/mount/rotator/guider/plate-solve/autofocus/file-saving/imaging-defaults/safety/site/sky-data. `WizardController` (`lib/state/wizard_state.dart`) is a Riverpod auto-dispose Notifier with `next` / `back` / `skipCurrent` / `jumpTo` / `snapshot`; tracks skipped screens per §37.8 + emits a `copyWith()` rebuild on terminal-step skip. `ProfileWizard.steps` static catalog maps step number → stage label + screen title. `WizardShell` (`lib/screens/wizard/wizard_shell.dart`) renders progress bar + screen body + bottom nav (Back / Skip / Next / Save Profile) + Save & Exit action; falls back to `Navigator.pop` when no `onComplete` is wired so the wizard route always dismisses. 18 placeholder screens (`lib/screens/wizard/wizard_screens.dart`) — each shows the stage label + title + a one-paragraph description of what the real form will collect (sourced from §37.1-§37.7). Per-screen forms land one-by-one in Phase 12b follow-up PRs. AppShell's bottom status bar gets a "Run profile wizard" entry point that pushes the wizard as a full-screen route.
- **Phase 12c.1 — Imaging + Framing tab cores.** Imaging tab (§25.5.1) now has: `_ImagingHeader` with title + always-visible §51 `StatusIndicator` ("Diagnostics: not connected" until 12c.2 wires the diagnostics stream), `FrameViewer` (placeholder — real `Image.network(previewUrl)` + InteractiveViewer zoom/pan + plate-solve overlay lands in 12c.2), `HistogramStrip` (empty-bin gradient placeholder), `ExposureControlsPanel` (right-side: exposure-seconds + gain + offset + bin + frame type + Take One button + Live View toggle, all Riverpod-backed via `ExposureController` in `lib/state/imaging/exposure_state.dart`). Framing tab (§25.5.2) now has: `_TargetSearchBar` (text input + Search button — bundled catalog lookup lands in 12c.2, query state in `TargetSearchQueryNotifier`), `_SkyChartPreview` (placeholder), `_FramingParamsPanel` (rotation slider, mosaic cols/rows steppers, overlap-pct slider, Set-as-Target button stub — all Riverpod-backed via `FramingController` in `lib/state/imaging/framing_state.dart`).
- **Phase 12h.2-filter-labels — Editable filter wheel slot labels.** §37.4 + §46.2 filter wheel slot labels are now editable. New `FilterWheelLabels` model holds a `List<String>` of 8 labels (defaults L/R/G/B/Hα/OIII/SII/blank per the §46.2 reference wheel). `FilterWheelLabelsNotifier.setLabel(slot, label)` is 1-indexed (matches user-facing slot numbers); input is trimmed; out-of-range slots are no-ops; empty labels are allowed (blank an unused slot). `equipment_filter_wheel_panel.dart` renders the slots as `EditableTextRow` instances with an "Empty = unused" hint. Real hardware will report slot count on connect — `setSlotCount` lands in 12h.2b alongside daemon round-trip. 6 new tests, total 147.
- **Phase 12h.2-equipment-connect — Per-device-type auto-connect-on-boot.** §52.1 connection lifecycle: new `EquipmentConnectionSettings` model holds a `Map<EquipmentDeviceType, bool>` for the 10 device types (camera/mount/focuser/filterWheel/rotator/guider/flatPanel/dome/weather/safetyMonitor). Defaults: core imaging chain on (camera/mount/focuser/filterWheel/rotator/flatPanel/safetyMonitor), guider/dome/weather off (side-effect devices). `EquipmentConnectionNotifier.setAutoConnect(type, bool)` is the single setter; copy-with-map ensures a new instance each toggle. All 10 equipment panels become `ConsumerWidget`s reading the shared provider — the "Auto-connect on boot" row is now an editable `SettingsSwitchRow`. Other panel fields remain read-only stubs until 12h.2b daemon round-trip. 3 new tests, total 141.
- **Phase 12h.2-dropdown — Shared `SettingsDropdownRow` + `EditableNumberRow` reuse.** Followup on the PR #104 self-review note. New `SettingsDropdownRow<T>` in `editable_field.dart` factors out the enum-dropdown row layout. `safety_policies_panel` now uses it for the 3 action dropdowns + uses the shared `EditableNumberRow` for the 3 numeric fields, removing the local `_DropdownRow<T>` (39 LoC) and `_NumberRow` + `_NumberRowState` (87 LoC). Net safety_policies file drop: 126 LoC.
- **Phase 12h.2-switch — Shared `SettingsSwitchRow`.** Consolidates the 6 nearly-identical `_SwitchRow` copies that had drifted into `safety_policies_panel`, `safety_site_panel`, `session_filenames_panel`, `session_notifications_panel`, `imaging_autofocus_panel`, and `imaging_plate_solve_panel` into a single shared `SettingsSwitchRow` in `lib/widgets/settings/editable_field.dart`. The shared widget supports an optional 2-line `hint` (used by §35 safety policies and autofocus to surface playbook-section refs). Resolves the PORT_TODO entry from #99 self-review.
- **Phase 12h.2-trim — Whitespace-tolerant string setters.** CR followup on #102 promotion review: all 6 string setters across autofocus, plate-solve, site, and storage notifiers now `trim()` first and reject empty/whitespace-only input, so values like `'   '` no longer pass the previous `isEmpty` check. Saved values are also the trimmed form (no leading/trailing whitespace persisted). Tests updated to assert both the trim + the reject behavior. (Notifications setters already trim; behavior preserved.)
- **Phase 12h.2-diagnostics — Editable Diagnostics Mode picker.** §51 diagnostics mode picker now wires the 3-radio selection through `diagnosticsModeProvider` (Riverpod `Notifier<DiagnosticsMode>`). New `DiagnosticsMode` enum (`notifyOnly` / `pauseOnCritical` / `abortOnCritical`) with default `notifyOnly`. `_ModeOption` is now an `InkWell`-wrapped tap target (the visual-only radio dot becomes interactive). 2 new tests, total 138. Persistence via `/api/v1/profile/diagnostics-mode` deferred to 12h.2b.
- **Phase 12h.2-platesolve — Editable Plate Solve panel.** §37.10 Plate Solving panel is now fully editable. New `PlateSolveSettings` model + 11 fields: engine (`PlateSolveEngine` enum: ASTAP / astrometry.net / PlateSolve2), executable path or endpoint URL, index download path, search radius (°, clamped (0, 180]), downsample factor (clamped [1, 8] — 1 = no downsample, 8 = practical limit before star detection breaks), timeout (s, >0), blind-solve-fallback toggle, center-after-slew toggle, sync-to-coordinates toggle, max iterations (≥1), convergence tolerance (″, >0). Path setters reject empty strings. 8 new tests, total 136. Built on the shared `EditableTextRow`/`EditableNumberRow` from #95.
- **Phase 12h.2-autofocus — Editable Autofocus panel.** §37.11 Autofocus panel is now fully editable. New `AutofocusSettings` model + 12 fields: method (`AutofocusMethod` enum: HFR V-curve / brightest-star HFR / FWHM Gaussian fit), step count (clamped [3, 31] — V-curve needs ≥3 points to fit a parabola, 31 keeps runs short), step size, exposure seconds, binning, AF filter, run-after-filter-change toggle, temp-delta trigger °C, HFR-drift trigger %, every-N-hours trigger, abort-sequence-on-failure toggle, restore-position-on-failure toggle. Positive-value setters reject ≤0; HFR drift clamps to [0, 100]%. 8 new tests, total 128. Built on the shared `EditableTextRow`/`EditableNumberRow` from #95.
- **Phase 12h.2-filenames — Editable File Naming panel.** §29.2 File saving + naming panel made editable for the naming-specific options without duplicating storage's existing state. New `FilenamesSettings` model holds 2 fields: `dateSeparator` (DateSeparator enum: forwardSlash / underscore / dash) + `compressDarksAndBias` (bool, defaults on since dark/bias frames compress losslessly very well). Storage-owned fields (template / format / compression) are surfaced as read-only refs with a "Edit in Settings → Session → Storage" hint to keep one source of truth. The §29.2 token reference list (Date / Target / Frame / Sensor groups) stays. 3 new tests, total 120.
- **Phase 12h.2-site — Editable Site preferences panel.** §37.12 Site panel is now fully editable. New `SiteSettings` model + 10 validated setters: latitude [-90, 90]°, longitude [-180, 180]°, elevation [-500, 9000]m, Bortle class [1, 9], default horizon altitude [0, 90]°, typical seeing (0, 20]″; empty siteName / timeZone rejected. `TwilightDefinition` enum (civil/nautical/astronomical) dropdown. Custom-horizon switch hides the default-altitude row when on (custom horizon polygon import lands in 12h.2b with §36.8). 9 new tests, total 117. Built on the shared `EditableTextRow`/`EditableNumberRow` from #95.
- **Phase 12h.2-display-sync — Shared EditableTextRow/EditableNumberRow.** Extracts the round-1-CR fix from PR #94 into shared widgets at `lib/widgets/settings/editable_field.dart` so all three editable settings panels (Imaging Defaults, Storage, Safety Policies) handle rejected input the same way: setter rejects → `getCanonical` reads the unchanged state → controller text snaps back so the field never shows a value that didn't stick. `didUpdateWidget` syncs external state changes when the user isn't mid-edit. Imaging + Storage panels swap their local `_NumberField`/`_TextField` for the shared widgets; Safety Policies will pick up the shared widget in the next CR cleanup if needed (currently uses an in-panel copy with the same semantics).
- **Phase 12h.2-safety — Editable Safety Policies panel.** §35 Safety Policies panel is now fully editable. New `SafetyPolicies` model + 3 enums (`UnsafeAction`, `AltitudeLimitAction`, `GuiderLostAction`). Panel renders 4 dropdown actions + 3 numeric thresholds (resume delay, meridian pause, guider retry timeout) + 5 boolean toggles. Numeric setters reject negative values at the boundary; enum setters assign directly. 5 new tests, total 108.
- **Phase 12h.2-notifications — Editable Notifications panel.** §54 Notifications panel made fully editable. New `NotificationsSettings` model + `NotificationsSettingsNotifier` (5 channel toggles: in-app banner / OS desktop / sound alert / Pushover token / Telegram bot token; 7 trigger toggles: sequence-complete / sequence-paused / critical-diagnostic / safety-event / autofocus-failed / plate-solve-failed / disk-space-low). Token setters trim whitespace at the boundary. Panel renders 3 Switch channels + 2 token text fields (StatefulWidget controllers per PR #63) + 7 trigger switches with Save snackbar. 6 new tests, total 103.
- **Phase 12h.2-storage — Editable Storage panel.** §29 Storage panel is now fully editable: save directory + filename template (multiline `_TextField` with proper StatefulWidget controller disposal per PR #63 contract), `StorageFileFormat` dropdown (FITS / XISF / FITS+RICE / FITS+gzip), `StorageCompression` dropdown (Off / RICE / gzip). New `StorageSettings` model + `StorageSettingsNotifier` rejects empty strings on `setSaveDirectory` + `setFilenameTemplate` (downstream consumers can't get an empty path). 4 new tests, total 97. §29.1.3 SD-card wear banner stays at the top; free-space probe + §63 directory picker land in 12h.2b alongside daemon round-trip.
- **Phase 12h.2-imaging-b — Editable Imaging Defaults panel UI.** The Imaging Defaults panel (read-only stubs since Phase 12h.1) is now fully editable. Six `TextField` rows for exposure / gain / offset / bin / cooler target / ramp rate, a `DropdownButtonFormField` for default frame kind (Light/Dark/Bias/Flat), and a `Switch` for warm-up at session end. All wired to `imagingDefaultsProvider`. Per the round-1 CR contract from PR #63, the `_NumberField` widget is a `StatefulWidget` with `TextEditingController` + `FocusNode` allocated in `initState` and disposed in `dispose` — never built inline. Commit happens on focus-out and on submit so users can tab/click between fields without explicit enter. A Save button shows a snackbar; daemon round-trip via `/api/v1/profile/imaging-defaults` lands in 12h.2b.
- **Phase 12h.2-imaging — Imaging Defaults state.** New `ImagingDefaults` model + `ImagingDefaultsNotifier` (`lib/state/settings/imaging_defaults_state.dart`) holding the 8 §37.11 imaging-defaults fields: default exposure / gain / offset / bin / frame-kind + cooler target temp (°C) + ramp rate (°C/min) + warm-up at session end. Setters validate at the boundary matching the round-2 ExposureController pattern (no zero/negative exposure, no negative gain/offset, bin >= 1, cooler temp clamped to [-60, 30]°C, ramp 0..10°C/min). 8 new tests bring total to 93. The editable panel UI lands in Phase 12h.2-imaging-b — keeping the notifier + tests in their own focused PR so the form-field controller lifecycle doesn't get reviewed alongside business-logic changes.
- **Phase 12-tests-6 — saved_server_state coverage.** 3 more tests bringing total to 85. `saved_server_state_test.dart`: SavedServersNotifier.build returns persisted list, add appends to state on success, and **add keeps in-memory state even when the underlying service throws** — locks in the cleanup-1 contract (PR #72) that first-run isn't blocked by transient keyring/storage errors on Linux-without-libsecret. Uses a `_FakeSavedServerService` injected via `savedServerServiceProvider.overrideWithValue` so the test owns the throw vs success path.
- **Phase 12-tests-5 — framing + live-view + library-grouping coverage.** Adds 12 more Dart unit tests bringing the total to 82. `framing_state_test.dart` (5): FramingController defaults, setRotationDeg, mosaic params update independently, FramingParams.copyWith preserves untouched fields; TargetSearchQueryNotifier defaults + set. `live_view_state_test.dart` (3): LiveViewController defaults to false, toggle flips, set assigns. `library_grouping_test.dart` (3): LibraryGroupingNotifier defaults to bySession + cycles through all 3, librarySessionsProvider returns 7 fully-populated sessions.
- **Phase 12 CR cleanup round 6 — `package_info_plus` for app version.** Round-1 review of promotion PR #84 caught the hardcoded `_kAppVersion = '0.0.1-ara.0'` in help_dialog.dart — pubspec is the single source of truth at `0.0.1+1`, so the two could drift. Replaced with `package_info_plus ^8.0.2` (newly added dep) read via a `_appVersionProvider` FutureProvider; the diagnostics-copy path also calls `PackageInfo.fromPlatform()` so the clipboard payload always has the freshest version.
- **Phase 12-tests-4 — stats + sky atlas state coverage.** Adds 14 more Dart unit tests bringing the total to 70. `stats_state_test.dart` (9): `statsOverviewProvider` reports 7 sessions / 7 targets / 7 nights / 60 frames over the demo data, total integration is non-zero, avg HFR is in 1.0..3.0 (sanity range); `targetRollupsProvider` returns one rollup per target sorted by integration descending; `bestFramesProvider` returns at most 10 Light frames ranked by HFR ascending. `sky_atlas_state_test.dart` (5): mode notifier defaults to catalogView + toggles, search notifier defaults to empty + updates, `skyImageryAvailableProvider` returns the Phase 12e.1 stub `false`.
- **Phase 12i — §54 Help / Report-a-bug dialog.** AppShell's bottom-status-bar Help button (disabled since Phase 12a per CR's cleanup-4 ask) is now wired to a real dialog. The dialog shows: app version (read at runtime from `package_info_plus` per cleanup-6), active saved server, saved-server count, and links to the GitHub Issues + Wiki. A "Copy diagnostics" button puts a Markdown-formatted issue template on the clipboard with the build info pre-filled + sections for Steps-to-reproduce / Expected / Actual. Clipboard uses the round-2 async + try/catch + context.mounted pattern from PR #70's CSV export; PlatformException failures log via `dart:developer` and show a "Failed to copy" snackbar. Lives at `lib/widgets/help_dialog.dart`.
- **Phase 12 CR cleanup round 5.** 4 findings on promotion PR #80. **1 Major**: `LibrarySelectionNotifier.toggle` now publishes via `Set<String>.unmodifiable(next)` so external consumers can't mutate the selection in place and bypass the notifier (would cause stale UI drift). Added `library_selection_test` case locking in the unmodifiable contract — total 56 tests now. **3 Minor**: `bulk_action_bar.dart` wrapped in `SafeArea(top: false)` so the bottom bar doesn't underlap system gesture/navigation insets; CHANGELOG fixed `LibrarySelectionActiveProvider` → `librarySelectionActiveProvider` casing; PORT_PROGRESS.md "Current" updated to reflect promotion #80 in flight.
- **Phase 12-tests-3 — SequenceController coverage.** Adds 14 tests on top of Phase 12-tests-2 (total 55) for the most complex notifier in the client. `sequence_state_test.dart` covers: demo seed sanity, `findNode` for root + deep child + miss-returns-null, `load` swaps root + clears selection, `addChild` refuses instruction parents (12h.2 single-root invariant), `addChild` refuses `SequenceNodeKind.root` (12-cr-cleanup-2 contract), `addChild` appends + auto-selects, `addSiblingAfter` inserts at idx+1 + selects, `addSiblingAfter` refuses root, `moveSelectedUp` swaps with previous sibling, no-op on first sibling, `moveSelectedDown` swaps with next, `deleteSelected` removes + clears selection, no-op on root, `moveSelected` with null selection no-op.
- **Phase 12-tests-2 — wizard + exposure + tab-index notifier coverage.** Adds 18 more Dart unit tests on top of the 23 from Phase 12-tests, bringing the total to 41. `wizard_state_test.dart` (8) — locks in step clamping (next never exceeds totalSteps, back never < 1, jumpTo `clamp` correctness over [-5, totalSteps+100], 7]), skipCurrent advances + tracks skippedScreens, skipCurrent on the terminal step still emits a new state (so the §37 banner re-renders per CR finding on PR #56), ProfileWizard catalog covers all 18 steps. `exposure_state_test.dart` (7) — round-2 validation contract: setExposure rejects zero/negative, setGain/setOffset reject negative, setBin rejects < 1, setFilterSlot rejects empty, setFrameKind accepts any enum. `app_shell_state_test.dart` (3) — SelectedTabIndexNotifier defaults to 0, accepts 0..4, rejects negative + ≥5.
- **Phase 12-tests — initial unit tests + CI integration.** 23 Dart unit tests across 3 files seed test coverage for the most-critical state + model invariants — replaces "just `flutter analyze`" with executable verification. `library_selection_test.dart` (6 tests): notifier starts empty, toggle adds-then-removes, multi-id accumulates, clear drops everything (and is identity-no-op when already empty), contains/membership. `settings_search_test.dart` (9 tests): empty + non-positive limit guards, exact-label rank, "dither"→Guider, "park"→Mount + Safety Policies, group-label match, no-match, limit cap, registry covers all 22 panels. `sequence_node_test.dart` (8 tests): default + nested params/children are unmodifiable (the round-3 deep-freeze contract), deep equality + hashCode over nested lists, copyWith preserves untouched fields + sentinel clears nullable instructionType, isContainer matches kind. New `client-test` CI job (`.github/workflows/ci.yml`) runs `flutter pub get && flutter analyze && flutter test` against the pinned `.flutter-version`; Flutter setup uses `subosito/flutter-action` pinned to commit SHA per zizmor immutable-ref policy.
- **Phase 12f.3a — Library multi-select skeleton.** §40.8 selection mode for the Image Library. New `librarySelectionProvider` Notifier (Set\<String\> of frame ids) backs a `librarySelectionActiveProvider` derived bool. `FrameThumbnail` gains `selected` / `selectionMode` / `onLongPress` props — long-press any thumbnail enters selection mode and adds that frame; subsequent taps toggle selection (and tap-to-open-viewer is short-circuited while in selection mode). New `LibraryBulkActionBar` slides up from the bottom whenever the selection set is non-empty, showing a Material-elevation card with: "N selected" + Close (clears selection), and 5 disabled-for-now action buttons — Rate, Tag, Move to session, Export, Delete (last in `AraColors.accentBusy` to signal destructive). The action handlers wire up in Phase 12f.3b once `/api/v1/frames/bulk` lands.
- **Phase 12 CR cleanup round 4.** Five real findings from promotion PR #71 round-4 review. **3 Major**: `app_shell.dart` bottom status bar height bumped 32px → 40px so the TextButton.icon launchers don't clip at 110% text-scale; `image_library_screen.dart` AppBar `PreferredSize` bumped 48 → 64 to fit the dropdown's intrinsic 48px + 16px vertical padding; `diagnostics_mode_panel.dart` mode options wrapped in `Semantics(label, selected, button)` so screen readers announce the active mode (the Icon-based indicator alone is visual-only). **2 Minor**: `main.dart` error screen no longer interpolates raw exception text; uses generic message and logs internal details via `dart:developer`; `image_library_screen.dart` session header texts wrapped in `Expanded`/`Flexible` with `maxLines: 1` + `TextOverflow.ellipsis` so long target/site names don't blow out the row. The 2 round-4 "Criticals" about a missing `lib/settings/registry.dart` have CR hallucinating a file that was never part of the design — our settings registry lives at `lib/state/settings/settings_nav.dart` (settingsTree) and `lib/state/settings/settings_search.dart` (per-panel keyword corpus) per Phase 12h.1; replied on those threads.
- **Phase 12 CR cleanup round 3 — deep-freeze `SequenceNode.params`.** Round-3 CR review of promotion PR #71 caught that the round-2 switch to `DeepCollectionEquality` made comparison deep but the constructor only shallow-froze the top-level map (`Map.unmodifiable(params)`). Nested `List`/`Map` values could still be mutated through the wrapper, which would change `==`/`hashCode` after construction. New `_freezeParams` + `_freezeDeep` helpers recurse through Maps and Lists wrapping each with the unmodifiable variant.
- **Phase 12 CR cleanup round 2.** Round-2 promotion-PR #71 review surfaced 8 new findings (not duplicates of round-1; CR's expanded scan caught additional issues). **5 Major**: `SequenceNode` equality/hashCode used `mapEquals`/`Object.hashAllUnordered` which is shallow on nested params (the demo `ForEachFilter` loop's `filters: ['L','R','G','B']` list would make structurally-identical nodes compare unequal); now uses `DeepCollectionEquality` from `package:collection ^1.19.1` (newly added). `SelectedTabIndexNotifier.select` clamps to 0..4. `ExposureController` setters reject invalid values (negative duration, gain/offset, non-positive bin, empty filter slot). `CalendarHeatmap` aggregates only dates within the 49-day window so out-of-window sessions don't wash out cell contrast. `ImageLibraryScreen` header chips wrapped in `SingleChildScrollView` for narrow-window responsiveness (same pattern as sequencer_toolbar in round 1). **3 Minor**: `AppShell` Help button `onPressed: null` until §54 dialog lands. `FrameThumbnail` rating clamped to 0..5. `DataManagerModal` MPC asteroid catalog size `'placeholder'` → `'TBD'`.
- **Phase 12 CR cleanup.** Batch-fixes 13 of the 14 CR findings raised on promotion PR #71 (the 14th — adding a filter-slot control to ExposureControlsPanel — is deferred to PORT_TODO.md as a Phase 12c follow-up feature). **3 Critical**: `clamp(...)` returns `num`, not the receiver's type — `.toInt()` added to `wizard_state.dart` jumpTo, `.toDouble()` added to `histogram_strip.dart` alpha + `frame_quality_chart.dart` yMax. **4 Major**: `wizard_shell.dart` Save & Exit now always dismisses the route regardless of whether `onComplete` was wired; `saved_server_state.dart` `add` keeps in-memory state updated on persist failure so first-run isn't blocked by transient keyring errors; `sequence_state.dart` `addChild` + `addSiblingAfter` refuse `SequenceNodeKind.root` to preserve the single-root invariant; `calendar_heatmap.dart` anchor uses real `DateTime.now()` (not a hardcoded 2026-05-27). **6 Minor**: framing_tab `Set as Target` button disabled until 12c.2 wires it; imaging_tab status pill tap handler passes `null` instead of an empty closure; equipment_chip semantics report button affordance for either `onTap` *or* `onLongPress`; sequencer_toolbar buttons wrapped in `SingleChildScrollView` to prevent narrow-window overflow; guiding_rms_chart x-axis title corrected from "session #" → "Session date" to match the M/D tick labels; PORT_PROGRESS.md updated for this PR.
- **Phase 12g.3 — Stats CSV export.** Wires the previously-disabled "Export CSV" button on the Stats dashboard into a `PopupMenuButton` offering two flavors: **Sessions summary** (one row per session — date / target / site / frame count / integration minutes / avg HFR / guiding RMS RA-Dec) and **Per-frame details** (one row per frame — 17 columns covering the full §40.5 metadata grid). RFC-4180 quoting in `lib/services/stats_csv_export.dart` handles commas, quotes, and newlines in target/site names. Selecting a flavor copies the CSV to the clipboard via `flutter/services` `Clipboard.setData` and shows a snackbar with the row count. Real file-save dialog (file_picker `saveFile` API differs per platform) lands in 12g.4.
- **Phase 12d.5 — Sequencer Add Child / Add Sibling actions.** Two new icon-button menus in the InstructionEditor header: Add Child (`+`) is enabled for container nodes only; Add Sibling (`↳`) is enabled for any non-root node. Each opens a `PopupMenu` of 13 ready-made specs — 4 containers (sequential / parallel / conditional / loop), 1 target, 8 common instructions (SlewToTarget, AutoFocus, TakeManyExposures, SwitchFilter, WaitForAltitude, WaitForTime, DitherBetweenFrames, ParkMount). Backing `SequenceController` methods (`addChild`, `addSiblingAfter`) walk the immutable tree and rebuild via copyWith, stamp new nodes with a `node-{epochMs}-{counter}` id, and select the new node so the user can edit it immediately. Editable params + drag-and-drop reorder land in 12d.6.
- **Phase 12d.4 — Sequencer node reorder + delete.** Three new icon-buttons in the InstructionEditor (Move up, Move down, Delete) drive new `SequenceController` methods (`moveSelectedUp` / `moveSelectedDown` / `deleteSelected`). The internal `_withParentModified` helper recursively finds the selected node's parent and rebuilds the path back to the root with `copyWith` so the immutable tree's invariants stay clean. Buttons are disabled when the root is selected; Delete also clears the selection (since the id is dangling). Drag-and-drop reorder + Add Child / Add Sibling actions land in Phase 12d.5.
- **Phase 12d.3 — Sequencer conditional/loop UI.** The §38.5/§38.6 `loopContainer` + `conditionalContainer` kinds (added to the `SequenceNode` enum in 12d.1 but not previously demoed or rendered specially) now have visible UI. Demo sequence gains a `ForEachFilter` loop under M42 (iterates over L/R/G/B with `$$ITER$$` substitution) and an `IfCondition` conditional under NGC 7000 (skip-if-unsafe per §35 with explicit `elseBranch: 'skipTarget'`). `InstructionEditor` renders a blue condition banner showing the boolean expression + an "Else branch" section explaining what happens when false (skipTarget / skipArea / abortSequence / continue), and an amber loop banner showing iteration values + label template. Tree icons (`alt_route` for conditionals, `loop` for loops) were already wired in 12d.1 — Phase 12d.3 just gives them content to render.
- **Phase 12g.2 — Real fl_chart visualizations.** Replaces the four Phase 12g.1 placeholder cards in the §50 Stats dashboard with real charts via `fl_chart ^1.1.1` (added as a runtime dep). **Focus & Temperature** — `ScatterChart` plotting (sensor temp, focus position) per captured frame across all sessions; slope reveals the §37.4 temp-comp coefficient. **Guiding RMS Trends** — `LineChart` with one line per axis (RA blue, Dec amber) over chronological session ordering; sessions without guider data are skipped. **Frame Quality** — `BarChart` of per-session avg HFR, color-banded (blue ≤ 1.70, amber ≤ 2.00, red > 2.00). **Calendar Heatmap** — GitHub-style 49-cell tile grid (fl_chart has no native heatmap) over the rolling window ending at the demo "today"; cell shade scales with that night's integration minutes, with a Less→More legend. Shared `ChartCard` wrapper keeps title/subtitle/border treatment consistent. Demo library expanded from 2 sessions to 7 (spanning ~6 weeks) so the charts have enough data to be meaningful — added `CaptureSession.guidingRmsRa` / `guidingRmsDec` nullable fields populated for each demo session. Phase 12g.3 wires real `/api/v1/stats/*` data + per-target detail + CSV export.
- **Phase 12h.3 — §61 ⌘K smart search palette.** Global ⌘K / Ctrl+K shortcut anywhere in AppShell opens a centered command palette (`lib/widgets/command_palette.dart`). The Search settings (⌘K) button in the Settings shell header now also opens it. Indexes all 22 panels from the `settingsTree` registry built in 12h.1 + a 22-entry per-panel keyword corpus (`lib/state/settings/settings_search.dart`) so users can find panels by intent (e.g. "dither" → Guider, "cooler" → Camera + Imaging Defaults, "park" → Mount + Safety Policies). Scoring: exact label match (1000) > prefix (500) > contains (300) > group match (250/200) > keyword exact (180) > keyword prefix (120) > keyword contains (80). Top 20 results, ↑↓ to navigate, Enter to jump, Esc to close. Activating a result selects the Options tab + sets `selectedSettingsPanelProvider` via the new `selectedTabIndexProvider` (lifted from `_AppShellState` local state so external triggers can drive tab selection).
- **Phase 12h.2 — Remaining settings panels (read-only stubs).** Brings the §25.5.5 settings tree from 3 rendered panels up to **22** by adding 18 new read-only panel stubs (1 helper widget + 18 panel files). Equipment cluster (10): Camera (§52 connection + sensor + cooling), Mount (slew/track/meridian/limits), Focuser (movement + AF triggers), Filter Wheel (8 slots + per-filter focus offsets), Rotator, Guider (PHD2 connection + dithering + calibration), Flat panel (CoverCalibrator wiring per §10.6), Dome (slaving), Weather (thresholds + polling), Safety Monitor. Imaging extras (2): Autofocus (§37.11 algorithm + triggers + safety), Plate Solving (§37.10 solver + slew/sync). Session (2): File saving + naming (§29.2 template + 4 token groups), Notifications (§54 channels + triggers). Safety (2): Policies (§35 weather/meridian/altitude/guider/session-end), Site (§37.12 location + horizon + conditions). Sky Atlas (1): Sky Data — launches existing §36.2 DataManagerModal. Profile (2): Active profile (4 metadata fields + 5 action buttons), Run wizard again (launches the §37 18-screen WizardShell from Phase 12b). All 22 panels share a new `SettingsRow` / `SettingsSectionHeader` widget pair (`lib/widgets/settings/settings_row.dart`) so every label/value pair has consistent typography. Editable forms + persistence to `/api/v1/profile/*` land in Phase 12h.2b/c; §61 ⌘K smart search lands in 12h.3.
- **Phase 12h.1 — Settings shell + tree nav.** §25.5.5 Options tab now opens a real `SettingsShell` (left-side 6-group tree: Equipment / Imaging / Session / Safety / Sky Atlas / Profile, right-side selected panel). Tree backed by `settingsTree` const (`lib/state/settings/settings_nav.dart`) — single source of truth for every settable option in the app, which Phase 12h.3's §61 ⌘K smart-search will index. `selectedSettingsPanelProvider` Notifier owns the active panel id (defaults to `img.defaults`). Three panels are fully rendered: **Imaging Defaults** (`img.defaults`) — read-only stub for default exposure/gain/offset/bin/frame-type + cooling target/ramp/warmup; **Storage** (`session.storage`) — §29 form stub with the §29.1.3 SD-card-wear warning banner + save dir / free space / format / compression / filename template rows; **Diagnostics Mode** (`safety.diagnostics`) — §51 mode picker (notify only / pause on critical / abort on critical) with per-mode descriptions. All other panels show a generic placeholder that confirms the routing works and notes the form lands in Phase 12h.2. Panel header has a disabled "Search settings (⌘K)" button that wires up in 12h.3. Editable forms + persistence to the active profile JSON via `/api/v1/profile/*` endpoints land in Phase 12h.2.
- **Phase 12g.1 — Stats dashboard skeleton.** Full-screen `StatsDashboardScreen` (§50) reachable from AppShell's bottom status bar. **Overview tiles** — 6 stats computed live from `librarySessionsProvider`: Nights, Targets, Sessions, Frames, Total Integration (`Xh Ym`), Avg HFR. **Targets section** — `TargetRollup` cards listing per-target session count + frame count + integration time + avg HFR, sorted by integration descending. **Best Frames** — top 10 lights by lowest HFR, ranked with avatar number + filename + HFR + star count + filter + rating stars. **Visualizations section** — 4 placeholder cards (Focus & Temperature scatter, Guiding RMS trends, Frame Quality composite, Calendar heatmap) — real `fl_chart` renders land in Phase 12g.2. CSV export button (disabled until 12g.2). Riverpod providers: `statsOverviewProvider`, `targetRollupsProvider`, `bestFramesProvider` all derive from the library state.
- **Phase 12f.1 — Image Library skeleton.** Full-screen `ImageLibraryScreen` (§40) — sessions grouped By Session (toggle to By Target / By Date in 12f.2), each session card shows date + target + site + total integration + filter counts + thumbnail strip + Capture Matching Flats / Resume Target buttons (stubs until 12f.2). `FrameThumbnail` widget renders the filter label + rating stars over a placeholder square (12f.2 swaps in `Image.network`). `FrameViewerScreen` (§40.5) opens on thumbnail tap — shows filename + 5-star rating + preview placeholder + 12-field metadata panel (exposure/gain/offset/filter/bin/HFR/stars/median ADU/background/sensor temp/focus steps/captured time) + 6-button action bar (Rate/Tag/Open in App/Show in Folder/Download FITS/Delete, all disabled in 12f.1). `CaptureSession` + `CapturedFrame` data models with `totalIntegration` + `framesByFilter` getters. Demo data seeds two sessions (M42 with 6 L frames, NGC 6188 with 3×3 Hα/OIII/SII frames). AppShell bottom status bar gets an "Image Library" launcher between "Run profile wizard" and the help icon. Real backend (`/api/v1/sessions` + `/api/v1/frames`), stretch picker (§65), auto-rating + HFR drift display (§40.7), bulk operations (§40.8), backup UI (§43/§44) land in Phase 12f.2/.3.
- **Phase 12e.1 — Sky Atlas tab + Data Manager modal scaffold.** Sky Atlas tab body (§25.5.4 + §36) with `SegmentedButton` mode toggle (Catalog View / Tonight's Sky), universal search bar (`SkyAtlasSearchNotifier`), Data Manager launcher, and an Aladin placeholder that responds to mode + search query (real `webview_flutter` Aladin Lite embed in 12e.2). `DataManagerModal` is a full-screen 4-tab dialog (§36.2): Sky Imagery (21 surveys grouped by wavelength), Star Catalogs (Tycho-2 / GAIA DR3 / UCAC4 / HD / HIP / Bayer-Flamsteed), Thumbnails (Famous Targets Pack + per-target placeholder), Solar System (DE440 ephemerides + MPC asteroid v0.1.0 deferral note). All download buttons disabled until 12e.2 wires `/api/v1/data-manager/*`. `SkyDataMissingBanner` (§36.13) — amber strip across the top when no HiPS survey downloaded yet; "Open Data Manager" CTA jumps to §36.2 inline. Riverpod state: `skyAtlasModeProvider` + `skyAtlasSearchProvider` + `skyImageryAvailableProvider`.
- **Phase 12d.1 — Sequencer skeleton.** Tree-based sequence editor per §25.5.3 + §38: `SequenceNode` data model in `lib/models/sequence/sequence_node.dart` (root/area/target/4×container/instruction kinds, params map, recursive children list, value-equality + copyWith). `SequenceController` Notifier (`lib/state/sequencer/sequence_state.dart`) seeds an in-memory demo sequence (Tonight area → M42 + NGC 7000 targets → Slew/AutoFocus/Capture/Wait instructions) so the tree renders on first open. `SelectedNodeIdNotifier` tracks the selected node id; `findNode(root, id)` walks the tree. `SequencerTab` body has: `SequencerToolbar` (New / Load / Save / Validate / Run / Pause / Abort, all disabled until 12d.2 wires the `/api/v1/sequences` endpoints), `SequenceTree` (flat indented ListView with kind-icon + display name + instruction-type label, click-to-select with highlight), `InstructionEditor` (right pane showing selected node's kind + instructionType + params table + child summary; read-only in 12d.1, editable in 12d.2). NINA `.json` import flow + conditional/loop UI + template instantiation + Run wiring all land in Phase 12d.2/12d.3.
- **Phase 12c.2 — Live View Notifier + Diagnostic Panel.** Live View toggle lifted out of `_ImagingTabState` widget state into `liveViewControllerProvider` (`lib/state/imaging/live_view_state.dart`) so cross-component observation works (bottom status bar, sequence-runner pause, etc.). §51 always-visible Diagnostic Panel (`lib/widgets/imaging/diagnostic_panel.dart`) renders below the histogram — collapsed by default, expanding shows recent events with per-event status dot + source + timestamp. `diagnosticsStateProvider` is the data source — stubbed in 12c.2 as "Diagnostics: not connected"; real `/api/v1/ws` event stream (`diagnostics.*` events per §60.9) wires up in Phase 12c.3 alongside Take One + frame-fetch.

### Fixed
- **Plate solving — clearer AstrometryNet fallback.** A profile still configured for the (removed) AstrometryNet cloud solver — on either the primary or the blind solver — was silently falling back to ASTAP. The daemon now substitutes ASTAP explicitly and logs it, so the solver actually used isn't a surprise. ARA ships local solvers only (§18.I); ASTAP is the intended backend.

---

## Pre-release history (backfilled 2026-05-26)

The port started by forking `nighttime-imaging/NINA` and demolishing the WPF
client to leave a `.NET 10` daemon + Flutter client architecture (see
`design/PORT_PLAYBOOK.md` for the full plan). The work below is grouped by
phase boundary; each phase ends with a `phase-N-complete` git tag.

### [phase-10.5-complete] - 2026-05-26

#### Added
- `packaging/debian/` overlay tree for the arm64 `.deb` package — `DEBIAN/control` (Recommends alpaca-bridge + openastro-phd2), `DEBIAN/postinst` (creates `openastroara` user, joins dialout/video/plugdev groups, sets `CAP_SYS_TIME` on the binary, refreshes `systemd-tmpfiles`, validates the sudoers drop-in, enables + starts the service), `DEBIAN/prerm` (graceful stop), `DEBIAN/postrm` (cleanup on purge), hardened `openastroara-server.service` per playbook §13 (NoNewPrivileges + ProtectSystem=strict + ProtectHome + PrivateTmp + RestrictAddressFamilies + CapabilityBoundingSet=CAP_SYS_TIME + SystemCallFilter=@system-service), `sudoers.d/openastroara` (passwordless sudo limited to update.sh + configure-storage.sh), `logrotate.d/openastroara` (daily rotation w/ copytruncate), `tmpfiles.d/openastroara.conf` (creates `/var/run/openastroara` on boot), `etc/openastroara/server.env.example`. (PR #49)
- `packaging/build-deb.sh` — assembles the `.deb` from a `dotnet publish` output + version string. Validates `visudo -cf` and runs `dpkg-deb --build --root-owner-group`. (PR #49)
- CI: `server-build` job now builds the `.deb` on every push and uploads it as an artifact (`openastroara-server_<version>_arm64.deb`, 30-day retention). (PR #49, PR #51)

#### Changed
- systemd unit `PrivateDevices=false` rationale moved off the inline trailing comment to a dedicated comment line — systemd-analyze doesn't accept inline comments on key=value lines. (PR #51)
- CI `.deb` version detection tightened to `git describe --tags --exact-match --match 'v*'` so non-release tags like `phase-10.5-complete` don't get used as Debian Version fields. (PR #51)

### [phase-10-complete] - 2026-05-26

#### Added
- Repo-root `Dockerfile` for the arm64 daemon (`mcr.microsoft.com/dotnet/runtime-deps:10.0-noble-chiseled-arm64v8` base; `EXPOSE 5555` to match the daemon's actual default port; `USER 1000` per §13 hardening). (PR #46)
- CI `server-build` job: `dotnet build` + `dotnet publish OpenAstroAra.Server -c Release -r linux-arm64 --self-contained -p:PublishAot=false` + ELF-arch verification + arm64 Docker buildx via QEMU. Implements the long-promised Phase-0.5p + Phase-4 CI growth. (PR #46)
- `.claude/skills/port-driver/SKILL.md` — autonomous port-driver Claude Code skill that drives sub-PRs under `/loop /port-driver` per design/COMMIT-PR-RULES.md (orient → branch → CR poll/fix → merge → advance phase). Includes the 2026-05-26 `/review` fallback policy for CR rate-limit. (PR #44, clarifications in PR #48)

#### Changed
- `OpenAstroAra.Core/Utility/Notification/Notification.cs` — warning/error variants now route to `Logger.Warning`/`Logger.Error` (with `[CallerXxx]` attribute propagation so the original call site is preserved) instead of dropping operational issues silently. Info/Success stay no-op. (PR #43)
- `OpenAstroAra.Core/MyMessageBox/MyMessageBox.cs` `Show(...)` — affirmative defaults (Yes/OK) map to safe non-affirmative results (No/Cancel) so `SequenceHasChanged.AskHasChanged` no longer silently auto-detaches. Real user choice replaces this when the §35/§60.9 modal-event flow lands. (PR #43)
- `OpenAstroAra.Core.csproj` — added `System.Management 10.0.0` for WMI usage in `Logger.cs` + `SerialPortProvider.cs`. (PR #43)
- `OpenAstroAra.Astrometry/DatabaseInteraction.cs` — reduced to `GetUT1_UTC` stub (honors `CancellationToken`) + `GetDisplayAlias` (full Levenshtein preserved). All other EF6-backed methods removed because their return types referenced deleted schema. (PR #43)

#### Removed
- `OpenAstroAra.Equipment/Equipment/MyCamera/EDCamera.cs` (Canon EDSDK vendor impl, ~1162 lines). (PR #43)
- `OpenAstroAra.Equipment/Equipment/MyGuider/MGENGuider.cs` (MGEN guider, ~596 lines). (PR #43)
- `OpenAstroAra.Equipment/Utility/ASCOMInteraction.cs` (ASCOM COM enumeration factory). (PR #43)
- `OpenAstroAra.Equipment/Equipment/MyFlatDevice/{AllProSpikeAFlat,AlnitakFlatDevice,ArteskyFlatBox,PegasusAstroFlatMaster}.cs` (vendor flat-device impls). (PR #43)
- `OpenAstroAra.Test/FlatDevice/{AlnitakFlatDeviceConnectTest,AlnitakFlatDeviceTest,PegasusAstroFlatmasterTest}.cs` (orphaned tests). (PR #43)

#### Fixed
- Phase 6 ASCOM Alpaca SDK signature bug in `AlpacaEquipmentProvider.cs`: `deviceType:` → `deviceTypes:` + added missing `logger: null` param. Built-but-broken since PR #38 — CI was sanity-only so the build error never surfaced. (PR #43)

### [phase-9-complete] - 2026-05-26

#### Added
- `OpenAstroAra.Server` Phase 9 endpoint surface — log/state + WebSocket + notifications + stats + system scaffold across ~141 endpoint registrations in 11 endpoint files. 75+ WS event tokens catalogued. `GET /api/v1/ws/catalog` returns the live catalog dump. (PR #41)

### [phase-8-complete] - 2026-05-26

#### Added
- `OpenAstroAra.Server` Phase 8 endpoint surface — image + session + backup + diagnostics scaffold. ~15 image DTOs (incl. composite quality score per §50.10, HFR analysis time series, backup-claim DTO), 4 service interfaces, 16 image endpoints, 3 diagnostics endpoints. (PR #40)

### [phase-7-complete] - 2026-05-26

#### Added
- `OpenAstroAra.Server` Phase 7 endpoint surface — sequence + calibration + mosaic scaffold. ~16 sequence records, 8 service interfaces, 16 sequence endpoints (incl. `PATCH /api/v1/sequences/{id}` for partial updates), 6 calibration endpoints, 6 mosaic endpoints (panel DTO includes §47.3 `crosses_ra_wrap` flag). (PR #39)

### [phase-6-complete] - 2026-05-26

#### Added
- `OpenAstroAra.Server` Phase 6 equipment scaffold + Alpaca discovery — 12 DTO records covering all device types, 12 service interfaces, functional `EquipmentDiscoveryService` (Alpaca UDP discovery on port 32227 via `ASCOM.Alpaca.Discovery.GetAscomDevicesAsync`), `GET /api/v1/equipment/discover/{type}` operational, per-device 501 stubs. Global `JsonStringEnumConverter` with `LowerCaseNamingPolicy`. (PR #38)

### [phase-5-complete] - 2026-05-26

#### Added
- `OpenAstroAra.Server/openapi.yaml` — full v0.0.1 API contract. 6 endpoint groups (Server / Equipment / Sequence / Image / Log / Stream). Cursor-based pagination per §60.2. `Idempotency-Key` header per §60.5. RFC 7807 `Problem` schema. `ServerStateSnapshot.ws_resume_token` for §60.9 resume. Full WebSocket protocol documented. OpenAPI 3.1 null unions throughout. (PR #37)

### [phase-4-complete] - 2026-05-25

#### Added
- `OpenAstroAra.Server` project (`Microsoft.NET.Sdk.Web`, `net10.0`, AOT in Release). Kestrel port resolution env → appsettings → 5555. `/healthz` + `/api/v1/server/info` operational. Scalar UI mounted at `/scalar/v1`.

### [phase-3-complete] - 2026-05-25

#### Changed
- `PHD2Guider.cs` now logs `openastro-phd2` vs upstream PHD2 detection via `PHDSubver`/`PHDVersion` substring match. Copyright mojibake fixed.

### [phase-2-complete] - 2026-05-24

#### Changed
- Equipment layer collapsed to Alpaca-only per §52. Added `IEquipmentProvider` per §6.2 with `DiscoverAsync` + `ConnectAsync<T>`.

### [phase-0.5p-complete] - 2026-05-24

#### Changed
- .NET 10 SDK pin (`global.json`) + csproj `TargetFramework` bumps. First build cleanup pass (Astrometry+Core+Test forced to `net10.0-windows` TFM, `UseWPF=true` restored where local build revealed deep WPF deps).
- Solution renamed `NINA.sln` → `OpenAstroAra.sln`. `.gitignore` rewritten.

#### Removed
- `OpenAstroAra.Core/Database/**` per §56 NINA-DB-greenfield (NINADbContext + BrightStars / Constellation / ConstellationBoundary / DsoDetail / EarthRotationParameters / VisualDescription / etc.).
- `OpenAstroAra.Core/Utility/Notification/CustomDisplayPart.xaml{,.cs}` (WPF toast UI).
- `OpenAstroAra.Core/MyMessageBox/MyMessageBoxView.xaml{,.cs}` (WPF dialog view).
- Various `Datatemplates.xaml{,.cs}` + `ProgressStyle.xaml` across Sequencer.

### [phase-0.5n-complete] - 2026-05-24

#### Changed
- Project renames: `NINA.Astrometry` → `OpenAstroAra.Astrometry`, `NINA.Profile` → `OpenAstroAra.Profile`, `NINA.Image` → `OpenAstroAra.Image`, `NINA.Equipment` → `OpenAstroAra.Equipment` (rename + cascade scrub of `using NINA.Equipment`), `NINA.Sequencer` → `OpenAstroAra.Sequencer`, `NINA.Platesolving` → `OpenAstroAra.PlateSolving`, `NINA.Test` → `OpenAstroAra.Test`.

### [phase-0.5g-complete] - 2026-05-24

#### Changed
- Project rename: `NINA.Core` → `OpenAstroAra.Core`.

### [phase-0.5f-complete] - 2026-05-24

#### Removed
- Stefan Berg / NINA branding stripped from user-facing strings.
- All non-English locale `.resx` files (en-US + en-GB only henceforth).

### [phase-0.5d-complete] - 2026-05-24

#### Removed
- ASCOM COM glue across `NINA.Equipment` (replaced by Alpaca in Phase 2).

#### Changed
- CI workflow hardened — `actions/checkout` pinned to commit SHA + `persist-credentials: false` per zizmor + CodeRabbit security findings on PR #12.

### [phase-0.5c-complete] - 2026-05-24

#### Removed
- All vendor SDK directories (Canon/Nikon/ZWO/QHY/etc.) and their vendor concrete equipment impls. Replaced by Alpaca-only providers in Phase 2.

### [phase-0.5b-complete] - 2026-05-23

#### Removed
- `NINA.MGEN`, `nikoncswrapper`, WiX setup projects, NINA Plugin scaffolding.

#### Added
- The four §1 tracking files (PORT_PLAYBOOK.md / COMMIT-PR-RULES.md / PORT_PROGRESS.md / PORT_TODO.md) added retroactively per playbook §1.

### [phase-0.5a-complete] - 2026-05-23

#### Removed
- `NINA/` WPF host project (~600+ files: View, ViewModel, Utility, Database, Resources, External, .sln deregister).
- `NINA.WPF.Base/` (~180+ files: ViewModel, Resources, Interfaces).
- `NINA.CustomControlLibrary/` (~67 files).
- Pure-delete WPF demolition, split across 6 sub-PRs (#4-#9) for CodeRabbit's 150-file-per-PR limit.

#### Added
- `prep-ci` baseline (PR #2): progressive `.github/workflows/ci.yml` placeholder, `.coderabbit.yaml` with `port/ara` in `auto_review.base_branches`, branch-naming Git-ref conflict fix (`port/ara/<name>` → flat `<name>`), AI merge authority granted.
- `rules-tighten` (PR #10): §19.1 merge-gate tightened (no merge on CR rate-limit), §22 periodic `port/ara → master` cadence added (replacing one-shot-at-Phase-15).
- The four §1 tracking files re-pinned in `tracking-files` (PR #11).

### Pre-Phase-0.5 — Design phase (2026-05-23, before any code change)

#### Added
- `design/PORT_PLAYBOOK.md` — 12k+ line port plan, baked across 6 design passes incorporating Tier-1 through Tier-3 decisions on AOT publishing, OpenAPI generation, FITS lib choice, systemd hardening, udev groups, watchdog model, port-number choice (5555), NTP strategy, WebSocket protocol, filename conventions, simulator pinning, API versioning, CORS policy, performance SLOs, Pi hardware matrix, USB cabling advisory, FITS corruption recovery, version compatibility matrix, network bring-up, GFS retention, equipment-DB deferral, NINA-DB greenfield decision, settings-registry gate, §73 exception policy, §8.1 DI mapping, §60.10 WS catalog, §74 CONTRIBUTING placeholder.
- `design/COMMIT-PR-RULES.md` — per-phase + sub-PR rhythm, branch naming, CR poll-and-fix loop, §19.1 merge-gate, `.coderabbit.yaml` config, Phase 12 8-sub-PR split, settings-registry + help-registry mechanical gates.
- `design/GAPS-ARA.md` — feature gaps between NINA and Ara's target.
- `design/PHD2-GAP.md` — PHD2 fork plan for `openastro-phd2`.
- `design/PORT_DECISIONS.md` — append-only decision log.
- `design/PORT_TODO.md` — append-only `TODO(port)` + deferred-CR-finding log.
- `design/API_CONTRACT.md` — early stub (superseded by `OpenAstroAra.Server/openapi.yaml` at Phase 5).

[Unreleased]: https://github.com/open-astro/openastro-ara/compare/phase-10.5-complete...HEAD
[phase-10.5-complete]: https://github.com/open-astro/openastro-ara/compare/phase-10-complete...phase-10.5-complete
[phase-10-complete]: https://github.com/open-astro/openastro-ara/compare/phase-9-complete...phase-10-complete
[phase-9-complete]: https://github.com/open-astro/openastro-ara/compare/phase-8-complete...phase-9-complete
[phase-8-complete]: https://github.com/open-astro/openastro-ara/compare/phase-7-complete...phase-8-complete
[phase-7-complete]: https://github.com/open-astro/openastro-ara/compare/phase-6-complete...phase-7-complete
[phase-6-complete]: https://github.com/open-astro/openastro-ara/compare/phase-5-complete...phase-6-complete
[phase-5-complete]: https://github.com/open-astro/openastro-ara/compare/phase-4-complete...phase-5-complete
[phase-4-complete]: https://github.com/open-astro/openastro-ara/compare/phase-3-complete...phase-4-complete
[phase-3-complete]: https://github.com/open-astro/openastro-ara/compare/phase-2-complete...phase-3-complete
[phase-2-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5p-complete...phase-2-complete
[phase-0.5p-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5n-complete...phase-0.5p-complete
[phase-0.5n-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5g-complete...phase-0.5n-complete
[phase-0.5g-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5f-complete...phase-0.5g-complete
[phase-0.5f-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5d-complete...phase-0.5f-complete
[phase-0.5d-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5b-complete...phase-0.5d-complete
[phase-0.5c-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5b-complete...phase-0.5c-complete
[phase-0.5b-complete]: https://github.com/open-astro/openastro-ara/compare/phase-0.5a-complete...phase-0.5b-complete
[phase-0.5a-complete]: https://github.com/open-astro/openastro-ara/releases/tag/phase-0.5a-complete
