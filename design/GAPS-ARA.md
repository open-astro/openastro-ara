# GAPS-ARA — Open Items Tracking

**Purpose:** comprehensive list of things discussed during planning that aren't yet in [`PORT_PLAYBOOK.md`](PORT_PLAYBOOK.md), or that are partially covered and need a dedicated section. As each is discussed and baked, it moves to **Resolved** with a pointer to the playbook section.

**Convention:** check the box when resolved. Status notes inline. New items added to the bottom of their tier.

---

## ⏸ Session checkpoint — pick up here next time (2026-05-23)

**Where we left off:** Closed the last design-phase loose ends — COMMIT-PR-RULES.md 3 remaining TBDs all decided (no skip-review markers; every PR gets full CodeRabbit review including mechanical deletes/renames; minimal `.coderabbit.yaml` excludes only generated code + temp files; no `develop` branch — sub-PRs land on `port/ara` directly with Phase 15 final PR going `port/ara → master`). PHD2-GAP.md Path A confirmed but deferred to post-Phase-12 (when Phase 12 sub-PRs 12b + 12c are merged, the upstream openastro-phd2 PR gets filed with integration-validated event/RPC shapes; ARA v0.0.1 ships with workarounds, v0.1.0 switches to new events). Resolved all 6 Tier-2 items in a batch earlier this session: §36.10.5 Linux WebView fallback (external-browser via localhost shelf server, offline-aware), §28.13 cooler warmup at session end (off default for CMOS, ramp for CCDs, immediate option), §28.12 paused-sequence semantics (15-row matrix), §31.5 explicit DST/TZ policy, §47.3 mosaic RA-wrap confirmation + integration test, §23 safer bulk-rename pattern with dry-run + license-file excludes. Total Tier 1 + Tier 2 resolved: 23 items. Earlier this session resolved testing strategy — expanded §14 from 3 short subsections into a full 8-subsection spec covering 3 test layers per component (unit/integration/E2E-smoke for server; widget/integration/manual-UI for client), pre-PR gate via `scripts/pre-pr-check.sh` (runs `dotnet format/build/test/publish`, `dart format`, `flutter analyze/test/integration_test/build linux`, settings-registry check; ~2–5 min runtime), equipment simulators (Alpaca simulators from ASCOM Platform bundled to `OpenAstroAra.Test/fixtures/`; openastro-phd2 headless mode; no real hardware in CI), manual UI verification with screenshots required for any Flutter PR touching user-visible UI (golden path + edge case attached to PR description), soft coverage policy (all existing NINA tests pass + new code has tests + integration tests for cross-cutting features; no hard % in v0.0.1), updated §15 build gate to invoke the pre-PR script, added §61 search registry entry. Also updated COMMIT-PR-RULES.md CodeRabbit loop to require pre-PR-gate-green + screenshots before opening PR. Earlier this session resolved Phase 12 split shape — confirmed 8 sub-PRs (12a–12h) per `COMMIT-PR-RULES.md` augmented with all session decisions (Data Manager in 12e Sky Atlas; §65 stretch picker in 12f frame viewer; §29.1.3 reformat UX in 12h Settings; §51 diagnostics mode picker in 12h; §61 smart search cross-cuts in 12h; §64 Live View in 12c; §63 PHD2 settings in 12h; §41 mobile conditional shell in 12a; §46 notification feed in 12a; §35 emergency stop + alarm modal in 12a). Also baked a **CodeRabbit poll-and-fix workflow** per user request: AI polls open PRs every 60s for CodeRabbit comments, auto-fixes trivial + correctness findings via additional commits, posts "Ready for human review" after 3 minutes idle, user handles merge. AI auto-continues to next sub-PR after user merges (no nudge between sub-PRs). Multiple "Things still to decide" items in COMMIT-PR-RULES.md ticked off (sub-PR letter scheme, push cadence, mid-port CodeRabbit fix flow, auto-continue policy). **Next discussion topic queued: testing strategy** — per user "we need to discuss testing to make sure we are running multiple tests on the code before we submit a pr." Earlier this session resolved §51 diagnostics default mode — flipped v0.0.1 default from `balanced` → `notify_only`. Smart diagnostics still run + surface in Health Indicator + Diagnostic Panel + per-frame FITS metadata, but ARA takes no autonomous corrective action (auto-refocus, auto-pause, auto-skip) without explicit user opt-in. Three converging reasons: hard-coded thresholds aren't per-user-calibrated (calibration ships v0.1.0 §51.9); matches NINA + ASIAir conservative posture; first-impression risk on uncalibrated thresholds. Promotion path: `balanced` becomes default in v0.0.2 / v0.1.0 once per-user calibration ships and telemetry confirms rare false-positives. Settings-only picker (no wizard screen added — keeps wizard at 18 screens). §51.12 added 5 search registry entries. Earlier this session resolved §67 security model — the "threat model paragraph" gap turned out to be a full architectural decision per user: **no authentication in v0.0.1, matching ASCOM Alpaca + ZWO ASIAir**. User framing: "we are imagers not hackers of imaging rigs. Multiple people hit dark sky parties with ZWO ASIAir and no one is hacking one another." Baked §67 as canonical statement (threat model, defense without auth, deployment recommendations, v0.1.0 remote-access mode for over-internet imaging, anti-features list, search registry). Stripped token/auth from ~20 places across the playbook: §2 (target stack), §9 (API contract), §12 (Flutter first-run flow), §19 (secrets handling reframed as forward-looking), §24 + Phase 15 demo flow, §25 (token-reset button → "Forget this server"), §27 (connect endpoint), §29.1 (token file removed from SD-card listing), §30.1/30.3 (launch flow simplified), §30.6 (Token management → Server connection management), §33.3/33.4 (update push uses SHA-256 as integrity gate without auth), §34.3 (postinst drops token generation), §34.6 (DEPLOY.md drops token retrieval), §41 (mobile aligned), §44 (backup-stream/claim), §49 (Swagger UI all-open), §60.4 (ws_resume_token clarified as replay cursor not auth), §60.6 (rate limiting simplified), §64 (stale "token auth per §9" line fixed). Earlier this session resolved §29.1.1/§29.1.3/§29.1.4 storage permissions bootstrap — server-side `configure-storage.sh` helper installed by .deb postinst (§34.3) with narrow sudoers scope (only update.sh + configure-storage.sh, NOPASSWD); WILMA's "Configure storage" flow automatically mounts + chowns + appends fstab line via the helper; in-app ext4 reformat ships in v0.0.1 with label-echo confirmation UX (user must type drive's label to authorize wipe — defense against accidental wipes); 9 distinct exit codes mapped to 422 error codes for granular WILMA messaging; §28.9 strengthened with two layers of ext4 enforcement (configure-time helper validation + startup-time server check); §29.7 DEPLOY.md trimmed (manual path now positioned as advanced fallback, default is automatic via WILMA); §29.8 search registry entries added. Earlier this session resolved §66 server concurrency model (simplified after user pushback on over-engineering — 1 image-processor worker is plenty for 30–900 s DSO exposures; bounded queues only where memory leaks matter; silent backpressure WS event, no notifications; log-only telemetry in v0.0.1) AND made the §18.J architectural scope decision: ARA targets DSO + comets only, planetary/lunar lucky-imaging is permanently out of scope (Alpaca has no video API — NINA has the same limit). Cleanup propagated through §38.7 (lunar.json + planetary.json dropped; comet.json added), §62 (short-exposure dither examples reworded), §64 (v0.1.0 video extension aspirations removed; SER / ROI / native-video marked permanently out per §18.J), §55 roadmap (planetary entry moved from v0.2.0 to "out of scope indefinitely"), §36 reference cleanup. Earlier this session resolved §28.6–§28.11 data durability + power-loss handling — SQLite WAL + synchronous=NORMAL, FITS atomic-write pipeline (tmp + fsync + rename + dir-fsync + DB row), startup orphan scan with silent auto-recovery, ext4 hard-refused on non-ext4 storage, DEPLOY.md fstab updated with data=ordered/noatime/errors=remount-ro, UPS advisory in DEPLOY.md, full scenario matrix documented. Earlier this session resolved §65 image stretching pipeline + preview API (7-stretch palette, per-profile default + frame-type auto-override, manual sliders ship in v0.0.1 with 200 ms debounce, 6-variant LRU cache, batch re-stretch, full §61 registry coverage), and §36 Sky Atlas via a delivery-shift rescope (installer slims from ~1 GB to ~50 MB; all functional features keep; Data Manager + mandatory wizard + sky-data banner added). Prior sessions closed the AI/user/sleeping-rig safety arc (Stop Mount + meridian flip + unattended-failure shutdown), the autofocus + dither + collimation work (Smart Focus + Classic AF fallback), the PHD2 lifecycle integration (aligned with the actual openastro-phd2 fork), the discoverability foundation (§0.5 design principles + §61 smart settings search + the COMMIT-PR-RULES settings-registry gate), the §60 API conventions, and the §30.7 equipment-change check. Captured PHD2 RPC gaps in a dedicated `design/PHD2-GAP.md` for later action.

### Tier 1 + Tier 2 + COMMIT-PR-RULES + PHD2-GAP items resolved across recent sessions (27)

| Item | Resolution |
|---|---|
| §60 API conventions | Baked §60 |
| Meridian flip workflow | Baked §58 (with §57 Stop Mount + slew safety as foundation) |
| Autofocus orchestration | Baked §59 (Smart Focus + Classic AF fallback + collimation detection) |
| Dither policy | Baked §62 (with §30.7 equipment-change check as spillover) |
| PHD2 / openastro-phd2 lifecycle | Baked §63 (aligned with existing openastro-phd2 fork; gaps captured in `PHD2-GAP.md`) |
| Design principles preamble | Baked §0.5 (three pillars) |
| Smart settings search | Baked §61 (with settings-registry gate enforced via `COMMIT-PR-RULES.md`) |
| Equipment-change check on profile load | Baked §30.7 (spillover from dither + PHD2 work) |
| §36 Sky Atlas (2026-05-23) | Baked §36 rewrite — installer slimmed to ~50 MB, all functional features kept, Data Manager added (§36.2 four-tab UI), wizard made mandatory (§37 preamble + §30.4), sky-data-missing banner added (§36.13, sharing §30.7.3 banner shell). Predicted "cut features" rescope rejected; user chose delivery-shift instead. |
| §65 Image stretching pipeline (2026-05-23) | Baked §65 — 7-stretch palette (auto_stf, linear, log, asinh, sqrt, equalized, manual), per-profile default for Lights + auto-override to linear for Dark/Bias/Flat, manual sliders in v0.0.1 with 200 ms debounce server round-trip, 6-variant LRU cache per frame, batch re-stretch endpoint (`POST /sessions/{id}/restretch`), full WS event suite (`frame.preview.*`, `session.restretch.*`), mobile gets full palette via same API, §61 search registry entries for every knob. Real-time client-side slider on desktop deferred to v0.1.0. §40.2 + §40.3 updated to reference §65. |
| §28.6–§28.11 Data durability + power-loss (2026-05-23) | Baked into §28 — SQLite `journal_mode=WAL` + `synchronous=NORMAL` (modern standard, zero corruption); FITS atomic-write pipeline (`<frame>.fits.tmp` → fsync → rename → fsync directory → DB row); startup orphan scan with silent auto-recovery (re-inserts FITS metadata, emits `frame.recovered_orphan` WS event); ext4 hard-required (server refuses to start otherwise); recommended fstab options (`data=ordered,noatime,errors=remount-ro`) added to §29 DEPLOY.md guide; UPS advisory added (Geekworm X728 / PiJuice / 12V HATs) — not required for v0.0.1; full crash-scenario matrix documented. Net property: no torn FITS ever appears under real name, no orphan ever invisible to library, max data loss = single in-flight exposure. |
| §66 Server concurrency model + §18.J imaging scope (2026-05-23) | Simplified after user pushback on over-engineering. Baked §66 — 1 image-processor worker (was 2), bounded queues only where memory leaks matter (WS per-client 256, alt-stretch 8, diagnostics 3, image processor 10, backup stream 1), silent `capture.backpressure` WS event with no notification, log-only telemetry in v0.0.1 (`/api/v1/server/internal-state` + Stats panel deferred to v0.1.0), ~1 GB peak RSS budget on Pi 4 (3 GB headroom), storage-slow cascade self-heals with USB-SSD upgrade prompt. Also baked §18.J scope statement: ARA = DSO + comets only, planetary/lunar lucky-imaging permanently out of scope (Alpaca has no video API, NINA has same limit). Cleanup: §38.7 templates trimmed (lunar+planetary dropped, comet added → 3 templates), §62 short-exposure dither examples reworded, §64 v0.1.0 video extension aspirations removed (SER/ROI/native-video permanently out per §18.J), §55 planetary entry moved from v0.2.0 to "out of scope indefinitely," §36 "if they're not doing planetary" reference replaced. |
| §29.1.1/§29.1.3/§29.1.4 Storage permissions bootstrap (2026-05-23) | Server-side `configure-storage.sh` helper installed by .deb postinst (§34.3) at `/opt/openastroara/scripts/`; narrow sudoers scope (update.sh + configure-storage.sh, NOPASSWD). WILMA "Configure storage" automatically: validates UUID → validates ext4 → creates mount point → appends fstab line with §28.9 mount options → mounts → chowns to openastroara:openastroara. In-app ext4 reformat ships in v0.0.1 with label-echo confirmation (user types drive's label to authorize wipe). 9 helper exit codes mapped to 422 `code` fields for granular WILMA messaging. §28.9 strengthened with two-layer ext4 enforcement (helper-time + server-startup-time). §29.7 DEPLOY.md restructured (automatic path is default; manual fstab fallback for advanced users). §29.8 search registry entries added (5 entries). |
| §67 Security model — NO AUTH v0.0.1 (2026-05-23) | Threat-model-paragraph gap turned out to be architectural: per user, "no authentication like Alpaca — we are imagers not hackers of imaging rigs." Baked §67 (trusted-LAN posture matching ASCOM Alpaca + ZWO ASIAir; threat model; defense-without-auth list; deployment recommendations for home / star party / public Wi-Fi / remote observatory; v0.1.0 opt-in remote-access mode for over-internet imaging; anti-features list; search registry entries). Stripped token/auth from ~20 references across the playbook (§2, §9, §12, §19, §24, §25, §27, §29.1, §30.1, §30.3, §30.6, §33.3, §33.4, §34.3, §34.6, §41, §44, §49, §60.4, §60.6, §64 endpoints). User experience now: plug Pi in → mDNS discover → connect — no token entry, no postinst journalctl-fishing for the initial token, no per-server token storage. Integrity preserved where it actually matters: binary updates still validate SHA-256 (§33.4); storage reformat still requires label-echo confirmation (§29.1.3); single-client policy (§27) still provides session ownership UX. v0.1.0 brings auth + TLS back, but ONLY for opt-in remote-access mode on a separate interface — default LAN deployment stays open forever. |
| §51.5/§51.11/§51.12 Diagnostics default mode (2026-05-23) | Flipped v0.0.1 default from `balanced` → `notify_only`. Smart diagnostics + Health Indicator + Diagnostic Panel + per-frame FITS metadata enrichment all still run; ARA simply takes no autonomous corrective action (auto-refocus, auto-pause, auto-skip, re-slew) without explicit user opt-in via Settings → Diagnostics. Reasoning: hard-coded thresholds aren't per-user-calibrated (calibration ships v0.1.0 §51.9); matches NINA + ASIAir conservative posture; first-impression risk on uncalibrated thresholds. Promotion path documented in §51.11: `balanced` becomes default in v0.0.2/v0.1.0 once per-user calibration ships and telemetry confirms rare false-positives. Settings-only mode picker (no new wizard screen — keeps the 18-screen wizard intact). §51.12 NEW added 5 search registry entries (`diagnostics.mode`, `diagnostics.auto_refocus_threshold`, `diagnostics.pause_on_cloud_detection`, `diagnostics.pause_threshold_star_count_drop_pct`, `diagnostics.max_consecutive_solve_failures`). |
| Phase 12 split confirmed + CodeRabbit polling workflow (2026-05-23) | Phase 12 split = 8 sub-PRs (12a–12h) per `COMMIT-PR-RULES.md`, augmented mapping integrates all session decisions (Data Manager in 12e Sky Atlas; §65 stretch picker in 12f frame viewer; §29.1.3 reformat UX + §51 diagnostics mode picker + §61 smart search + §63 PHD2 settings in 12h Settings; §64 Live View + §51 Health Indicator + §47 mosaic in 12c Imaging; §41 mobile conditional shell + §46 notification feed + §35 emergency stop/alarm in 12a App shell). Branch naming `port/ara/phase-N/<letter>`. AI auto-continues to next sub-PR after user merge. **NEW: CodeRabbit poll-and-fix workflow** — AI polls open PRs every 60s for CodeRabbit comments via `gh api ...pulls/<pr>/comments`, auto-fixes trivial findings (formatting/naming/clarity) + correctness issues via additional commits, replies "Fixed in <sha>"; disagreements get reasoned replies (no code change); out-of-scope suggestions tracked in `design/PORT_TODO.md`; quiescence detected after 3 minutes idle → AI posts "Ready for human review @<user>"; user reviews + merges (AI never merges, ever). COMMIT-PR-RULES.md "Things still to decide" list trimmed: 6 of 8 items resolved (sub-PR letter scheme, per-phase rhythm, auto-continue, mid-port CodeRabbit fix flow, push cadence; CodeRabbit skip-review marker + `.coderabbit.yaml` + `develop` branch still TBD). §3 Phase 12 entry updated to reference the sub-split. |
| §14 Testing strategy expansion (2026-05-23) | Expanded §14 from 3 short subsections to 8 covering: §14.1 three server test layers (unit xUnit+NSubstitute / integration WebApplicationFactory+SQLite+sims / E2E smoke ARM64 Docker); §14.2 three client test layers (widget flutter_test+mocktail / integration_test with mocked server / manual UI verification with screenshots); §14.3 CI matrix expanded (server-e2e job + settings-registry job added); §14.4 NEW pre-PR gate (`scripts/pre-pr-check.sh` — runs dotnet format/build/test/publish + dart format + flutter analyze/test/build linux + settings-registry check based on changed files; ~2–5 min); §14.5 NEW equipment simulators (Alpaca simulators from ASCOM Platform bundled to `OpenAstroAra.Test/fixtures/`; openastro-phd2 headless; no real hardware in CI); §14.6 NEW manual UI verification (golden path + edge case screenshots attached to PR description for any user-visible Flutter UI change); §14.7 NEW coverage policy (soft target for v0.0.1: all existing NINA tests pass + new code has tests + integration tests for cross-cutting features; hard % deferred to v0.1.0+); §14.8 NEW search registry entry. §15 build gate updated to invoke `scripts/pre-pr-check.sh`. COMMIT-PR-RULES.md CodeRabbit loop updated to require pre-PR-gate-green + screenshots before opening PR. |

### Tier 1 items still to discuss next session (in suggested order)

1. ~~**Live view / loop imaging**~~ → **Resolved §64** (2026-05-22).
2. ~~**§36 Sky Atlas rescope**~~ → **Resolved §36** (2026-05-23). Outcome was the opposite of the predicted cut: all 21 surveys, Tonight's Sky planetarium, full DE440, comet motion trails, star catalogs all KEPT in v0.0.1, restructured as opt-in downloads via new Data Manager (§36.2). Mandatory wizard (§37) + recommended-set logic (§36.10) + silent banner (§36.13) ensure users get appropriate downloads without bundling everything.
3. ~~**Image stretching pipeline + preview API knobs**~~ → **Resolved §65** (2026-05-23). 7 stretch IDs, per-profile default + frame-type auto-override, manual sliders in v0.0.1, 6-variant LRU cache, batch re-stretch endpoint, full §61 search registry coverage. Real-time client-side slider deferred to v0.1.0.
4. ~~**Power loss durability**~~ → **Resolved §28.6–§28.11** (2026-05-23). WAL + synchronous=NORMAL; FITS atomic-write with fsync; orphan auto-recovery; ext4 hard-required; UPS advisory in DEPLOY.md.
5. ~~**Concurrency model on Pi during a session**~~ → **Resolved §66 + §18.J** (2026-05-23). Simplified: 1 image-processor worker, bounded queues only for memory-leak edge cases, silent backpressure, log-only telemetry. Coupled scope decision: §18.J bakes ARA = DSO + comets only (planetary/lunar lucky-imaging permanently out — Alpaca has no video API).
6. ~~**Storage permissions bootstrap**~~ → **Resolved §29.1.1/§29.1.3/§29.1.4** (2026-05-23). Server-side helper (`configure-storage.sh`) installed by .deb postinst with narrow sudoers scope; WILMA "Configure storage" automatically mounts + chowns + appends fstab line; in-app ext4 reformat with label-echo confirmation ships in v0.0.1.
7. ~~**Threat model paragraph**~~ → **Resolved §67** (2026-05-23). Turned out to be much bigger than a paragraph: per user, drop authentication entirely in v0.0.1 to match Alpaca + ASIAir. Baked §67 (security model) + cleaned token/auth from ~20 references.
8. ~~**§51 diagnostics default mode**~~ → **Resolved §51.5 + §51.11 + §51.12** (2026-05-23). Flipped default from `balanced` → `notify_only` for v0.0.1; smart diagnostics still run (Health Indicator, Diagnostic Panel, per-frame FITS metadata) but no autonomous corrective action without explicit user opt-in. Promotion path: `balanced` becomes default in v0.0.2/v0.1.0 once per-user calibration ships and telemetry confirms rare false-positives.
9. ~~**Phase 12 split confirmation**~~ → **Resolved 2026-05-23**. 8 sub-PRs (12a–12h) confirmed; augmented mapping integrates all session decisions; CodeRabbit poll-and-fix workflow added (60s polling, AI auto-fixes, user merges).

### Next discussion topic (queued)

~~**Testing strategy**~~ → **Resolved §14.1–§14.8** (2026-05-23). Expanded testing spec covers 3 test layers per component, pre-PR gate script, equipment simulators, manual UI verification with screenshots, soft coverage policy. See "Tier 1 items resolved" table for full detail.

**Design phase complete (2026-05-23).** All Tier-1 (9 of 9), all Tier-2 (6 of 6), all COMMIT-PR-RULES TBDs (3 of 3), and PHD2-GAP.md Path A decision all resolved.

**Remaining work:**
- **Documentation** (post-Phase 15, AI-executed): README rewrite, NOTICE.md full text, DEPLOY.md, User guide, CONTRIBUTING.md, ISSUE_TEMPLATE / PULL_REQUEST_TEMPLATE, API_CONTRACT.md initial content. No design decisions needed — write-the-thing tasks per existing playbook references (§17.2 for NOTICE.md, §34.6 for DEPLOY.md, §56 for migration guide, etc.). Folded into Phase 15.
- **PHD2-GAP.md upstream PR** (post-Phase 12): file the upstream openastro-phd2 PR with the 3 real gaps + 7 doc clarifications once Phase 12 sub-PRs 12b + 12c are merged. ARA v0.0.1 ships with workarounds; v0.1.0 switches to the new events.

**Ready to start Phase 0.5 execution** whenever the user gives the go-signal. The first sub-PR is 0.5a (delete WPF UI) per the 16-sub-PR plan in `COMMIT-PR-RULES.md`. AI runs `scripts/pre-pr-check.sh` before opening each PR; CodeRabbit reviews every PR; AI auto-fixes findings; user reviews + merges.

### Parked work in other docs to revisit

- **`COMMIT-PR-RULES.md`** — per-phase PR rhythm + sub-PR splitting approved in principle; still need to (1) decide the open items in the "Things still to decide" list and (2) bake into `PORT_PLAYBOOK.md` (§0 rule 9, §3 phase plan, §19.1 git safety, §22 final pass). The **settings-registry gate** within this doc IS settled and applies from Phase 12 onward.
- **`PHD2-GAP.md`** — 3 real gaps in openastro-phd2 (dark-library progress events, structured equipment-disconnect events, `get_version` RPC) + 7 nice-to-haves + 7 doc clarifications. Decide whether to add them to openastro-phd2 or have ARA work around them.

### Suggested re-entry prompt for next session

> "Continuing the ARA design pass from 2026-05-21. Read `design/GAPS-ARA.md` session checkpoint; let's pick up [item]."

---

---

## Tier 1 (late additions — from review pass, 2026-05-19)

Found during a final review of the playbook. Worth v0.0.1 attention because they're foundational hygiene the AI would otherwise skip.

- [x] ~~**Accessibility (a11y) commitment**~~ → **Resolved §53**. WCAG 2.1 AA-leaning baseline (not formally certified). Color + symbol on status indicators (covers color-blind item), high-contrast theme variant (toggle in Settings → Display), Flutter Semantic widgets on custom controls, font scaling honored, reduce-motion respected, keyboard navigation on desktop, visible focus indicators, 4.5:1 minimum text contrast verified across `AraColors` tokens, screen-reader smoke test in Phase 11. Benefits color-blind users, aging astrophotographers, low-vision users, glare/night-vision conditions, keyboard users, outreach orgs needing ADA/Section 508/EAA compliance.
- [x] ~~**Color-blind friendly status indicators**~~ → **Resolved §53.2** (folded into a11y section). Shape + color on every status indicator: ✓ check / ⚠ warning / ⛔ no-entry / ○ disabled / ⓘ info. Reusable `StatusIndicator` Flutter widget used everywhere.
- [x] ~~**PII redaction in bug reports**~~ → **Resolved §54**. Review-first submission, not auto-redact-first (GPS data is genuinely useful for debug). User sees what's in the zip before submitting, with sensitivity-detected items highlighted (GPS, serial numbers, hostnames, paths, internal IPs). Four sharing modes: include-everything (default, best debug), coarse-GPS-only (round to ~111 km), redact-all (placeholder substitution), let-me-edit-zip-first (power-user escape). Always-blacklisted regardless of mode: API tokens, secrets, SSH keys, files outside ARA data dirs. Bug template auto-records "sharing mode used" so maintainer knows redaction level.

## Tier 1 (late additions — from second review pass, 2026-05-21)

Found during a second end-to-end review of the playbook. These are gaps the AI will hit during the port and either skip ("TODO(port)") or guess at, because the playbook references the feature but doesn't decide the user-visible policy. Spec before turning the AI loose on Phase 0.5.

**Inherited-from-NINA workflows that need explicit policy specs (the AI cannot guess these without producing bad UX):**

- [x] ~~**Meridian flip workflow**~~ → **Resolved §58** (with foundational §57 + new §61 spilling out of the same discussion). §58 specs the three timing knobs (`pause_before_min`, `pause_after_min`, `max_wait_after_min`), rule-of-thumb table per rig class (C14 → 1-2 min; RedCat on GEM → 10-15 min; strain-wave → 45-75 min; never-flip alt-az/fork-on-wedge → `enabled: false`), post-flip recovery (slew → plate-solve+re-center → conditional re-focus → guider re-cal `auto_restore` default), side-of-pier verification (no brand-quirk DB per §52), failure-handling matrix, first-flip-confirmation safety net (catches "I set the wrong number" before damage), four-layer unattended flip safety (pre-flight check → watchdog → post-flip verification gate → safe rest state with mount park), severity escalation during unattended hours, pre-sleep checklist surfaced before astronomical dusk, **10-minute unattended-failure graceful shutdown** with cooler warmup before camera disconnect, WebSocket events, API endpoints. Spillovers: §57 (Stop Mount contextual button during all autonomous slews + safety-speed slews at 50% default + hardware-kill-switch DEPLOY.md recommendation; horizon profile / HA limits / no-go polygons deferred to v0.1.0 Mount Safety v2); §61 (smart settings search — committed because the meridian-flip + mount-safety + safety-policy + notification settings proliferation would be lost in a NINA-style tree without it); §0.5 design principles preamble (codifies "Better than NINA / AI-assisted design / Robust + friendly + discoverable" as values the whole playbook serves).
- [x] ~~**Autofocus orchestration**~~ → **Resolved §59 (Smart Focus + Classic AF fallback)**. Pivoted from "spec NINA's AF behavior" to "design a smart algorithm that obsoletes most of NINA's 17 knobs." **Smart Focus** is the primary mode: after one-time ~5 min Classic AF calibration per profile, every subsequent AF completes in 2-3 exposures (30-90 s vs NINA's 3-5 min) by extracting a feature vector (HFR, FWHM, donut outer/inner diameter, ring thickness, asymmetry coefficient, peak-to-bg) from a single image and looking up the predicted focuser offset+direction in the calibration table. Telescope-type-aware (Refractor/SCT/Maksutov/RC/Newtonian/Other) drives which features matter. Six triggers (sequence start, time interval 90 min, temp Δ 1.5°C, HFR drift 15%, post-meridian-flip per §58, first use of filter). Filter policy: `use_current_filter` only — no swap-to-luminance because non-parfocal filters break that pattern. **Three-layer backlash auto-discovery**: probe routine appended to calibration (~60-90 s) + passive refinement every AF run + equipment-change auto-invalidation via Alpaca DriverInfo. Zero backlash questions in the wizard. **Collimation health detection** as free byproduct of calibration — measures donut centroid offset (outer ring vs inner shadow), reports severity (good/warning/critical at 5%/15% thresholds) with clock-position direction; advisory only in v0.0.1 (prescriptive screw-turning guidance deferred to v0.1.0 community knowledge base). **Diagnostic-skip integration with §51** defers AF during clouds/dew/aperture-blocked states. Fallback to Classic AF (9-step parabolic-with-hyperbolic-fallback curve, HocusFocus star detection preserved verbatim) when Smart Focus diverges. Schema reduced from NINA's 17 visible settings to 6 (everything else hidden in `advanced` disclosure or derived from observation). Wizard's telescope screen reduced from NINA's multi-page form to 5 questions. v0.1.0 paths: ML feature extraction, temp-aware backlash, prescriptive collimation guidance, refractor collimation detection, star-test mode, tilt-aware focus.
- [x] ~~**Dither policy**~~ → **Resolved §62** (with spillover §30.7 equipment-change check). PHD2 handles the actual mount nudging via JSON-RPC; ARA decides when/how/whether with three "better than NINA" defaults: **auto-compute magnitude from pixel scale** (target 5 arcsec angular displacement → computed dither_pixels per rig, clamped [3, 15]), **auto-disable for short-exposure workflows** (< 60 s threshold catches lunar/planetary/lucky-imaging without manual toggle), **diagnostic-aware skip** (don't waste 10-15 s settling during clouds/dew/aperture-blocked per §51 integration). Defaults: RA+Dec random pattern, settle 1.5 px for 10 s, settle timeout 60 s. Cross-meridian-flip behavior automatic via PHD2 auto-restore (per §58.4). No-guider fallback in v0.0.1 = disabled with one-time notification (direct-mount-pulse dither deferred to v0.1.0). Auto-reduce cadence on persistent timeout failures (graceful degradation). Wizard reduced to 2 dither questions, both default "auto." Per-target-type overrides via sequence templates (DSO dithers, lunar/planetary don't). Schema: 4 visible settings; settle params + diagnostic-skip in advanced disclosure. v0.1.0 paths: adaptive magnitude from walking-noise detection, per-filter cadence overrides, dither-quality scoring in §50 Stats. **Spillover §30.7** — equipment-change check fires on every subsequent profile load asking "has any gear changed?" Two-tap path for 95% no-change case; Alpaca DriverInfo auto-detection pre-checks suspicious changes; user confirms or unchecks. Invalidation matrix covers camera (dither magnitude recomputed), telescope/focal length (dither + AF + framing), focuser (Smart Focus + backlash), filter wheel (offsets + flat library), mount + pier/tripod/dovetail (§58 first-flip-confirm reset), guider (PHD2 cal). Banner on main app shell shows what's stale; lazy recalibration on next use. Calibration state stored in profile's `calibration_state` block. NINA has no equivalent — users discover stale calibrations the hard way.

**Operational features referenced but unspec'd:**

- [x] ~~**Live view / loop imaging mode**~~ → **Resolved §64**. NINA's Live View is driver-specific (QHY native video mode, Canon EOS Live View) and unavailable for Alpaca cameras — `Generic`/ASCOM/Alpaca all return `CanShowLiveView => false`. ASCOM `IVideo` is deprecated. ARA implements Live View from scratch as a server-side loop of short normal captures via standard Alpaca `ICamera`, with previews delivered as single-overwrite tmpfile JPEG + cache-busted URL. Cadence ~1 frame per 2-4 s (bounded by exposure + Alpaca readout) — adequate for framing/focus-check, inadequate for planetary (deferred to v0.1.0 with a custom AlpacaBridge video extension since IVideo is dead). Scope: continuous loop, no FITS, no library entry, mid-loop settings change aborts in-flight exposure (same pattern as Stop). **Save Current Frame** promotes the next finished frame through the normal capture pipeline (FITS write, library row, `frame.complete` event); tagged `capture_source = "live_view"`, routed to synthetic `live-view-<date>` session by default to keep Stats clean. Hard upper bound: 30s exposure. Stop is immediate via `AbortExposure` (~< 100 ms). Auto-stop on sequence start / plate-solve / AF / PA / Take One / camera disconnect / WILMA idle > 5 min. **No §51 diagnostics on live frames** (interactive with human present, not session-integrity workflow). Cooler/guider/mount/focuser/filter wheel/rotator all untouched (user manually adjusts during framing). Mobile companion (§41) is read-only — desktop-only controls. Single-session enforced via §27 single-client policy. Profile schema is 6 settings (registered in §61); wizard touchpoint = none (camera-aware defaults). v0.1.0 paths: native SDK video via custom AlpacaBridge endpoints (5-30 fps for planetary), ROI capture, SER output, live-stacking integration (folds into §55 v0.1.0 commitment), mobile full control, Bahtinov focus indicator.
- [x] ~~**Image stretching pipeline + preview API knobs**~~ → **Resolved §65** (2026-05-23). 7 stretch IDs in v0.0.1 (`auto_stf` default, `linear`, `log`, `asinh`, `sqrt`, `equalized`, `manual`). Per-profile default for Lights + automatic linear override for Dark/Bias/Flat frames (calibration frames render honestly). No per-filter overrides in v0.0.1 — single default per profile; per-filter deferred to v0.1.0 if demand emerges. All compute server-side via OpenCvSharp4 (§26); WILMA never touches FITS pixels. Capture-time default-stretch preview always exists; alt-stretches computed on demand + cached at `<frame>.preview.<stretch-id>.jpg` with 6-variant LRU cap per frame (configurable 1–20). Manual sliders ship in v0.0.1 with 200 ms debounce server round-trip — adequate UX on LAN; client-side real-time slider deferred to v0.1.0. Frame viewer (§40.5) gets a stretch picker + manual-slider panel (§65.9). Batch re-stretch: `POST /api/v1/sessions/{id}/restretch` enqueues background job, WS progress events. New API endpoints: `?stretch=` on preview, `/profiles/{id}/stretch-defaults`, `/sessions/{id}/restretch`, `/jobs/{job_id}`, `/frames/{id}/preview/variants` (flush). WS events: `frame.preview.ready`, `frame.preview.variant.{ready,evicted}`, `session.restretch.{progress,complete,failed}`. Mobile gets full stretch palette via the same `?stretch=` API (viewing capability, not editing). 10 §61 search registry entries (every knob discoverable). Eviction policy under storage pressure: alt variants evict first as recoverable cache; defaults + thumbs + FITS preserved.

**API conventions — bundled (one short section unblocks every endpoint spec):**

- [x] ~~**§60 API conventions**~~ → **Resolved §60**. RFC 7807 problem+json error bodies with ARA-specific extension fields + 422 validation `errors` map keyed by JSON-pointer. Cursor-based pagination (opaque token, `limit`/`cursor` query, `items`/`next_cursor`/`has_more` response, default 50/max 500, `total` on first page only where useful). Request size caps: 10 MB JSON / 100 MB multipart / 64 KB WS, env-var overrides for the first two. `GET /api/v1/server/state` defined as the UI-rehydrate snapshot with a `ws_resume_token` enabling 1-hour WS-event replay on reconnect. `Idempotency-Key` header on mutating endpoints (24h dedup, 409 on body mismatch); required on sequence start, emergency stop, polar-align, server update, backup restore. Rate limiting minimal in v0.0.1 (token-retry only); per-endpoint limits deferred to v0.1.0 when remote-access mode arrives.

**Pi-side architecture gaps:**

- [x] ~~**Concurrency model on Pi during a session**~~ → **Resolved §66** (2026-05-23). Simplified design after user pushback ("we are over complicating this — DSO exposures are 30–900 s, processing takes 1–3 s, headroom is massive"). v0.0.1 ships: 1 image-processor worker (not 2), 1 worker per pool except REST handlers (ASP.NET pool), bounded queues only where memory leaks matter (image processor 10 jobs / alt-stretch 8 / WS per-client 256 / backup stream 1 / diagnostics 3 / notifications 64; SQLite writer unbounded as critical path), silent `capture.backpressure` WS event with no notification (only emitted under storage problems, never under DSO workloads). Standard .NET thread priorities (capture AboveNormal, alt-stretch/diagnostics BelowNormal, backup stream Lowest, stats aggregator Idle). Storage-slow cascade self-heals via §66.6 USB-SSD upgrade prompt. Peak RSS ~1 GB on Pi 4 (3 GB headroom). Telemetry is log-only in v0.0.1 — `/api/v1/server/internal-state` + Stats "Pi performance" panel deferred to v0.1.0 (operational value low under DSO workloads where backpressure essentially never fires). **Coupled architectural decision baked as §18.J**: ARA's imaging scope is permanently DSO + comets only, planetary/lunar lucky-imaging is out of scope (not deferred) because Alpaca has no video API — same architectural limit NINA hits. Cleanup propagated through §38.7 (lunar.json + planetary.json dropped, comet.json added), §62 (short-exposure dither examples reworded), §64 (v0.1.0 video extension aspirations removed; SER / ROI / native-video marked permanently out), §55 (planetary moved to "out of scope indefinitely"), §36 (cleanup of planetary aside).
- [x] ~~**Power loss durability**~~ → **Resolved §28.6–§28.11** (2026-05-23). SQLite hardened with `journal_mode=WAL` + `synchronous=NORMAL` + `busy_timeout=5000` + manual `wal_checkpoint(PASSIVE)` at session boundaries (modern standard; zero corruption guarantee; only the last few seconds of WAL transactions vulnerable on power loss, and those auto-heal via the orphan scan because corresponding FITS files persist). FITS write pipeline now uses atomic-rename: write to `<frame>.fits.tmp` → `fsync()` file → atomic rename → `fsync()` parent directory → DB row insert. Same pattern for preview JPEGs + thumbnails + profile saves + sequence saves. fsync overhead is < 1% of typical exposure time on USB SSD (5–200 ms vs 60–300 s exposures) — effectively free; the §29 USB-mandatory + quality-SSD recommendation reinforces this. Startup performs filesystem audit before §28.2 recovery: mount check, ext4-type check (hard refuse on non-ext4), `.tmp` sweep (older than 5 min deleted), orphan FITS scan (re-insert into DB by parsing FITS header, emit `frame.recovered_orphan` WS event silently per the bake decision — no banner, no quarantine, no per-frame prompt). DEPLOY.md fstab guidance updated with `data=ordered,noatime,errors=remount-ro` mount options; ext4 reformat step changed from "optional" to "REQUIRED" with rationale. UPS advisory added (DEPLOY.md "Recommended hardware" section) — Geekworm X728 / PiJuice / 12V HATs listed; not required for v0.0.1 (sequences work without UPS, in-flight exposures just lost on power loss). v0.1.0 paths deferred: `durability_mode` config knob (paranoid/balanced/fast), active power-loss detection via UPS GPIO, background FITS checksum scrubbing, multi-disk RAID. **Net property:** no partial FITS appears under real name; no orphan FITS ever invisible to library; no SQLite corruption possible; max data loss = the single exposure actively integrating when power died.
- [x] ~~**PHD2 / openastro-phd2 lifecycle**~~ → **Resolved §63**. User flagged mid-spec that openastro-phd2 is already a designed-and-implemented fork at `github.com/open-astro/openastro-phd2` with the systemd lifecycle, headless mode (`xvfb-run -a ... --headless --headless-auto-connect`), 80+ documented JSON-RPC methods, profile management RPCs, `build_dark_library` + `build_defect_map_darks`, equipment selection RPCs (Alpaca + INDI + ASCOM COM transports), Alpaca discovery RPCs, algorithm-param RPCs, and a transitional .deb path from the old `phd2-alpaca` name. §63 aligns ARA Core's integration with the actual fork rather than re-inventing it. **Key behaviors baked:** systemd unit owned by the openastro-phd2 .deb (not ARA's job to ship one); user `openastro-phd2` + working dir `/var/lib/openastro-phd2`; `RestartSec=3`; ARA layers crash detection + hung-process detection + auto-restart backoff (1/5/15/30/60/120 s) on top of systemd; per-ARA-profile to PHD2-profile mapping (`ara-<slug>`) via `create_profile` / `set_profile_by_name`; full profile push from wizard (camera + mount + FL + algorithm params) via `set_profile_setup` + `set_selected_*` + `set_algo_param`; **critical precondition** that PHD2 rejects setters while equipment connected — ARA wraps `set_connected(false)` → push → `set_connected(true)` as atomic operation; dark library + defect map build from wizard with cover-the-scope modal flow; event stream ingestion via persistent TCP connection (NOT log-file tailing — the JSON-RPC socket carries events); graceful degradation when openastro-phd2 not installed; equipment-change pipeline from §30.7 invokes the disconnect-update-reconnect sequence; PHD2 version detection on connect (fork vs upstream PHD2 warning + continue). **Moved from v0.1.0 to v0.0.1:** profile push + dark library + per-profile mapping (no longer "deferred" — all already implemented in the fork; ARA just calls the documented RPCs). v0.1.0 paths: WILMA-pushed openastro-phd2 binary updates (§33.6, §55.1), AI-driven calibration tuning, multi-guider support, advanced algorithm tuner UI.
- [x] ~~**Storage permissions bootstrap**~~ → **Resolved §29.1.1 + §29.1.3 + §29.1.4** (2026-05-23). Server-side helper pattern, matching §33.5's `update.sh` approach. .deb postinst (§34.3) installs `/opt/openastroara/scripts/configure-storage.sh` + sudoers drop-in granting the `openastroara` user passwordless invocation of update.sh and configure-storage.sh only (narrow scope; system user has no shell, isn't interactively reachable). WILMA "Configure storage" flow calls `POST /api/v1/server/storage/configure {uuid}` → server invokes helper via sudo → helper validates UUID → validates ext4 → creates `/media/openastroara` → appends idempotent fstab line with §28.9 durability mount options (`data=ordered,noatime,errors=remount-ro,nofail,x-systemd.device-timeout=10`) → mounts → chowns to `openastroara:openastroara`. On non-ext4: 422 `{code:"not_ext4", fs:"exfat"}` surfaces in-app reformat flow (§29.1.3) requiring user to type the drive's label exactly to authorize wipe (defense in depth — server and helper independently validate the label match before `mkfs.ext4 -F -L <label>` runs). Helper has 9 distinct exit codes (uuid_not_found, not_ext4, label_mismatch, device_busy, mkfs_failed, mount_failed, chown_failed, fstab_write_failed) each mapped to a structured `code` field in the 422 response for granular WILMA messaging. §28.9 strengthened with two layers of ext4 enforcement (configure-time helper validation + server-startup validation as defense in depth against fstab edits / manual remounts). §29.7 DEPLOY.md restructured: automatic-via-WILMA is now the default path; manual fstab steps positioned as advanced-user fallback for immutable distros / custom partitioning / troubleshooting. §29.8 added 5 search registry entries (`storage.configure`, `storage.reformat`, `storage.switch_drive`, `storage.eject`, `storage.troubleshoot`). User experience is now: plug drive in → pick in WILMA → done. No SSH, no terminal, no manual fstab editing required.

**Security:**

- [x] ~~**Explicit threat model paragraph (§9 or §17)**~~ → **Resolved §67** (2026-05-23). Outcome was bigger than the gap implied. Per user ("no authentication like Alpaca — we are imagers not hackers of imaging rigs. Multiple people hit dark sky parties with ZWO ASIAir and no one is hacking one another"), the gap question "what does token auth protect" was answered by **removing the token auth entirely** to match Alpaca + ASIAir's trusted-LAN posture. Baked §67 covering: threat model (imagers are peers, not adversaries; star parties prove the threat model), defense without auth (mDNS client-initiated discovery + single-client policy + confirmation UX on destructive ops + SHA-256 binary integrity + no remote access by default + local-logs-only telemetry), deployment recommendations (home / star party / public Wi-Fi / remote observatory), v0.1.0 opt-in remote-access mode for over-internet imaging (where auth + TLS re-enter), anti-features list (no auth, no TLS, no audit logging, no AV, no driver scanning, no signing v0.0.1), responsible-disclosure channel, search registry entries. Stripped token/auth from ~20 references across the playbook: §2 (target stack auth row), §9 (Phase 5 API contract), §12 (Flutter first-run flow), §19 (secrets handling forward-looking framing), §24 + Phase 15 demo flow, §25 (token-reset button removed from app shell), §27 (connect endpoint), §29.1 (token file dropped from SD-card listing), §30.1/30.3 (launch flow simplified — no "no saved token" branch), §30.6 (Token management → Server connection management), §33.3/33.4 (binary update uses SHA-256 as integrity gate without auth), §34.3 (postinst drops token generation step), §34.6 (DEPLOY.md drops journalctl-token-retrieval), §41 (mobile companion aligned), §44 (backup-stream/claim no auth), §49 (Swagger UI all-open + "Try It Out" needs no setup), §60.4 (ws_resume_token clarified as replay cursor, not auth), §60.6 (rate limiting simplified — single-client policy is the limit), §64 (stale "All endpoints require token auth per §9" line fixed). User experience: plug Pi in → mDNS → connect, no token entry ever. v0.1.0 remote-access mode (per §67.4) reintroduces auth + TLS, but ONLY on opt-in remote interface — default LAN deployment stays open forever.

**Scope rescoping (be honest before the AI commits):**

- [x] ~~**§36 Sky Atlas — honest v0.0.1 rescope**~~ → **Resolved §36** (2026-05-23). Rescope went in the opposite direction from this gap's prediction: instead of cutting features, restructured *delivery*. Installer slims from ~1 GB to ~50 MB by moving all HiPS surveys, supplementary star catalogs (Tycho-2 / GAIA / UCAC / HD / HIP / Bayer+Flamsteed), pre-baked DSO target thumbnails, and full DE440 ephemerides into a new four-tab **Data Manager** (§36.2) — SkySafari-style on-demand downloader. All 21 surveys, Tonight's Sky planetarium, comet motion trails, and Aladin Lite integration stay in v0.0.1 as before. New flows added to drive coverage: wizard becomes **mandatory and the only profile-creation path** (§37 preamble + §30.4 minimal modal removed); wizard Screen 17 (§37.6) defaults-checks a recommended download set keyed to focal length (§36.10) + always-checked Famous Targets + Star Catalogs preset + Full DE440; downloads run in background while wizard completes; §36.13 sky-data-missing banner (sharing §30.7.3 shell) surfaces if recommended downloads weren't completed. No truncated-DE440 variant ships — user explicitly wanted the full files because "Aladin is the differentiator vs NINA." Empty-download fallback documented (Aladin shows blank-with-grid + bundled DSO catalogs + working search; framing degraded, plate-solving still works). Storage schema extended (`sky_data_state` block in `calibration_state`). New API endpoints: `/api/v1/data-manager/*`, `/api/v1/profiles/{id}/sky-data-recommendations`, `/api/v1/profiles/{id}/sky-data-banner/dismiss`. New WS events: `data_manager.download.{progress,complete,failed}`. §61 search registry entries added (§36.14).
- [ ] **Phase 12 split (12a/12b/12c)** — §3 phase plan packs ~50% of client engineering into Phase 12: app shell + 7 tabs (Imaging/Framing/Sequencer/Sky Atlas/Image Library/Stats/Settings) + a11y baseline + disconnect modal + safety UI + bug report flow + mobile companion shell selection. As specified the AI cannot make this gate green in one pass. Split into 12a (shell + first-run + Imaging + Equipment), 12b (Sequencer + Framing + Settings), 12c (Sky Atlas + Image Library + Stats + mobile companion).
- [x] ~~**§51 real-time diagnostics default mode**~~ → **Resolved §51.5 + §51.11 + §51.12** (2026-05-23). Chose option (a) from the gap's proposal: ship `notify_only` as v0.0.1 default + opt-in to `balanced` / `aggressive` / `conservative` via Settings → Diagnostics. Diagnostic decision tree (all 12 patterns) still runs on every frame; Health Indicator + Diagnostic Panel + per-frame FITS metadata enrichment all still surface in real time; ARA simply doesn't take autonomous corrective action (auto-refocus, auto-pause, auto-skip, re-slew) without explicit user policy change. Three converging reasons: (1) hard-coded thresholds (40% star-drop, HFR 1.5×, etc.) aren't per-user-calibrated and v0.1.0 §51.9 is when calibration ships; (2) matches NINA (no smart corrections) + ASIAir (hardware-only auto-pauses) ecosystem posture; (3) first-impression risk — a single false-positive auto-pause during a clean Bortle 1 session would teach the user to distrust + disable the feature entirely. Promotion path documented in §51.11: `balanced` becomes the v0.0.2 / v0.1.0 default once per-user calibration ships and telemetry confirms calibrated thresholds rarely false-positive. **No wizard screen added** — Settings-only picker keeps the wizard at 18 screens (the user explicitly chose to keep wizard mandatory; adding screens has a real time cost). §51.12 NEW: 5 search registry entries make the picker discoverable via WILMA ⌘K search.

---

## Tier 2 (late additions — from second review pass, 2026-05-21)

Smaller gaps and edge cases. Worth covering but won't crash the port if briefly described.

- [x] ~~**Flutter WebView on Linux desktop verification**~~ → **Resolved §36.10.5** (2026-05-23). Phase 11 verification step + external-browser fallback. WebView fails on Linux desktop → WILMA shows banner with [Open in browser] + [Copy URL] options; localhost shelf server (§36.6) serves bundled Aladin Lite JS + downloaded HiPS tiles. **Offline-aware** — fallback works without internet as long as Aladin JS bundled (yes, in §36.1 installer base ~5 MB) + surveys downloaded; no surveys + no internet shows same blank-with-grid + bundled DSO overlays state. Mac/Windows/iOS/Android keep embedded WebView. Help-wiki entry documents Linux WebView dep install (libwebkit2gtk + libgtk). v0.2.0 native Flutter sky-renderer eliminates Linux-only issue entirely.
- [x] ~~**Camera cooler warmup at session end**~~ → **Resolved §28.13** (2026-05-23). New profile setting `cooler_warmup_on_session_end`: 3 modes (`off` default for modern CMOS / `ramp` at 1°C/min for cooled CCDs + humid environments / `immediate`). Wizard Camera screen (§37.3 Screen 5) updated. Post-sequence cooldown phase added when `ramp` selected (banner shows ETA; [Skip warmup] available). Profile schema extended. 2 §61 search registry entries. v0.1.0 paths: auto-detect CCD vs CMOS via Alpaca `CameraType`; humidity-aware override (force ramp on humid nights).
- [x] ~~**"Paused sequence" semantics**~~ → **Resolved §28.12** (2026-05-23). 15-row matrix documenting what runs vs holds vs aborts during pause: cooler keeps running (temp stability), guider keeps running (mount settled), mount keeps tracking (target centered), filter/focuser/rotator/dome hold positions, in-flight exposure aborts (per §28.7 atomic-write — no torn file), WS broadcasts + diagnostics + notifications + backup stream + stats aggregator + REST API all continue. Resume semantics + pause-vs-abort-vs-stop distinctions documented.
- [x] ~~**DST / timezone handling**~~ → **Resolved §31.5** (2026-05-23). Explicit DST + TZ policy paragraph confirms: server UTC throughout (timestamps, DB writes, log timestamps, FITS DATE-OBS, filename templates $$DATE$$/$$DATETIME$$); $$DATEMINUS12$$ already TZ-correct for night observation; astronomical twilight uses lat/long (TZ-independent by construction); USB GPS feeds UTC per spec; manual entry on WILMA accepts local + converts to UTC; client displays local via Flutter `intl` (DST automatic). Edge cases documented (DST transitions during sequence, user travels mid-session, no-time-source mode). No additional user config — handled automatically.
- [x] ~~**Mosaic across RA wrap (0h/24h)**~~ → **Resolved §47.3** (2026-05-23). Confirmed NINA's `Coordinates` class normalizes panel-center RA via `((computed_ra + 360) % 360) % 360` — handles 0h/24h boundary correctly. Worked example for a 6-panel mosaic centered at RA 23h45m documented. §14.5 integration test suite includes a mosaic-across-RA-wrap test case (e.g., 5×5 mosaic centered at RA 23h55m) to prevent regression.
- [x] ~~**§23 bulk-rename sed safety**~~ → **Resolved §23** (2026-05-23). Updated one-liner: explicit `-not -path` excludes for `LICENSE*`, `NOTICE*`, `AUTHORS*`; dry-run pattern (find files first, sample changes, prompt for confirmation before applying); only operates on `*.cs` + `*.csproj`. Added warning paragraph noting license headers + user-visible strings (error messages, log strings, resource files) get edited deliberately — Phase 0.5 sub-PR 0.5f covers that deliberate string sweep per `COMMIT-PR-RULES.md`.

---

## Tier 1 (late additions — from third review pass, 2026-05-23)

Found during a third end-to-end review of the playbook + design folder. Spot-checks against common gap categories (process supervision, log retention, schema migration, dep contracts, monitoring hooks) surfaced items the AI would either skip or guess at during the port. Spec before turning the AI loose on Phase 0.5.

- [x] ~~**`apt upgrade` mid-session safety**~~ → **Resolved §34.7** (2026-05-23). Server-managed lock file (`/var/run/openastroara/sequence.lock`, written at sequence start, heartbeat every 30 s, removed at stop/abort/complete) + postinst defer (`deb-systemd-invoke restart` skipped if lock present + heartbeat fresh within 5 min) + `.needs-restart` flag the server surfaces via `pending_restart` in `/api/v1/server/state` + WILMA banner reusing §30.7.3 shell with [Restart now] / [Restart when sequence ends] / [View release notes] actions. Stale-lock detection (5 min heartbeat) prevents indefinite deferral if server crashed. Three new endpoints (`POST /server/restart`, `POST /server/restart-on-idle`, `GET /server/release-notes`), two new WS events (`server.pending_restart`, `server.restart_imminent`), three §61 search registry entries, three §14.5 integration test cases. tmpfs created at boot via `/usr/lib/tmpfiles.d/openastroara.conf`. Edge cases documented (stacked apt upgrades, manual systemctl restart, headless Pi). User agency wins — no auto-force-restart even on 24h+ deferrals.
- [x] ~~**Log rotation / retention policy**~~ → **Resolved §29.9** (2026-05-23). logrotate drop-in `/etc/logrotate.d/openastroara` installed by .deb postinst: daily rotation, 30-day retention, 100 MB max per file, gzip with `delaycompress`, `copytruncate` so Serilog's FD survives. Disk budget ~3 GB worst case / ~200–500 MB realistic. Log pressure handling: below 500 MB free → drop to Info level + `storage.log_pressure` WS event (warning); below 100 MB → drop to Warning + force logrotate + critical event. Logs live on SD card (not USB) — keeps log writes off data-write path + survives USB unmount. Bug-report zip (§54) updated to "10–500 MB". systemd-journal coexists with optional `SystemMaxUse=1G` recommendation in DEPLOY.md. Cross-platform table covers Pi (logrotate) vs dev (Serilog in-process retention). WILMA Settings → Storage → Logs sub-panel shows current volume + [Force rotate] + [Download current log]. No user-facing rotation knobs in v0.0.1 (Linux defaults proven; v0.1.0 could expose). New endpoint `POST /api/v1/server/logs/rotate`, 2 §61 entries (`storage.logs.location`, `storage.logs.retention`), 3 §14.5 test cases.
- [x] ~~**SQLite schema migration across releases**~~ → **Resolved §28.14** (2026-05-23). EF Core migrations (Microsoft.EntityFrameworkCore.Sqlite + provider, ~10 MB runtime cost) + **mandatory pre-migration backup** policy: WAL checkpoint → SQLite online backup API → integrity check → apply migrations → on failure auto-restore. Backups land at `/media/openastroara/.araback/migrations/data_pre-v<from>-to-v<to>_<iso8601>.db`, last 5 retained. Forward-compat: server refuses to start if DB schema is ahead of binary (`schema_ahead_of_binary` error); restore-from-backup path is manual DEPLOY.md SSH in v0.0.1, WILMA UI deferred to v0.1.0. No down-migrations generated (one-way only — restore-backup-then-downgrade-binary is the rollback path). 3 new endpoints (`/server/migrations/history`, `/server/migrations/backups`, `/server/restore-from-backup` returns 501 in v0.0.1), 3 new WS events (`server.migrating_database`, `server.migration_complete`, `server.migration_failed`), 2 §61 entries, 5 §14.1 integration test cases (apply-on-startup, failure-restores-backup, forward-compat-refuses-unknown, integrity-fail-aborts, retention-keeps-5). Cross-platform dev paths use `~/Library/Application Support/OpenAstroAra/Backups/migrations/` (macOS) and `%LOCALAPPDATA%\OpenAstroAra\Backups\migrations\` (Windows). Workflow: `dotnet ef migrations add <Name>` → hand-review SQL → integration test → commit.
- [x] ~~**AlpacaBridge contract surface**~~ → **Resolved §68** (2026-05-23). Thin section that pins ARA-specific assumptions only; leans on ASCOM Alpaca spec at ascom-standards.org for protocol details. Min version `alpaca-bridge >= 1.2.0`, three-band version gate (>=1.5.0 accept / 1.2-1.5 accept-with-banner / <1.2 hard-refuse with 503 `alpaca_bridge_outdated` blocking Equipment endpoints). Handshake: discover (UDP 32227 or static) → probe `/version` → version gate → list `/management/v1/configureddevices`. Capability detection is per-device via standard Alpaca interface flags — ARA doesn't redocument ASCOM. Missing-bridge UX: wizard Screen 2 shows `sudo apt install alpaca-bridge` + [Retry detection], cannot advance without successful handshake; mid-session missing surfaces critical notification + diagnostic. Upgrade path: apt v0.0.1 (Recommends pull-in, subject to §34.7 sequence-lock during apt restart), WILMA-push v0.1.0 per §33.6. 2 §61 entries (`equipment.alpacabridge.version`, `equipment.alpacabridge.troubleshoot`), 3 §14.5 integration test cases (below-min blocks, warn-band emits banner, missing blocks wizard advance), cross-refs to §2/§34/§37/§52/§55.

## Tier 2 (late additions — from third review pass, 2026-05-23)

- [x] ~~**Health-check HTTP endpoint**~~ → **Resolved §60.8** (2026-05-23). Two endpoints at the root (not under `/api/v1/` — well-known names for monitors): `GET /healthz` returns 200 `ok` text if the HTTP handler runs (<10 ms, no DB hit) — pure liveness; `GET /readyz` runs 5 checks (database `SELECT 1`, storage stat+1-byte test write to `.araback/.readyz_probe`, cached AlpacaBridge handshake from §68 within last 30 s, `.needs-restart` flag per §34.7, `__EFMigrationsHistory` consistency per §28.14) and returns 200 JSON or 503 with per-check `detail` strings. `pending_restart` doesn't push to 503 — reports nested status but stays "ready" (server is working on stale binary). No auth (matches §67); both `Cache-Control: no-store`; logged at Trace level so logrotate isn't drowned. ~30 LOC C# implementation, AlpacaBridge handshake cache shared with §68.1. 5 §14.1 integration tests, 1 §61 entry (`monitoring.health_endpoints`), DEPLOY.md monitoring sidebar with Prometheus blackbox example.
- [x] ~~**In-app changelog / "What's new" viewer**~~ → **Resolved §33.7** (2026-05-23). Bundled `CHANGELOG.md` (Keep-a-Changelog 1.1.0 format) installed by .deb at `/opt/openastroara/CHANGELOG.md`. Server endpoint `GET /api/v1/server/release-notes?version=X.Y.Z` (or `?since=X.Y.Z` for skipped-version aggregation) parses + returns Markdown. WILMA tracks `last_seen_server_version` per-server-UUID in `flutter_secure_storage`; on connect when current > last_seen, shows modal with new content. Modal supports [Close] / [Don't show again] / [View full changelog]. Skipped-versions aggregated newest-first as collapsible sections (capped at 20). §-references render as clickable deep-links via §61. Help → Release notes is always-available surface for [Don't show again]-pressed users. Fresh-WILMA first-connect silently sets last_seen without modal. Release process: PRs append to `## [Unreleased]`; tag-cut renames to versioned section + fresh placeholder; pre-PR gate (§14.4) enforces CHANGELOG.md entry for user-visible changes. Wired to both §33.3 (WILMA-push) and §34.7 (apt-upgrade's [View release notes] button). 2 §61 entries (`app.changelog.view`, `app.changelog.suppress`), 4 §14.2 widget test cases.

## Tier 3 (late additions — from third review pass, 2026-05-23)

- [x] ~~**In-app contextual help framework**~~ → **Resolved §69** (2026-05-23). Tooltip system with registry mirroring §61's enforcement model. `lib/help/registry.dart` holds `Help(key, title, body, learnMoreUrl, relatedHelpKeys, relatedSettings)` entries. Widgets reference via `helpKey:` parameter; `HelpIcon` widget renders ⓘ glyph (§53 a11y symbol-+-color), hover/long-press shows tooltip with title + first sentence, tap opens modal sheet with full Markdown body + [Learn more] + related-chips clickable to §61 deep-links or other help entries. Default is **no tooltip** — opt-in per §69.1 scope rules (label can't carry full meaning, non-obvious effect, hidden interactions, novice-confusion risk). Four enforcement layers identical to settings-registry gate (pre-commit hook `check-help-registry.mjs` / CI / PR template checkbox / CodeRabbit path instructions); script fails on missing keys, empty bodies, bodies < 50 chars, duplicate keys, wrong-domain learnMoreUrls. Complementary per-screen [Learn more] link in every major screen header opens openastro.net wiki at per-screen anchor (workflow + concept explanations). `app.help.disable_tooltips` setting lets power users globally suppress icons. 2 §61 entries (`app.help.search`, `app.help.disable_tooltips`), 5 §14.2 widget tests. COMMIT-PR-RULES.md updated with parallel help-registry gate section. Phase 12 activation alongside settings gate; 12h consolidates initial bulk-population.
- [ ] **Profile / sequence community sharing** — §43 covers same-user backup/restore; nothing for "I want to share my profile with a friend." Calibration is equipment-bound (calibration_state per §30.7), so a share-export needs an equipment-stripped variant. Sequences are more portable (already in §38 format). Possible v0.1.0 punt with a brief §55 entry; possible v0.0.1 if it's just a "strip calibration_state + export JSON" button. Not even listed in §55 currently.
- [ ] **Multi-Pi / multi-rig controlled from one WILMA** — single-client policy (§27) is per-server, but observatory users with 2 rigs on 2 Pis aren't addressed. Possible v0.1.0/v0.2.0 punt. Decision needed: explicit "out of scope until vX" entry in §55 with rationale, OR scope spec. Cross-cuts with §32 reconnect (now per-Pi), §50 Stats (cross-rig rollups), §46 notifications (per-Pi vs aggregated).
- [ ] **WILMA mobile distribution channel** — §41 covers mobile companion scope; §18.G says "Flutter handles client per platform"; but App Store / Play Store / TestFlight pipeline isn't planned anywhere. Decision needed for v0.0.1 mobile path: official Play Store + App Store listings (requires developer accounts + review processes + ongoing fees), OR sideload-via-APK + TestFlight, OR defer mobile to v0.1.0 entirely. Cross-cuts with §18.F code signing (currently "ship unsigned, revisit when funded") — App Store needs signing.

---

## Tier 1 — Open, high-impact

These would actively bite real users on day one if missing. Discuss + bake before turning the AI loose on Phase 0.5.

- [x] ~~**Calibration frames management**~~ → **Resolved §39**. Light/Dark/Bias/Flat sequence types preserved; capture-only philosophy (no calibration at capture); rich FITS session metadata; session library API; **session-metadata-driven matching-flats workflow** (pick past session → server generates flat sequence matching exact equipment state); temp-mismatch handling; dark library auto-generation; calibration library browsing in WILMA.
- [x] ~~**Captured-image library workflow**~~ → **Resolved §40 + §41**. By-session organization with by-target rollups; two-tier preview JPEGs (thumb + full-res for pixel peep); rate/tag/notes; bulk operations; "Resume Target" workflow for multi-year project alignment (records plate-solve + rotator for reproducibility); auto-rating + HFR drift pattern detection ("clouds, not focus"); OS file-association handoff for FITS export. §41 adds mobile companion mode scope: phone/tablet does monitoring + library viewing + pinch-to-zoom + GPS push + emergency stop, NOT sequence editing or full sky atlas (desktop-only).
- [x] ~~**Sequence file format + NINA `.json` import**~~ → **Resolved §38**. NINA schema verbatim + `schemaVersion: "openastroara-sequence-v1"`; OpenAPI-documented; import endpoint with equipment-remap + unsupported-instruction handling; 4 bundled starter templates; sequence-template variable system.
- [x] ~~**Hardware fault recovery (per-equipment)**~~ → **Resolved §42**. Universal retry-then-action pattern (Continue/Notify/Pause/Abort+park), per-fault configurable in profile safety policies. 16-row fault matrix covering camera/mount/focuser/EFW/rotator/guider/plate-solve/ASTAP/dew-heater/switch-value-mismatch/dew-formation. Hot-reconnect with backoff schedule. Switch value tolerance for PWM/dimmable devices. Faults logged to session DB and visible per-frame in image library. NINA's retry semantics preserved; ARA adds value-tolerance + dew detection + unified retry pattern.
- [x] ~~**Backup / restore of profiles + sequences**~~ → **Resolved §29 + §43 + §44**. §29 updated to make USB drive MANDATORY (SD card OS-only — protects against SD wear from sustained FITS writes). Because everything (profiles, sequences, session DB, calibration, FITS, logs) lives on USB, the drive itself is portable — pull it out, plug into another Pi, ARA resumes exactly. §43 covers four backup layers: drive-to-drive clone (primary, user-managed), **real-time stream to desktop WILMA (§44 — pull-based, throttled, capture-aware backoff)**, server-generated lightweight ZIP (profiles + sequences + DB + calibration metadata), auto-snapshots on the USB. Restore flows for same-drive-new-Pi, fresh-USB-with-ZIP, and fresh-start.

## Tier 2 — Open, medium-impact

Important workflows but won't crash anything on night-one. Can be specced after Tier 1 or in parallel.

- [x] ~~**Polar alignment routine**~~ → **Resolved §45**. TPPA dropped entirely (fragile + slow over Alpaca). Replaced with iPolar-style continuous-loop PA using main camera with optimizations: autofocus first, dark-frame caching, aggressive binning (3×3 or 4×4) for fast transfer, ~500 ms loop cadence, zooming bullseye UI in WILMA. Same math as iPolar (RA-axis vs pole offset from rotated plate solves). v0.1.0 adds native dedicated PA camera support.
- [x] ~~**Notifications system**~~ → **Resolved §46**. In-app only (no push/email/webhook for v0.0.1 — field users often have no internet). Full event catalog (~45 event kinds spanning sequence/equipment/safety/calibration/recovery/storage/time/meridian flip/PA/dew/etc.). Four severity levels with distinct UX (info → feed only; warning → toast; critical → sticky toast + chime; urgent → modal + alarm per §35.5). Per-event opt-in/out + severity override + quiet hours (urgent always delivers). Persistent SQLite log on Pi with 30-day prune for info/warning. Notification feed UI with severity icons, filter pills, action buttons. v0.1.0 paths noted: push, email, webhooks, scripting.
- [x] ~~**Mosaic / multi-panel imaging**~~ → **Resolved §47**. N×M grid built in Framing Assistant with Aladin Lite panel-overlay preview. Server computes per-panel RA/Dec via tangent-plane projection (10% default overlap, configurable). Interleaved scheduling (one frame per panel rotating per filter — balances airmass + sky conditions across all panels). Per-panel sub-targets in existing target system. Mosaic-aware Resume workflow: years-spanning projects supported — server tracks completion per panel per filter, new sessions roll up cleanly into the same mosaic. Image Library shows visual grid colored by completion. ARA captures + tags; stitching is post-processing (PixInsight/Siril) per user's choice.
- [x] ~~**Auto-flats / auto-darks**~~ → **Resolved §48**. Sequence-start prompt asks user: capture flats tonight (panel or sky) or capture them later via §39.5 session-matching workflow. User preference saved per profile ("ask each time" default, or "always panel", "always sky", "never"). `FlatPanelFlats` + `SkyFlats` instructions preserved from NINA. Dark library is NOT in the prompt (different scope — built separately by user as a multi-hour overnight task using `DarkLibraryInstruction`). Bias library similarly user-initiated.
- [x] ~~**API documentation serving**~~ → **Resolved §49**. Swagger UI v5 (matching ASCOM Alpaca's convention — both ecosystems use the same tool). Served open (no token) at `/api/v1/docs`. Raw OpenAPI 3.1 spec at `/api/v1/openapi.yaml` + `.json`. Swagger UI's "Authorize" lets power users paste their token to test auth'd endpoints. ASCOM-style CSS customizations.
- [x] ~~**Astrometry.net star index downloads**~~ → **Deferred to v0.1.0** (§18.I updated). ASTAP covers 99% of solving needs; astrometry.net adds another binary + index-file management workflow not worth v0.0.1 complexity. Phase 8 strips astrometry.net call sites from inherited NINA code. v0.1.0 may add it back if demand emerges, with a Survey-Manager-style UI for 4100/4200/5000-series index downloads.

## Tier 3 — Polish / deferred

- [x] ~~**Sequence templates**~~ → **Resolved §38.7**. v0.0.1 ships 4: `lrgb-dso.json`, `narrowband-shoo.json`, `lunar.json`, `planetary.json`. Additional templates (comet wide-field, mosaic-specific, etc.) added in v0.1.0+ as the community shapes preferences.
- [x] ~~**Session analytics**~~ → **Resolved §50**. Full analytics dashboard as a flagship differentiator vs NINA. Stats top-level tab with 11 sub-views: Overview tiles, Targets rollup + per-target detail, Focus & Temperature (HFR-vs-temp scatter + regression per filter), Guiding (RMS trends + altitude/wind correlations), Frame Quality (composite quality score + Best Frames auto-sort), Equipment Health (cooler-power trend, fault rates), Session Efficiency (time-breakdown), Conditions (weather/lunar correlations), Achievements/milestones, Calendar heatmap, Exports (CSV/PDF/Astrobin). Composite quality score formula specced (HFR + star count + roundness + ADU + eccentricity, profile-weighted). Materialized daily aggregates for performance. All local — no telemetry. v0.0.1 ships Overview, Targets, Focus&Temp, Guiding, Frame Quality, Best Frames, Composite Score, Calendar, CSV export, all API endpoints. Equipment Health, Session Efficiency, Conditions, Achievements, PDF/Astrobin exports → v0.0.2/v0.1.0.
- [x] ~~**Mount-specific behavior profiles**~~ → **Resolved §52 (and pivoted)**. The original framing was wrong — Alpaca's ConformU certification eliminates most mount quirks at the protocol level, so ARA doesn't need a brand-profile system. Instead §52 commits to: Alpaca-only-forever (permanent architectural decision; INDI/INDIGO never native, bridges required); feature detection via Alpaca capability flags (CanFindHome, AxisRates, SlewSettleTime, etc.); sensible generic defaults in wizard (no brand-specific code); optional first-connect conformance check that surfaces driver bugs to upstream rather than working around them in ARA. NINA's brand-specific mount code is stripped in Phase 8.
- [x] ~~**Multi-user / read-only spectators**~~ → **Deferred to v0.1.0** (§27.4 already notes this). Single-client policy is correct for v0.0.1; spectator mode adds complexity (auth roles, read-only API surface, UI mode toggle) that isn't needed yet.
- [x] ~~**Live stacking**~~ → **Committed v0.1.0 feature** (not optional — explicit roadmap commitment). Star registration + sigma-clipped running stack + live preview during integration. Provides EAA (Electronically Assisted Astronomy) and "is this target worth tonight" instant feedback. ASIAir/SharpCap parity. Engineering work: real-time star detection, frame alignment, calibration application, memory management on Pi.
- [x] ~~**Equipment scripting / custom hooks**~~ → **Deferred to v0.1.0+** (folds into the plugin SDK design from §10). Power-user feature for custom integrations (dome control via GPIO scripts, webhook firing on frame complete, ancillary-equipment automation). v0.0.1 users who need this run external automation (cron, separate scripts watching the filesystem for new FITS files). v0.1.0 design pass introduces hooks + plugin SDK together as a cohesive extensibility layer.
- [x] ~~**Pi RTC hardware option**~~ → **DEPLOY.md documentation item.** Documented as optional hardware add-on (DS3231 / PiRTC modules); avoids the §31 time-sync waterfall when present. AI writes this into DEPLOY.md during the Phase 11 documentation pass.
- [x] ~~**Pre-built RPi OS image**~~ → **Deferred to v0.2.0** (per §34 — .deb on apt.openastro.net is v0.0.1 path; pre-flashed image is a polish-tier "make onboarding zero-friction" feature requiring CI image-build pipeline).
- [x] ~~**AlpacaBridge + openastro-phd2 WILMA-push updates**~~ → **Deferred to v0.1.0** (§33.6 already specs this). Same atomic-swap + rollback pattern; new endpoints per component; bundled binaries grow WILMA app size another ~50-100 MB combined.
- [ ] **AlpacaBridge enhancement: switch power levels for RPi devices** — outside ARA Core scope (AlpacaBridge-side work). Support PWM / value-based switches for RPi-attached devices like dew heaters with variable output, dimmable flat panels, voltage-controlled outputs. ARA Core (§42.4) already consumes the full Alpaca `ISwitch` interface so it's ready when AlpacaBridge supports it.
- [x] ~~**Bulk asteroid catalog**~~ → **Deferred to v0.1.0** (§36.8 already specs this). Targeted lookup ("Ceres", "433 Eros") works in v0.0.1; bulk MPC asteroid layer (~1.4M numbered asteroids) with smart-culling by magnitude/visibility is v0.1.0.
- [x] ~~**Survey downloader polish**~~ → **Deferred to v0.1.0** (§36 already calls this out). v0.0.1 ships functional-but-rough single-threaded sequential downloader; v0.1.0 adds parallel + resume-across-restarts + background mobile + `If-Modified-Since` incremental updates.

## Tier 3 (late additions — deferred to v0.1.0+, from review pass)

- [x] ~~**Planetary / lunar imaging workflow specifics**~~ → **Closed won't-do** (2026-05-23). Resolved by §18.J architectural scope decision: ARA's scope is permanently DSO + comets only, planetary/lunar lucky-imaging is out of scope (not deferred). Reason: ASCOM Alpaca has no video API (`IVideo` is deprecated, unsupported by Alpaca), so 5–30 fps capture isn't possible through the protocol ARA committed to (§52). NINA has the same limitation; this isn't an ARA-specific gap. Users wanting planetary use FireCapture / SharpCap / AstroDMx with vendor-native drivers — different tool category. §38.7 templates trimmed accordingly (lunar.json + planetary.json dropped, comet.json added). §64, §55, §62, §36 cleanup propagated. §55.3 "out of scope indefinitely" updated.
- [ ] **Comet motion tracking during exposure** — comets move appreciably during long exposures (minutes). Pro tools update RA/Dec per exposure from orbital elements. We have comet catalog data (§36.9) and motion math but no motion-aware tracking during capture. v0.1.0.
- [ ] **Multi-device WILMA settings sync** — user with Mac + iPad + phone configures each separately in v0.0.1. v0.1.0 could store WILMA UI preferences server-side and sync across devices on connect.
- [ ] **TLS / remote-internet access** — §2.3 explicitly says no TLS in v0.0.1, LAN-only. Some users want to image from remote observatories over internet. v0.1.0 path: TLS termination + optional remote-access mode (with appropriate warnings); v0.0.1 documented workaround is VPN.

## Documentation — separate effort, post-Phase 15

- [ ] **README rewrite** (Open Astro front matter, lineage attribution, install quickstart, link to wiki + DEPLOY.md)
- [ ] **NOTICE.md full text** (snippet in §17.2; needs to become a real file at repo root with NINA + Aladin Lite + PI.N.S. credit + complete dep list)
- [ ] **DEPLOY.md** content (mostly drafted in §34.6 but not yet a real file)
- [ ] **User guide / "how to use ARA"** — wizard walkthrough, sky atlas tour, sequence builder basics, safety policy explainer, troubleshooting
- [ ] **CONTRIBUTING.md update** for the fork (current file is NINA's; needs fork-specific content)
- [ ] **`.github/ISSUE_TEMPLATE/`** + **`.github/PULL_REQUEST_TEMPLATE.md`** for the new repo
- [ ] **API_CONTRACT.md initial content** — currently scaffolded as a tracking file in §1; will fill as endpoints are designed in Phase 5

## Things confirmed resolved (for the record)

These came up in conversation and ARE in the playbook. Listed here so we don't accidentally re-litigate.

- ✅ System.Drawing on Linux → OpenCvSharp4 (§26)
- ✅ Multi-client behavior → single-client + popup transfer (§27)
- ✅ Sequence durability + crash recovery (§28)
- ✅ Disk-space policy + storage (§29, plus WILMA-side §29.0)
- ✅ First-run + launch flow (§30)
- ✅ Time + location sync waterfall — WILMA acronym defined (§31)
- ✅ Network resilience + reconnect modal (§32)
- ✅ Version compatibility + WILMA-pushed updates (§33)
- ✅ Distribution via apt.openastro.net + .deb structure (§34)
- ✅ Safety policies — user-configurable per profile (§35)
- ✅ Sky imagery + 21-survey downloader + Tonight's Sky planetarium + slim installer + Data Manager + mandatory wizard (§36 rescoped 2026-05-23 — delivery-shift, not feature-cut)
- ✅ Profile setup wizard (18 screens, 7 stages) (§37)
- ✅ Sequence file format + NINA `.json` import (§38)
- ✅ Calibration frames + session-metadata-driven matching flats (§39)
- ✅ Captured-image library workflow + "Resume Target" multi-year alignment + HFR drift detection (§40)
- ✅ Mobile companion mode scope — phones/tablets do monitor + library + GPS-push only, no editing (§41)
- ✅ Hardware fault recovery (per-equipment) + switch value tolerance + dew detection (§42)
- ✅ Mandatory USB storage + backup/restore + drive portability across Pis (§29 updated + §43)
- ✅ Real-time backup stream from Pi to desktop WILMA (§44 — protects against mid-session USB failure)
- ✅ Polar alignment — iPolar-style continuous loop with binned main camera (§45)
- ✅ Notifications system — in-app feed with 4 severity levels + quiet hours + per-event prefs (§46)
- ✅ Mosaic / multi-panel imaging — N×M grid in Aladin, interleaved scheduling, mosaic-aware Resume across years (§47)
- ✅ Auto-flats / auto-darks + sequence-start prompt offering "capture later via §39.5" (§48)
- ✅ API documentation serving — Swagger UI matching ASCOM Alpaca's convention (§49)
- ✅ Astrometry.net star index downloads → deferred to v0.1.0 (§18.I updated)
- ✅ Session analytics + Stats dashboard (§50 — flagship NINA differentiator)
- ✅ Real-time acquisition diagnostics + smart corrections (§51 — pattern-based cause diagnosis ["clouds vs focus drift vs trees"], auto-actions, user-policy aggression dial, per-frame FITS metadata enrichment, configurable from balanced default → aggressive/conservative/notify-only)
- ✅ Alpaca-only commitment + feature-detection mount handling (§52 — INDI/INDIGO never native; bridges only)
- ✅ Equipment scripting / custom hooks → deferred to v0.1.0 (folds into plugin SDK design from §10)
- ✅ Accessibility (a11y) baseline + color-blind friendly status indicators (§53)
- ✅ Bug report submission + PII handling (§54 — review-first, four sharing modes, always-blacklisted secrets)
- ✅ Live View / Loop Imaging (§64 — ARA-built loop of standard Alpaca captures, ephemeral previews, Save Current promotion, mobile read-only)
- ✅ Image stretching pipeline + preview API (§65 — 7 stretch IDs, per-profile default + frame-type auto-override, manual sliders v0.0.1 with 200ms debounce, 6-variant LRU cache, batch re-stretch, mobile gets full palette via same API)
- ✅ Data durability + power-loss handling (§28.6–§28.11 — SQLite WAL + synchronous=NORMAL, FITS atomic-write + fsync pipeline, silent orphan auto-recovery, ext4 hard-required, UPS advisory in DEPLOY.md)
- ✅ Server concurrency model (§66 — 1 image-processor worker, bounded queues only for memory-leak edge cases, silent backpressure, log-only telemetry; ~1 GB peak RSS on Pi 4)
- ✅ Imaging scope: DSO + comets only, planetary/lunar lucky-imaging permanently out of scope (§18.J — Alpaca has no video API; same limit as NINA)
- ✅ Storage permissions bootstrap (§29.1.1 + §29.1.3 + §29.1.4 — server-side helper auto-mounts + chowns + appends fstab; in-app ext4 reformat with label-echo confirmation; narrow sudoers scope; manual DEPLOY.md path remains as advanced fallback)
- ✅ Security model — NO AUTH v0.0.1, matching Alpaca + ASIAir trusted-LAN posture (§67 — threat model, defense without auth, deployment recommendations, v0.1.0 opt-in remote-access mode for over-internet; ~20 cleanup edits across the playbook)
- ✅ Diagnostics default mode — `notify_only` for v0.0.1, opt-in to balanced/aggressive (§51.5 + §51.11 + §51.12 — first-do-no-harm; smart diagnostics still surface in Health Indicator + Diagnostic Panel + per-frame FITS metadata; `balanced` promotes to default in v0.0.2/v0.1.0 once per-user calibration ships)
- ✅ Phase 12 split — 8 sub-PRs (12a–12h) confirmed in COMMIT-PR-RULES.md with augmented mapping integrating all session decisions; CodeRabbit poll-and-fix workflow added (60s polling, AI auto-fixes, user merges)
- ✅ Testing strategy (§14.1–§14.8 — 3 test layers per component, pre-PR gate script, equipment simulators, manual UI verification with screenshots, soft coverage policy; §15 + COMMIT-PR-RULES updated to reference)
- ✅ Flutter WebView on Linux desktop verification (§36.10.5 — Phase 11 verification step + external-browser fallback served by localhost shelf server; offline-aware)
- ✅ Camera cooler warmup at session end (§28.13 — profile setting, 3 modes, off default for modern CMOS, wizard + post-sequence cooldown phase)
- ✅ "Paused sequence" semantics (§28.12 — 15-row matrix; cooler/guider/mount keep running, filter/focuser/rotator hold, in-flight exposure aborts)
- ✅ DST / timezone handling (§31.5 — explicit policy: server UTC throughout, client local via Flutter intl, DST automatic)
- ✅ Mosaic RA wrap (§47.3 — NINA Coordinates math normalizes correctly; integration test in §14.5 prevents regression)
- ✅ §23 bulk-rename sed safety (§23 updated — explicit excludes, dry-run-first pattern, warning about license headers + user-visible strings handled in 0.5f sub-PR)
- ✅ COMMIT-PR-RULES: no skip-review markers (every PR gets full CodeRabbit review including mechanical); minimal `.coderabbit.yaml` excludes only generated code + temp files with focused-review on settings files; no `develop` branch (sub-PR → port/ara → master direct)
- ✅ PHD2-GAP Path A confirmed, deferred to post-Phase-12 (upstream openastro-phd2 PR for 3 real gaps + 7 doc clarifications, filed once ARA Phase 12 sub-PRs 12b + 12c are merged; ARA v0.0.1 ships with workarounds, v0.1.0 switches to new events)
- ✅ Aladin Lite license boundary (GPLv3 via WebView process boundary) (§36.11)
- ✅ NINA-style UI clone with bitmap-asset placeholders (§25)
- ✅ Fork hygiene — naming, identifiers, MPL preservation (§17)
- ✅ Auto-approve safety rails (§19)
- ✅ Quota-resume protocol (§20)
- ✅ Pi Wi-Fi (AP vs Client mode) — punted to OpenAstro wiki (§32.6)
- ✅ Updater (in-app) → DROPPED; replaced by §33 WILMA push (§18.A)
- ✅ Plugin store → deferred to v0.1.0 (§18.B + §10)
- ✅ Telemetry → local logs only, no network (§18.C)
- ✅ Community/branding links → GitHub only (§18.D)
- ✅ Localization → English-only (§18.E)
- ✅ Code signing → ship unsigned, revisit when funded (§18.F)
- ✅ Distribution formats → .deb for server, Flutter handles client per platform (§18.G + §34)
- ✅ Branding assets → placeholders, user supplies (§18.H)
- ✅ Plate solving → ASTAP cross-platform, no PlateSolve2 (§18.I)
- ✅ Stefan-NINA config import on first launch → DROPPED (went headless, then Mac-primary)
- ✅ AvalonDock layout migration → N/A (Flutter, not Avalonia)
- ✅ Avalonia plugin compat shim → N/A (no Avalonia port; no plugin SDK in v0.0.1)
- ✅ Apple Developer ID → defer (§18.F)
- ✅ INDI client from scratch → REJECTED (Alpaca-only via AlpacaBridge)
- ✅ NOTICE.md snippet (§17.2; full text generation TBD per Docs section above)
- ✅ Stefan upstream sync → hard fork, no sync (§17 + §0)
- ✅ Stefan branding strip (§4.3)
- ✅ Two-tap emergency abort vs single-tap → single-tap (§35.3)
- ✅ Pre-built RPi image → rejected in favor of .deb (§34); v0.1.0 reconsider
- ✅ MPC bulk asteroid catalog → deferred to v0.1.0, targeted lookup only in v0.0.1 (§36.8)
- ✅ `safety_alarm.wav` audio source → AI uses public-domain or generated tone (§35.5)

## NINA features to preserve (verify during port; no new section needed unless gap)

These are NINA capabilities the port should preserve as-is from inherited code. The AI verifies during Phase 8 and per-area phases.

- Auto-meridian-flip
- Auto-focus on temperature change (temp comp + per-filter offsets)
- Auto-center after dither
- Sequencer's conditional instructions (`IF`, `WAIT`, `LOOP`)
- Sequencer's recovery instructions
- Sequencer's pause/resume/abort semantics
- Per-target framing + rotation
- HFR + star detection algorithms (Hocus Focus etc.)
- Autofocus curve fitting (parabolic, hyperbolic, trend-line)
- Coordinate transformations (epoch, refraction, parallax)
- FITS header conventions (NINA-specific tags downstream apps recognize)
- Dither cadence configurations
- Filter offsets per-temperature curves

If anything in this list turns out NOT to be preservable during the port, it moves to a Tier 1 or Tier 2 gap.

---

## How to use this file

- **Adding a new gap**: append to the relevant tier with a one-line description.
- **Resolving a gap**: change `[ ]` to `[x]`, move to the **Confirmed resolved** section, add the playbook section number.
- **Closing without baking** (intentional defer or won't-fix): move to Confirmed resolved with a brief reason.
- **Updates as we go**: whenever a discussion in chat covers an item here, capture the resolution in the playbook AND tick it off here.
